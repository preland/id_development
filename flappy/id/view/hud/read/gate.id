// Two visibility gates the UI binds directly.

// The flying score is up on the ready screen and in the air, and comes down
// once the scoreboard takes over.
show_score() {
} return int (import g)[0] != 3;

// The NEW badge belongs to the scoreboard, so it needs both facts: the run is
// over, and it beat the best.
show_new() {
  int r = 0;
  if ((import g)[0] == 3) { r = (import g)[9]; }
} return int r;
