# How each project in this repo stands against the REAL ../idstd.
# Read by tests/idstd_real.sh; see that file for what the states mean.
#
#   both       builds with the library and without it
#   needs-std  builds only with it
#   broken     builds only WITHOUT it -- a collision with the library
#   neither    builds neither way (needs --backend or flags)
#
# Every `broken` line is work, not a permitted state: C9 in docs/IDSTD.md.
# Measured 2026-08-19.

adventure        both
average          needs-std
calc             both
control          both
fsdemo           both
hello            both
idc_in_id_calc   both
idview           both
wordcount        needs-std

# Graphics demos: need --backend, which this file does not pass. Their standing
# against the library is untested until the backend is wired in here.
flyover          neither
fpsmaze          neither
galaxy           neither
gfxdemo          neither
gl3d             neither
gl3dgame         neither

# ---------------------------------------------------------------------------
# The work list. Each of these defines a name idstd also defines, or a function
# whose logic idstd already has. Both are the one-name-one-type rule working:
# the duplicate should go, not the rule.
#
# engine, moonbuggy, solitaire -- the terminal engine and its two games, whose
#   helpers are headed for idstd anyway.
# idc_in_id, idc_in_id_parse -- the compiler's own two stages. bin/idc
#   bootstraps them with --no-std, so this does not break the build; it breaks
#   `bin/idc demos/idc_in_id_parse`, which is how the README says to build them.
# nativeapp/id -- the native-window demo.
engine           broken
idc_in_id        broken
idc_in_id_parse  broken
moonbuggy        broken
solitaire        broken
nativeapp/id     broken
