do_pokes(word p) {
  poke16(p, 4000);
  poke32(p + 8, 300000);
  poke64(p + 16, 5000000000);
} return void;

main() {
  word p = alloc(24);
  do_pokes(p);
  print("" + peek16(p) + " " + peek32(p + 8) + " " + peek64(p + 16));
} return int 0;
