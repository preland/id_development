report(word a, word b) {
  poke64(a, 111);
  poke64(b, 222);
  print("" + a + " " + b + " " + peek64(a) + " " + peek64(b));
} return void;

main() {
  word a = alloc(1);
  word b = alloc(8);
  report(a, b);
} return int 0;
