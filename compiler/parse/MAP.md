# Where everything is

GENERATED by `tools/mapgen.sh` -- do not edit. Regenerate after adding or
moving a function; `tests/run.sh` fails if this file is out of date.

1464 functions. The compiler is a pipeline, and this file is grouped the way
it runs: **lex** turns text into tokens, **front** turns tokens into an AST,
**mid** checks the AST and answers questions about types, **back** turns it
into C. Nothing in `front` or `mid` may emit C -- see
[`../../docs/BACKENDS.md`](../../docs/BACKENDS.md).

To add a language feature, start here, not in `idc.py`. `idc.py` is stage 0
of the bootstrap: it only has to compile *this tree's own source*, so it needs
a new construct only once this compiler uses that construct on itself.

## lex -- source text to tokens

| function | file | what |
| --- | --- | --- |
| `is_space_at` | `compiler/lex/chars/classify/at/at.id` | The character classes, asked of a source position rather than of a byte. |
| `is_digit_at` | `compiler/lex/chars/classify/at/at.id` | The character classes, asked of a source position rather than of a byte. |
| `is_alpha_at` | `compiler/lex/chars/classify/at/at.id` | The character classes, asked of a source position rather than of a byte. |
| `is_alnum_at` | `compiler/lex/chars/classify/at/more.id` | The same shape for the three classes that live with the literal scanners |
| `is_hex_at` | `compiler/lex/chars/classify/at/more.id` | The same shape for the three classes that live with the literal scanners |
| `is_bin_at` | `compiler/lex/chars/classify/at/more.id` | The same shape for the three classes that live with the literal scanners |
| `is_space` | `compiler/lex/chars/classify/charclass.id` | Character-class predicates over byte codes (charat returns a byte code, or |
| `is_digit` | `compiler/lex/chars/classify/charclass.id` | Character-class predicates over byte codes (charat returns a byte code, or |
| `is_alpha` | `compiler/lex/chars/classify/charclass.id` | Character-class predicates over byte codes (charat returns a byte code, or |
| `is_alnum` | `compiler/lex/chars/classify/ident_kw.id` | Identifier-continuation test and keyword recognition. |
| `is_kw` | `compiler/lex/chars/classify/ident_kw.id` | The twelve id keywords. One big boolean keeps this within the action limit. |
| `kindof` | `compiler/lex/chars/classify/ident_kw.id` | Identifier-continuation test and keyword recognition. |
| `scan_one` | `compiler/lex/chars/dispatch/dispatch.id` | whitespace, or hand off |
| `scan_word` | `compiler/lex/chars/dispatch/dispatch.id` | `//` line comment, or hand off |
| `scan_token` | `compiler/lex/chars/dispatch/dispatch.id` | identifier/keyword, or hand off |
| `scan_numop` | `compiler/lex/chars/dispatch/dispatch_tail.id` | hex literal, or hand off to the binary case. Hex is tested before the |
| `scan_strop` | `compiler/lex/chars/dispatch/dispatch_tail.id` | string literal (pass the index past the opening quote), else an operator |
| `skip_comment` | `compiler/lex/chars/dispatch/dispatch_tail.id` | consume to end of line (or end of input) |
| `scan_block` | `compiler/lex/chars/dispatch/more/block.id` | `/* ... */` is not id syntax -- the language has line comments and nothing |
| `block_tok` | `compiler/lex/chars/dispatch/more/block.id` | `/* ... */` is not id syntax -- the language has line comments and nothing |
| `skip_block` | `compiler/lex/chars/dispatch/more/block.id` | eat_space is reused for the step because it is exactly the right one: it |
| `bump_line` | `compiler/lex/chars/dispatch/more/line.id` | Line tracking for diagnostics: the running line number. The store helper it |
| `scan_decop` | `compiler/lex/chars/dispatch/more/number_or_op.id` | decimal number, or hand off |
| `scan_hashop` | `compiler/lex/chars/dispatch/more/number_or_op.id` | A `#<word> <rest>` marker, injected into the stream by the driver. `id` has |
| `scan_hash` | `compiler/lex/chars/dispatch/more/number_or_op.id` | The marker word is not parsed here: dropping the `#` and passing the rest of |
| `is_bin` | `compiler/lex/chars/numlit/bin/bin.id` | Binary literals, the mirror of the hex case: `id` lexes 0b1010 to the |
| `bin_start` | `compiler/lex/chars/numlit/bin/bin.id` | The trailing digit test is what keeps a bare `0` followed by an identifier |
| `bin_end` | `compiler/lex/chars/numlit/bin/bin.id` | Binary literals, the mirror of the hex case: `id` lexes 0b1010 to the |
| `scan_binop` | `compiler/lex/chars/numlit/bin/bin_value.id` | binary literal, or hand off. Tested before the decimal case for the same |
| `scan_bin` | `compiler/lex/chars/numlit/bin/bin_value.id` | i points at the '0'; i + 2 is the first binary digit. |
| `bin_val` | `compiler/lex/chars/numlit/bin/bin_value.id` | Accumulated in a `word` for the reason hex is: 64 binary digits do not fit |
| `num_end` | `compiler/lex/chars/numlit/dec/numkind.id` | Extend a digit run past an optional fractional part, and pick the token |
| `numkind` | `compiler/lex/chars/numlit/dec/numkind.id` | Extend a digit run past an optional fractional part, and pick the token |
| `digit_run_end` | `compiler/lex/chars/numlit/dec/numscan.id` | Digit-run scanning, and the float continuation test: a '.' immediately |
| `has_frac` | `compiler/lex/chars/numlit/dec/numscan.id` | Digit-run scanning, and the float continuation test: a '.' immediately |
| `is_hex` | `compiler/lex/chars/numlit/hex/hex.id` | Hex literals. `id` lexes 0xff to the *integer token* 255 -- hex is a |
| `hex_digit` | `compiler/lex/chars/numlit/hex/hex.id` | Hex literals. `id` lexes 0xff to the *integer token* 255 -- hex is a |
| `hex_alpha` | `compiler/lex/chars/numlit/hex/hex.id` | Hex literals. `id` lexes 0xff to the *integer token* 255 -- hex is a |
| `hex_start` | `compiler/lex/chars/numlit/hex/is_hex.id` | A hex literal is "0", then 'x' or 'X' (120 or 88), then at least one hex |
| `hex_end` | `compiler/lex/chars/numlit/hex/is_hex.id` | A hex literal is "0", then 'x' or 'X' (120 or 88), then at least one hex |
| `hex_val` | `compiler/lex/chars/numlit/hex/is_hex.id` | Accumulated in a `word`, because a 16-digit literal does not fit an int. |
| `scan_hex` | `compiler/lex/chars/numlit/hex/val/hex_value.id` | i points at the '0'; i + 2 is the first hex digit. |
| `udec` | `compiler/lex/chars/numlit/hex/val/hex_value.id` | A word printed as *unsigned* decimal. Plain "" + v prints it signed, so |
| `pad10` | `compiler/lex/chars/numlit/hex/val/hex_value.id` | i points at the '0'; i + 2 is the first hex digit. |
| `print_uint` | `compiler/lex/chars/numlit/hex/val/uint.id` | Printing an unsigned literal's value, shared by the hexadecimal and binary |
| `lex_all` | `compiler/lex/driver/all.id` | Reading the source and lexing it. Two statements, in their own function |
| `main` | `compiler/lex/driver/driver.id` | idc-in-id -- a C transpiler for `id`, written in `id` itself. |
| `eat_space` | `compiler/lex/driver/driver.id` | A `line N` marker is emitted whenever a newline is consumed, so every token |
| `lex` | `compiler/lex/driver/driver.id` | One pass over the source: advance the cursor one token at a time. The whole |
| `two_at` | `compiler/lex/scan/more/at.id` | The one and two character operator spellings at a position, and the choice |
| `one_at` | `compiler/lex/scan/more/at.id` | The one and two character operator spellings at a position, and the choice |
| `scan_op_out` | `compiler/lex/scan/more/at.id` | `one` is built whether or not it is printed. charat is a read of a byte |
| `is_two_op` | `compiler/lex/scan/more/ops.id` | Operator scanning: match the eight two-character operators, else a single |
| `oplen` | `compiler/lex/scan/more/ops.id` | How many characters the operator at `i` spans (2 or 1). Used by scan_op's |
| `scan_op` | `compiler/lex/scan/more/ops.id` | Operator scanning: match the eight two-character operators, else a single |
| `scan_ident_out` | `compiler/lex/scan/more/out/ident.id` | An identifier's token line. `kindof` decides between `ident` and `kw`, and |
| `scan_num_out` | `compiler/lex/scan/more/out/out.id` | Printing a token, split out from the scanner that found its extent. |
| `scan_str_out` | `compiler/lex/scan/more/out/out.id` | The quote is reprinted around the slice, which is why it is built here and |
| `scan_hash_out` | `compiler/lex/scan/more/out/out.id` | Printing a token, split out from the scanner that found its extent. |
| `scan_ident` | `compiler/lex/scan/scan.id` | Token scanners. Each advances its index parameter inside the loop (so no |
| `scan_number` | `compiler/lex/scan/scan.id` | An integer digit run, optionally extended with a '.' + fraction digits |
| `scan_string` | `compiler/lex/scan/scan.id` | Caller passes the index of the first char *after* the opening quote, so the |
| `str_end` | `compiler/lex/scan/strlit.id` | String-literal helpers: find the closing quote while respecting backslash |
| `adv_str` | `compiler/lex/scan/strlit.id` | advance one char, or two when the current char is a backslash (skipping the |
| `slice_str` | `compiler/lex/scan/strlit.id` | String-literal helpers: find the closing quote while respecting backslash |

## front -- tokens to an AST

| function | file | what |
| --- | --- | --- |
| `asm_lines` | `compiler/parse/front/parse/decl/asm/asm_body.id` |  |
| `scan_asm` | `compiler/parse/front/parse/decl/asm/asm_body.id` |  |
| `fill_asm` | `compiler/parse/front/parse/decl/asm/asm_body.id` | The body is a run of string literals and nothing else: an asm function is a |
| `init_asm` | `compiler/parse/front/parse/decl/asm/asm_state.id` | asm functions: a function whose body is instructions rather than statements, |
| `init_asm2` | `compiler/parse/front/parse/decl/asm/asm_state.id` | asm functions: a function whose body is instructions rather than statements, |
| `unquote` | `compiler/parse/front/parse/decl/asm/asm_state.id` | A string token arrives with its quotes; the triple is compared against a |
| `asm_tail` | `compiler/parse/front/parse/decl/asm/tail/asm_tail.id` |  |
| `asm_fin` | `compiler/parse/front/parse/decl/asm/tail/asm_tail.id` |  |
| `init_asm_all` | `compiler/parse/front/parse/decl/asm/tail/asm_tail.id` |  |
| `asm_lines_tail` | `compiler/parse/front/parse/decl/asm/tail/lines.id` | The tail of asm_body.id's asm_lines -- that file's other two functions |
| `asm_tail_mid` | `compiler/parse/front/parse/decl/asm/tail/more.id` | The rest of asm_tail.id's asm_tail, split across two more steps to stay |
| `asm_tail_end` | `compiler/parse/front/parse/decl/asm/tail/more.id` | The rest of asm_tail.id's asm_tail, split across two more steps to stay |
| `init_asm_rest` | `compiler/parse/front/parse/decl/asm/tail/more.id` | The rest of asm_tail.id's asm_tail, split across two more steps to stay |
| `take_cases` | `compiler/parse/front/parse/decl/func/cases/cases.id` | Returns 0 so it can be called from push_func's return clause, which is the |
| `push_case` | `compiler/parse/front/parse/decl/func/cases/cases.id` | A case is recorded as the half-open token range it occupies, so that two |
| `end_case` | `compiler/parse/front/parse/decl/func/cases/cases.id` | Test cases, the lines that follow a function's return clause: |
| `skip_to_rbrack` | `compiler/parse/front/parse/decl/func/cases/more/rbrack.id` | The constraint list's closing bracket. |
| `skip_case` | `compiler/parse/front/parse/decl/func/cases/more/take.id` | (args) : (expected) [constraints] |
| `skip_case_rhs` | `compiler/parse/front/parse/decl/func/cases/more/take.id` | Consuming one case's tokens: `(args) : (expected) [constraints]`. |
| `skip_paren` | `compiler/parse/front/parse/decl/func/cases/skip.id` | Consuming the bracketed groups a case is made of. |
| `skip_to_close` | `compiler/parse/front/parse/decl/func/cases/skip.id` | Consuming the bracketed groups a case is made of. |
| `skip_constraints` | `compiler/parse/front/parse/decl/func/cases/skip.id` | The constraint list is optional: `[time:O(n), mem:O(1)]`. |
| `parse_program` | `compiler/parse/front/parse/decl/func/func.id` | Parse every declaration in the file. An ordinary function is pushed onto |
| `parse_func` | `compiler/parse/front/parse/decl/func/func.id` | function: name ( params ) { body } return TYPE expr ;   (or `return void;`) |
| `func_sig` | `compiler/parse/front/parse/decl/func/func.id` | Parse every declaration in the file. An ordinary function is pushed onto |
| `one_param` | `compiler/parse/front/parse/decl/func/sig/param_ret.id` | One parameter, and the return clause after the closing brace. |
| `func_ret` | `compiler/parse/front/parse/decl/func/sig/param_ret.id` | One parameter, and the return clause after the closing brace. |
| `func_done` | `compiler/parse/front/parse/decl/func/sig/param_ret.id` | One parameter, and the return clause after the closing brace. |
| `parse_params` | `compiler/parse/front/parse/decl/func/sig/params.id` | Parameter list: ( [type name (, type name)*] ) |
| `scan_params` | `compiler/parse/front/parse/decl/func/sig/params.id` | Parameter list: ( [type name (, type name)*] ) |
| `fill_params` | `compiler/parse/front/parse/decl/func/sig/params.id` | Parameter list: ( [type name (, type name)*] ) |
| `ret_expr` | `compiler/parse/front/parse/decl/func/sig/parts/ret_expr.id` | The return expression: none for `void`, otherwise an expression. A void |
| `parse_type` | `compiler/parse/front/parse/decl/func/sig/parts/type.id` | Parse a type: a base keyword (int/float/string/void) plus any number of |
| `add_brackets` | `compiler/parse/front/parse/decl/func/sig/parts/type.id` | Parse a type: a base keyword (int/float/string/void) plus any number of |
| `void_close` | `compiler/parse/front/parse/decl/func/sig/parts/void/scan.id` | One past the parenthesis that closes the one at `lo`. Counted rather than |
| `void_step` | `compiler/parse/front/parse/decl/func/sig/parts/void/scan.id` | One past the parenthesis that closes the one at `lo`. Counted rather than |
| `void_tail` | `compiler/parse/front/parse/decl/func/sig/parts/void/tail.id` | `} return void;` takes no expression, and writing one there is a mistake |
| `void_call` | `compiler/parse/front/parse/decl/func/sig/parts/void/tail.id` | `} return void;` takes no expression, and writing one there is a mistake |
| `void_semi` | `compiler/parse/front/parse/decl/func/sig/parts/void/tail.id` | `} return void;` takes no expression, and writing one there is a mistake |
| `tok_at` | `compiler/parse/front/parse/decl/func/sig/parts/void/tok.id` | The token store by absolute index, for the one place that has to look past |
| `tok_str` | `compiler/parse/front/parse/decl/func/sig/parts/void/tok.id` | The token store by absolute index, for the one place that has to look past |
| `asm_sig_tail` | `compiler/parse/front/parse/decl/top/core/asm_sig_tail.id` | The last step of top.id's asm_sig -- more.id is already full. |
| `parse_asm_mid` | `compiler/parse/front/parse/decl/top/core/more.id` | The rest of top.id's parse_asm and asm_sig, split into further steps to |
| `parse_asm_tail` | `compiler/parse/front/parse/decl/top/core/more.id` | The rest of top.id's parse_asm and asm_sig, split into further steps to |
| `asm_sig_mid` | `compiler/parse/front/parse/decl/top/core/more.id` | The rest of top.id's parse_asm and asm_sig, split into further steps to |
| `parse_top` | `compiler/parse/front/parse/decl/top/core/top.id` | Top level is a marker the driver injected -- a file boundary or a conf.id |
| `parse_asm` | `compiler/parse/front/parse/decl/top/core/top.id` | asm "triple" name(params) { "insn" ... } return TYPE name; |
| `asm_sig` | `compiler/parse/front/parse/decl/top/core/top.id` | Top level is a marker the driver injected -- a file boundary or a conf.id |
| `parse_decl_top` | `compiler/parse/front/parse/decl/top/decl/decl_top.id` | A declaration is either an asm body or an ordinary function; a function is |
| `push_func` | `compiler/parse/front/parse/decl/top/decl/decl_top.id` | Any test cases following the return clause are taken in the return clause |
| `init_files2` | `compiler/parse/front/parse/decl/top/decl/decl_top.id` |   tline   the source line of each stored token, parallel to tkind/ttext |
| `push_func_line` | `compiler/parse/front/parse/decl/top/decl/more.id` | The rest of decl_top.id's push_func, split across two more steps to stay |
| `push_func_body` | `compiler/parse/front/parse/decl/top/decl/more.id` | The rest of decl_top.id's push_func, split across two more steps to stay |
| `init_consts` | `compiler/parse/front/parse/decl/top/marker/const/const.id` | A `#const` marker and the declaration after it: a constant from the |
| `take_const` | `compiler/parse/front/parse/decl/top/marker/const/const.id` | A `#const` marker and the declaration after it: a constant from the |
| `reg_const` | `compiler/parse/front/parse/decl/top/marker/const/const.id` | Registered in the same export tables an in-function `export` fills, so the |
| `init_files` | `compiler/parse/front/parse/decl/top/marker/file/cur_file.id` | Which source file the compiler is currently reading. The pipeline hands |
| `cur_file` | `compiler/parse/front/parse/decl/top/marker/file/cur_file.id` | Which source file the compiler is currently reading. The pipeline hands |
| `take_file` | `compiler/parse/front/parse/decl/top/marker/file/cur_file.id` | The marker carries the unit as well as the path (see unit/split.id): the |
| `cur_ln` | `compiler/parse/front/parse/decl/top/marker/file/cur_line.id` | The source line of the token the cursor is on. |
| `init_lines2` | `compiler/parse/front/parse/decl/top/marker/file/cur_line.id` |   pline  the line each entry of `prog` was declared on |
| `stmt_at_line` | `compiler/parse/front/parse/decl/top/marker/file/cur_line.id` | The source line of the token the cursor is on. |
| `bar_at` | `compiler/parse/front/parse/decl/top/marker/file/unit/split.id` | The two halves of a `#file N|PATH` marker. |
| `file_unit` | `compiler/parse/front/parse/decl/top/marker/file/unit/split.id` | The two halves of a `#file N|PATH` marker. |
| `file_path` | `compiler/parse/front/parse/decl/top/marker/file/unit/split.id` | The path is what every diagnostic prints, so it is exactly the text the |
| `init_units` | `compiler/parse/front/parse/decl/top/marker/file/unit/state.id` | The unit columns. |
| `take_file_mid` | `compiler/parse/front/parse/decl/top/marker/file/unit/state.id` | The rest of cur_file.id's take_file, split across two more steps to stay |
| `take_file_unit` | `compiler/parse/front/parse/decl/top/marker/file/unit/state.id` | The unit columns. |
| `init_files_rest` | `compiler/parse/front/parse/decl/top/marker/init_more.id` | The rest of cur_file.id's init_files -- that file is already full, so |
| `parse_atom` | `compiler/parse/front/parse/expr/atom/kind/atom.id` | Atom dispatch: integer, float, string, or identifier (which may begin a |
| `parse_atom2` | `compiler/parse/front/parse/expr/atom/kind/atom.id` | Atom dispatch: integer, float, string, or identifier (which may begin a |
| `parse_ident_or_call` | `compiler/parse/front/parse/expr/atom/kind/atom.id` | Atom dispatch: integer, float, string, or identifier (which may begin a |
| `parse_atom_float` | `compiler/parse/front/parse/expr/atom/kind/float.id` | Extends the primary-atom dispatch chain (parse_atom -> parse_atom2 -> here |
| `parse_float` | `compiler/parse/front/parse/expr/atom/kind/float.id` | Extends the primary-atom dispatch chain (parse_atom -> parse_atom2 -> here |
| `read_name` | `compiler/parse/front/parse/expr/atom/kind/float.id` | Shared by parse_ident_or_call and parse_import (atom.id, import_expr.id): |
| `parse_atom3` | `compiler/parse/front/parse/expr/atom/kind/import_expr.id` | `import name` (usually written `(import name)`) vs a plain identifier/call. |
| `parse_import` | `compiler/parse/front/parse/expr/atom/kind/import_expr.id` | `import name` (usually written `(import name)`) vs a plain identifier/call. |
| `fill_args` | `compiler/parse/front/parse/expr/atom/lit/call/args_int.id` | Argument list body + the integer leaf. |
| `skip_comma` | `compiler/parse/front/parse/expr/atom/lit/call/args_int.id` | Argument list body + the integer leaf. |
| `parse_int` | `compiler/parse/front/parse/expr/atom/lit/call/args_int.id` | Argument list body + the integer leaf. |
| `finish_atom` | `compiler/parse/front/parse/expr/atom/lit/call/call.id` | After an identifier: a '(' makes it a call, otherwise it is a variable. |
| `parse_call` | `compiler/parse/front/parse/expr/atom/lit/call/call.id` | After an identifier: a '(' makes it a call, otherwise it is a variable. |
| `scan_args` | `compiler/parse/front/parse/expr/atom/lit/call/call.id` | consume '(' args ')', pushing each argument node id onto `args` |
| `finish_int` | `compiler/parse/front/parse/expr/atom/lit/call/finish_int.id` | The tail of args_int.id's parse_int: building the "int" node needs both |
| `dec_val` | `compiler/parse/front/parse/expr/atom/lit/int/bigint.id` | An integer literal that does not fit a 32-bit int is a `word`, not a |
| `dec_step` | `compiler/parse/front/parse/expr/atom/lit/int/bigint.id` | An integer literal that does not fit a 32-bit int is a `word`, not a |
| `over_max` | `compiler/parse/front/parse/expr/atom/lit/int/bigint.id` | Only ever asked about literals of 10 digits or fewer, where the accumulator |
| `is_bigdec` | `compiler/parse/front/parse/expr/atom/lit/int/is_bigdec.id` | Is this literal too wide for an int? Eleven significant digits or more is |
| `wide_dec` | `compiler/parse/front/parse/expr/atom/lit/int/is_bigdec.id` | Is this literal too wide for an int? Eleven significant digits or more is |
| `strip_zeros` | `compiler/parse/front/parse/expr/atom/lit/int/is_bigdec.id` | Leading zeros are not significant digits; "007" is 7, not a wide literal. |
| `zeros_tail` | `compiler/parse/front/parse/expr/atom/lit/int/zeros_tail.id` | The tail of is_bigdec.id's strip_zeros: slicing off the run of leading |
| `parse_arr` | `compiler/parse/front/parse/expr/atom/lit/more/array_lit.id` | Array literal: `[ expr (, expr)* ]` (or `[]`). Builds an "arr" node whose |
| `scan_elems` | `compiler/parse/front/parse/expr/atom/lit/more/array_lit.id` | Array literal: `[ expr (, expr)* ]` (or `[]`). Builds an "arr" node whose |
| `fill_elems` | `compiler/parse/front/parse/expr/atom/lit/more/array_lit.id` | Array literal: `[ expr (, expr)* ]` (or `[]`). Builds an "arr" node whose |
| `is_mulop` | `compiler/parse/front/parse/expr/atom/lit/more/mulop.id` | Remaining operator-class predicate, and its lookahead wrapper alongside |
| `mulop_at` | `compiler/parse/front/parse/expr/atom/lit/more/mulop.id` | Remaining operator-class predicate, and its lookahead wrapper alongside |
| `addop_at` | `compiler/parse/front/parse/expr/atom/lit/more/mulop.id` | Remaining operator-class predicate, and its lookahead wrapper alongside |
| `parse_str` | `compiler/parse/front/parse/expr/atom/lit/more/str_ops.id` | The string leaf, and the operator-class predicates. |
| `is_relop` | `compiler/parse/front/parse/expr/atom/lit/more/str_ops.id` | `=` is included: in an expression a bare `=` means equality in id (assignment |
| `is_addop` | `compiler/parse/front/parse/expr/atom/lit/more/str_ops.id` | The string leaf, and the operator-class predicates. |
| `fold_mul_tail` | `compiler/parse/front/parse/expr/atom/primary/more.id` | The tail of primary.id's fold_mul -- see ladder/arith/fold/more.id for |
| `fold_mul` | `compiler/parse/front/parse/expr/atom/primary/primary.id` | multiplicative fold + the primary production. |
| `parse_primary` | `compiler/parse/front/parse/expr/atom/primary/primary.id` | primary: '(' expr ')' | '[' array literal | INT | STR | ident (var or call) |
| `parse_paren` | `compiler/parse/front/parse/expr/atom/primary/primary.id` | multiplicative fold + the primary production. |
| `parse_expr` | `compiler/parse/front/parse/expr/core/expr.id` | Expression parser (precedence climbing). Each level parses the tighter level |
| `parse_rel` | `compiler/parse/front/parse/expr/core/expr.id` | relational / equality: bitor (('=='|'!='|'<'|'>'|'<='|'>=') bitor)* |
| `fold_rel` | `compiler/parse/front/parse/expr/core/expr.id` | Expression parser (precedence climbing). Each level parses the tighter level |
| `fold_rel_tail` | `compiler/parse/front/parse/expr/core/more.id` | The tail of expr.id's fold_rel -- see ladder/arith/fold/more.id for why |
| `parse_add` | `compiler/parse/front/parse/expr/ladder/arith/fold/add_mul.id` | additive and multiplicative levels. |
| `fold_add` | `compiler/parse/front/parse/expr/ladder/arith/fold/add_mul.id` | additive and multiplicative levels. |
| `parse_mul` | `compiler/parse/front/parse/expr/ladder/arith/fold/add_mul.id` | additive and multiplicative levels. |
| `fold_add_tail` | `compiler/parse/front/parse/expr/ladder/arith/fold/more.id` | The tails of add_mul.id's and unary.id's fold/dispatch steps. Splitting |
| `unary_or_postfix` | `compiler/parse/front/parse/expr/ladder/arith/fold/more.id` | The tails of add_mul.id's and unary.id's fold/dispatch steps. Splitting |
| `fold_unary_tail` | `compiler/parse/front/parse/expr/ladder/arith/fold/more.id` | The tails of add_mul.id's and unary.id's fold/dispatch steps. Splitting |
| `parse_unary` | `compiler/parse/front/parse/expr/ladder/arith/fold/unary.id` | Unary level: a prefix `-`, `!` or `~` applied to a unary (right-recursive), |
| `fold_unary` | `compiler/parse/front/parse/expr/ladder/arith/fold/unary.id` | Unary level: a prefix `-`, `!` or `~` applied to a unary (right-recursive), |
| `is_unop` | `compiler/parse/front/parse/expr/ladder/arith/fold/unary.id` | Unary level: a prefix `-`, `!` or `~` applied to a unary (right-recursive), |
| `parse_postfix` | `compiler/parse/front/parse/expr/ladder/arith/postfix.id` | Postfix level: a primary followed by any number of `[ expr ]` index |
| `fold_index` | `compiler/parse/front/parse/expr/ladder/arith/postfix.id` | Postfix level: a primary followed by any number of `[ expr ]` index |
| `fold_index_tail` | `compiler/parse/front/parse/expr/ladder/arith/postfix.id` | Postfix level: a primary followed by any number of `[ expr ]` index |
| `fold_bitxor` | `compiler/parse/front/parse/expr/ladder/bits/bitand.id` | xor's fold step, and the and level. |
| `parse_bitand` | `compiler/parse/front/parse/expr/ladder/bits/bitand.id` | xor's fold step, and the and level. |
| `fold_bitand` | `compiler/parse/front/parse/expr/ladder/bits/bitand.id` | xor's fold step, and the and level. |
| `parse_bitor` | `compiler/parse/front/parse/expr/ladder/bits/bitor.id` | The bitwise levels of the precedence ladder. They sit *below* the |
| `fold_bitor` | `compiler/parse/front/parse/expr/ladder/bits/bitor.id` | The bitwise levels of the precedence ladder. They sit *below* the |
| `parse_bitxor` | `compiler/parse/front/parse/expr/ladder/bits/bitor.id` | The bitwise levels of the precedence ladder. They sit *below* the |
| `fold_shift_tail` | `compiler/parse/front/parse/expr/ladder/bits/shift/more.id` | The tail of shift.id's fold step -- see arith/fold/more.id for why the |
| `parse_shift` | `compiler/parse/front/parse/expr/ladder/bits/shift/shift.id` | The shift level -- the tightest of the bitwise group, just above additive. |
| `fold_shift` | `compiler/parse/front/parse/expr/ladder/bits/shift/shift.id` | The shift level -- the tightest of the bitwise group, just above additive. |
| `is_shiftop` | `compiler/parse/front/parse/expr/ladder/bits/shift/shift.id` | `>>` is the arithmetic (sign-propagating) shift; the logical one is the |
| `parse_and` | `compiler/parse/front/parse/expr/ladder/logic/and.id` | Logical-and level, between or and equality/relational. |
| `fold_and` | `compiler/parse/front/parse/expr/ladder/logic/and.id` | Logical-and level, between or and equality/relational. |
| `relop_at` | `compiler/parse/front/parse/expr/ladder/logic/lookahead.id` | A call may not be an argument to a call, so a `while` condition that reads |
| `shiftop_at` | `compiler/parse/front/parse/expr/ladder/logic/lookahead.id` | A call may not be an argument to a call, so a `while` condition that reads |
| `parse_or` | `compiler/parse/front/parse/expr/ladder/logic/or.id` | Logical-or level (lowest precedence), above and/equality. Mirrors idc.py: |
| `fold_or` | `compiler/parse/front/parse/expr/ladder/logic/or.id` | Logical-or level (lowest precedence), above and/equality. Mirrors idc.py: |
| `parse_block` | `compiler/parse/front/parse/stmt/block.id` | A block is `{ stmt* }`, returning the list of statement node ids. |
| `scan_block` | `compiler/parse/front/parse/stmt/block.id` | A block is `{ stmt* }`, returning the list of statement node ids. |
| `fill_block` | `compiler/parse/front/parse/stmt/block.id` | Each statement records the line it began on, captured before it is parsed |
| `parse_stmt4` | `compiler/parse/front/parse/stmt/dispatch/decl_start.id` | The leaf of statement dispatch: assignment vs expression statement, plus the |
| `is_decl_start` | `compiler/parse/front/parse/stmt/dispatch/decl_start.id` | a declaration starts with `export` or a type keyword |
| `is_type` | `compiler/parse/front/parse/stmt/dispatch/decl_start.id` | The leaf of statement dispatch: assignment vs expression statement, plus the |
| `parse_stmt` | `compiler/parse/front/parse/stmt/dispatch/dispatch.id` | Statement dispatch: if / while / declaration / assignment / expression-stmt. |
| `parse_stmt2` | `compiler/parse/front/parse/stmt/dispatch/dispatch.id` | Statement dispatch: if / while / declaration / assignment / expression-stmt. |
| `parse_stmt3` | `compiler/parse/front/parse/stmt/dispatch/dispatch.id` | Statement dispatch: if / while / declaration / assignment / expression-stmt. |
| `parse_stmt5` | `compiler/parse/front/parse/stmt/dispatch/more/index_or_expr.id` | Leaf of statement dispatch (after assignment): index-assignment |
| `pp2` | `compiler/parse/front/parse/stmt/dispatch/more/index_or_expr.id` | Leaf of statement dispatch (after assignment): index-assignment |
| `is_assign` | `compiler/parse/front/parse/stmt/dispatch/more/is_assign.id` | `name = ...` is an assignment; anything else starting here is an expression |
| `cur2_text` | `compiler/parse/front/parse/stmt/dispatch/more/is_assign.id` | `name = ...` is an assignment; anything else starting here is an expression |
| `skip_semi` | `compiler/parse/front/parse/stmt/dispatch/more/is_assign.id` | `name = ...` is an assignment; anything else starting here is an expression |
| `else_body` | `compiler/parse/front/parse/stmt/form/branch/else_if.id` | What follows an `else`: either a nested if (an `else if` chain) or a block. |
| `at_elif` | `compiler/parse/front/parse/stmt/form/branch/else_if.id` | 0 = no else arm, 1 = `else if`, 2 = a plain `else { ... }`. The third case |
| `if_tail` | `compiler/parse/front/parse/stmt/form/branch/else_if.id` | What follows an `else`: either a nested if (an `else if` chain) or a block. |
| `parse_exprstmt` | `compiler/parse/front/parse/stmt/form/branch/exprstmt_if.id` | Expression statement, and the start of an if. The `( expr )` of a condition |
| `parse_if` | `compiler/parse/front/parse/stmt/form/branch/exprstmt_if.id` | Expression statement, and the start of an if. The `( expr )` of a condition |
| `if_body` | `compiler/parse/front/parse/stmt/form/branch/if_while.id` | The else-if test has to happen before the else arm is parsed, because |
| `parse_else` | `compiler/parse/front/parse/stmt/form/branch/if_while.id` | The body of an if (then block + optional else block), and while. |
| `parse_while` | `compiler/parse/front/parse/stmt/form/branch/if_while.id` | The body of an if (then block + optional else block), and while. |
| `decl_rhs` | `compiler/parse/front/parse/stmt/form/decl/assign.id` | Declaration right-hand side, and assignment. |
| `parse_assign` | `compiler/parse/front/parse/stmt/form/decl/assign.id` | Declaration right-hand side, and assignment. |
| `assign_rhs` | `compiler/parse/front/parse/stmt/form/decl/assign.id` | Declaration right-hand side, and assignment. |
| `parse_decl` | `compiler/parse/front/parse/stmt/form/decl/decl.id` | Declaration: [export] type name = expr ; |
| `take_export` | `compiler/parse/front/parse/stmt/form/decl/decl.id` | Declaration: [export] type name = expr ; |
| `finish_decl` | `compiler/parse/front/parse/stmt/form/decl/decl.id` | Declaration: [export] type name = expr ; |
| `is_iassign` | `compiler/parse/front/parse/stmt/form/decl/index_assign.id` | Index-assignment: an identifier followed by `[` begins a list-element |
| `parse_iassign` | `compiler/parse/front/parse/stmt/form/decl/index_assign.id` | Index-assignment: an identifier followed by `[` begins a list-element |
| `iassign_rhs` | `compiler/parse/front/parse/stmt/form/decl/index_assign.id` | Index-assignment: an identifier followed by `[` begins a list-element |
| `parse_while_tail` | `compiler/parse/front/parse/stmt/form/more.id` | Tails of three split functions, one from each of branch/if_while.id, |
| `parse_assign_tail` | `compiler/parse/front/parse/stmt/form/more.id` | Tails of three split functions, one from each of branch/if_while.id, |
| `finish_decl_tail` | `compiler/parse/front/parse/stmt/form/more.id` | Tails of three split functions, one from each of branch/if_while.id, |
| `cur_kind` | `compiler/parse/front/tokens/cursor.id` | Cursor over the token store. `pos` is a one-element int[] cell passed by |
| `cur_text` | `compiler/parse/front/tokens/cursor.id` | Cursor over the token store. `pos` is a one-element int[] cell passed by |
| `advance` | `compiler/parse/front/tokens/cursor.id` | Consuming a token records its line. Every node is built immediately after |
| `eat_eq` | `compiler/parse/front/tokens/expect/eat_eq.id` | A declaration must initialise: `int x = 1`, never `int x`. |
| `init_lines3` | `compiler/parse/front/tokens/expect/eat_eq.id` |   chkfail  set by any failing check or syntax error; suppresses emission |
| `init_lines4` | `compiler/parse/front/tokens/expect/eat_eq.id` |   casefn     the function node each case was written under |
| `syn_err` | `compiler/parse/front/tokens/expect/syn_err.id` | Syntax diagnostics. Without these the parser silently mis-parses malformed |
| `expect_text` | `compiler/parse/front/tokens/expect/syn_err.id` | Syntax diagnostics. Without these the parser silently mis-parses malformed |
| `no_return_here` | `compiler/parse/front/tokens/expect/syn_err.id` | `id` puts the return clause after the closing brace, so a `return` where a |
| `no_op_atom` | `compiler/parse/front/tokens/expect/unexpected.id` | An operator where an expression belongs. |
| `unexpected_msg` | `compiler/parse/front/tokens/expect/unexpected.id` | The lexer folds a whole `/* ... */` into one `/*` token precisely so this |
| `syn_err_tail` | `compiler/parse/front/tokens/expect/unexpected.id` | The tail of syn_err.id's syn_err -- that file is already full. |
| `load` | `compiler/parse/front/tokens/load/load.id` | Loader: read the lexer's "<kind> <value>" lines from stdin into the token |
| `load_at` | `compiler/parse/front/tokens/load/load.id` | Loader: read the lexer's "<kind> <value>" lines from stdin into the token |
| `take_line` | `compiler/parse/front/tokens/load/load.id` | Loader: read the lexer's "<kind> <value>" lines from stdin into the token |
| `slice` | `compiler/parse/front/tokens/load/more/push_tok.id` | substring + token append. |
| `push_tok` | `compiler/parse/front/tokens/load/more/push_tok.id` | A `line N` marker is not a token: it updates the current line and is |
| `push_real` | `compiler/parse/front/tokens/load/more/push_tok.id` | substring + token append. |
| `src_line` | `compiler/parse/front/tokens/load/more/src_line.id` | The line the next stored token belongs to. |
| `push_line` | `compiler/parse/front/tokens/load/more/src_line.id` | The tail of push_tok.id's push_real -- that file is already full. |
| `line_end` | `compiler/parse/front/tokens/load/split_line.id` | Line helpers. |
| `proc_line` | `compiler/parse/front/tokens/load/split_line.id` | the line src[i..e) becomes a token: kind before the first space, text after |
| `find_space` | `compiler/parse/front/tokens/load/split_line.id` | Line helpers. |
| `init_rest` | `compiler/parse/front/tree/init/init_more.id` | Remaining exported state: the second string field, the two child-list |
| `init_c` | `compiler/parse/front/tree/init/init_more.id` | Remaining exported state: the second string field, the two child-list |
| `init_d` | `compiler/parse/front/tree/init/init_more.id` | Remaining exported state: the second string field, the two child-list |
| `init_sym` | `compiler/parse/front/tree/init/init_sym.id` | Symbol tables for the type pass, plus the exported-variable table. |
| `init_sym_a` | `compiler/parse/front/tree/init/init_sym.id` | Symbol tables for the type pass, plus the exported-variable table. |
| `init_sym_b` | `compiler/parse/front/tree/init/init_sym.id` | Symbol tables for the type pass, plus the exported-variable table. |
| `setup` | `compiler/parse/front/tree/init/setup/init.id` | idc-in-id, stage 2b: a parser for id FUNCTIONS and STATEMENTS, written in id. |
| `init_a` | `compiler/parse/front/tree/init/setup/init.id` | idc-in-id, stage 2b: a parser for id FUNCTIONS and STATEMENTS, written in id. |
| `init_b` | `compiler/parse/front/tree/init/setup/init.id` | idc-in-id, stage 2b: a parser for id FUNCTIONS and STATEMENTS, written in id. |
| `setup_rest` | `compiler/parse/front/tree/init/setup/more.id` | The tail of init.id's setup -- that file is already full. |
| `node_index` | `compiler/parse/front/tree/make/array/array.id` | Constructors for the array/index nodes. |
| `node_arr` | `compiler/parse/front/tree/make/array/array.id` | Constructors for the array/index nodes. |
| `node_iassign` | `compiler/parse/front/tree/make/array/array.id` | Constructors for the array/index nodes. |
| `node_iassign_tail` | `compiler/parse/front/tree/make/array/more.id` | The tail of array.id's node_iassign -- that file is already full. |
| `node_import` | `compiler/parse/front/tree/make/expr/import_float.id` | `(import name)` reads an exported global. It emits as just the name (the C |
| `node_float` | `compiler/parse/front/tree/make/expr/import_float.id` | The float leaf: like the str leaf, its raw source text (e.g. "0.8") is kept |
| `node_int` | `compiler/parse/front/tree/make/expr/literal.id` | Expression-leaf constructors. |
| `node_var` | `compiler/parse/front/tree/make/expr/literal.id` | Expression-leaf constructors. |
| `node_str` | `compiler/parse/front/tree/make/expr/literal.id` | Expression-leaf constructors. |
| `node_un` | `compiler/parse/front/tree/make/expr/unary.id` | Unary-operator node: ns1 = op ("-" or "!"), ni1 = operand expr id. |
| `node_bin` | `compiler/parse/front/tree/make/stmt/bin_assign.id` | binop + simple statement constructors. |
| `node_assign` | `compiler/parse/front/tree/make/stmt/bin_assign.id` | binop + simple statement constructors. |
| `node_exprstmt` | `compiler/parse/front/tree/make/stmt/bin_assign.id` | binop + simple statement constructors. |
| `node_decl` | `compiler/parse/front/tree/make/stmt/decl_call.id` | decl + param + call constructors. exported is 1 for `export`, else 0. |
| `node_param` | `compiler/parse/front/tree/make/stmt/decl_call.id` | decl + param + call constructors. exported is 1 for `export`, else 0. |
| `node_call` | `compiler/parse/front/tree/make/stmt/decl_call.id` | decl + param + call constructors. exported is 1 for `export`, else 0. |
| `node_if` | `compiler/parse/front/tree/make/stmt/if_while_func.id` | `elif` is 1 when the else arm was written `else if`, and 0 when it was |
| `node_while` | `compiler/parse/front/tree/make/stmt/if_while_func.id` | compound-statement + function constructors. |
| `node_func` | `compiler/parse/front/tree/make/stmt/if_while_func.id` | compound-statement + function constructors. |
| `i1_of` | `compiler/parse/front/tree/node/field/field.id` | Field accessors, part 1. |
| `i2_of` | `compiler/parse/front/tree/node/field/field.id` | Field accessors, part 1. |
| `s1_of` | `compiler/parse/front/tree/node/field/field.id` | Field accessors, part 1. |
| `s2_of` | `compiler/parse/front/tree/node/field/field_more.id` | Field accessors, part 2 (the second string field and the two child lists). |
| `l1_of` | `compiler/parse/front/tree/node/field/field_more.id` | Field accessors, part 2 (the second string field and the two child lists). |
| `l2_of` | `compiler/parse/front/tree/node/field/field_more.id` | Field accessors, part 2 (the second string field and the two child lists). |
| `newnode_tail` | `compiler/parse/front/tree/node/newnode/more.id` | The tail of newnode.id's newnode -- that file is already full. |
| `newnode` | `compiler/parse/front/tree/node/newnode/newnode.id` | newnode appends a full node and returns its id. newleaf is the common case |
| `newleaf` | `compiler/parse/front/tree/node/newnode/newnode.id` | newnode appends a full node and returns its id. newleaf is the common case |
| `k_of` | `compiler/parse/front/tree/node/newnode/newnode.id` | newnode appends a full node and returns its id. newleaf is the common case |
| `push_kii` | `compiler/parse/front/tree/node/push_node.id` | Appending a node: push one cell onto each of the seven parallel lists. |
| `push_ss` | `compiler/parse/front/tree/node/push_node.id` | nunit rides along here for the same reason nline rides along below: every |
| `push_ll` | `compiler/parse/front/tree/node/push_node.id` | nline keeps the parallel lists aligned: every node gets a line slot, filled |

## mid -- checks, types, symbol tables

| function | file | what |
| --- | --- | --- |
| `exp_one` | `compiler/parse/mid/names/exports/collect/add.id` | An exported declaration is recorded; other statements may nest exports. |
| `is_export_decl` | `compiler/parse/mid/names/exports/collect/add.id` | An exported declaration is recorded; other statements may nest exports. |
| `add_export` | `compiler/parse/mid/names/exports/collect/add.id` | An exported declaration is recorded; other statements may nest exports. |
| `collect_exports` | `compiler/parse/mid/names/exports/collect/collect.id` | Collect exported variables (with their owning function) by scanning each |
| `scan_func_exports` | `compiler/parse/mid/names/exports/collect/collect.id` | Collect exported variables (with their owning function) by scanning each |
| `exp_list` | `compiler/parse/mid/names/exports/collect/collect.id` | Collect exported variables (with their owning function) by scanning each |
| `add_export2` | `compiler/parse/mid/names/exports/collect/more/duplicate.id` | A name may be exported only once (idc.py's pass 1 over exported |
| `add_export2_rec` | `compiler/parse/mid/names/exports/collect/more/duplicate.id` | A name may be exported only once (idc.py's pass 1 over exported |
| `chk_dupexp` | `compiler/parse/mid/names/exports/collect/more/duplicate.id` | A name may be exported only once (idc.py's pass 1 over exported |
| `dupexp_err` | `compiler/parse/mid/names/exports/collect/more/report.id` | The duplicate is recorded anyway, even though it was just rejected: the |
| `dupexp_report` | `compiler/parse/mid/names/exports/collect/more/report.id` | The duplicate is recorded anyway, even though it was just rejected: the |
| `builtin_src` | `compiler/parse/mid/names/exports/extern/builtin/more/list.id` | The literal, and the comma-joined spelling the diagnostic prints. |
| `builtin_list` | `compiler/parse/mid/names/exports/extern/builtin/more/list.id` | The literal, and the comma-joined spelling the diagnostic prints. |
| `join_bnames` | `compiler/parse/mid/names/exports/extern/builtin/more/list.id` | The literal, and the comma-joined spelling the diagnostic prints. |
| `init_bnames` | `compiler/parse/mid/names/exports/extern/builtin/more/names.id` | The builtin names, as data. |
| `split_bnames` | `compiler/parse/mid/names/exports/extern/builtin/more/names.id` | The builtin names, as data. |
| `take_bname` | `compiler/parse/mid/names/exports/extern/builtin/more/names.id` | The builtin names, as data. |
| `extern_type` | `compiler/parse/mid/names/exports/extern/builtin/more/type.id` | The type of a call that no input file defines. |
| `bname_end` | `compiler/parse/mid/names/exports/extern/builtin/more/type.id` | The end of the name starting at i, and the name itself, split so that |
| `push_bname` | `compiler/parse/mid/names/exports/extern/builtin/more/type.id` | The type of a call that no input file defines. |
| `is_builtin` | `compiler/parse/mid/names/exports/extern/builtin/report.id` | What to do with an unresolved name, and the message when it is an error. |
| `report_unknown_fn` | `compiler/parse/mid/names/exports/extern/builtin/report.id` | With a backend attached the name is the linker's to find, so it is recorded |
| `report_nosuchfn` | `compiler/parse/mid/names/exports/extern/builtin/report.id` | idc.py also offers a "did you mean the builtin 'X'?" hint here, computed |
| `acc_callee2` | `compiler/parse/mid/names/exports/extern/builtin/unresolved.id` | The check that finds a name no function, no builtin and no variable owns. |
| `is_unresolved_call` | `compiler/parse/mid/names/exports/extern/builtin/unresolved.id` | Naming the composition, not just the call inside it: a call to the right |
| `unresolved_fn` | `compiler/parse/mid/names/exports/extern/builtin/unresolved.id` | Deliberately not is_unknown_fn: that one names the builtins it knows about |
| `note_extern` | `compiler/parse/mid/names/exports/extern/note.id` | First encounter wins: a name called from five places is declared once. |
| `is_runtime` | `compiler/parse/mid/names/exports/extern/note.id` | Whether this project is the runtime. Read by the reserved-name check and by |
| `nosuchfn_report` | `compiler/parse/mid/names/exports/extern/note.id` | Split out of report_nosuchfn so that block stays within the action limit; |
| `xok_set` | `compiler/parse/mid/names/exports/extern/tab/flags.id` | An exported scalar may only be assigned by its declaring function, so the |
| `extern_ok` | `compiler/parse/mid/names/exports/extern/tab/flags.id` | The two build flags the checks read. Neither is a property of the source: |
| `fs_set` | `compiler/parse/mid/names/exports/extern/tab/fs.id` | Whether the build is freestanding: no host, so no C-ABI entry point and no |
| `is_freestanding` | `compiler/parse/mid/names/exports/extern/tab/fs.id` | Whether the build is freestanding: no host, so no C-ABI entry point and no |
| `init_xtab` | `compiler/parse/mid/names/exports/extern/tab/table.id` | The extern table: names a program calls that no input file defines. |
| `init_rtc` | `compiler/parse/mid/names/exports/extern/tab/table.id` | The extern table: names a program calls that no input file defines. |
| `rt_set` | `compiler/parse/mid/names/exports/extern/tab/table.id` | The extern table: names a program calls that no input file defines. |
| `exp_else` | `compiler/parse/mid/names/exports/reach/branch.id` | The else side of an if, split from exp_branches so that block stays |
| `chk_dead` | `compiler/parse/mid/names/exports/reach/dead/check.id` | An export whose declaring function is never reached. |
| `init_reach` | `compiler/parse/mid/names/exports/reach/dead/check.id` | Seeded with "main" unconditionally. A project without one is a library, and |
| `reach_mark` | `compiler/parse/mid/names/exports/reach/dead/check.id` | Fixpoint: sweep until a sweep adds nothing. The number of sweeps is the |
| `reach_add` | `compiler/parse/mid/names/exports/reach/dead/more/add.id` | Only names this program actually defines are added: a builtin and a |
| `dead_final` | `compiler/parse/mid/names/exports/reach/dead/more/add.id` | One more sweep with the imports switched on, now that the reachable set is |
| `chk_import_at` | `compiler/parse/mid/names/exports/reach/dead/more/report.id` | A read of an exported name whose owner is not reachable. Reached only from |
| `is_reach_owner` | `compiler/parse/mid/names/exports/reach/dead/more/report.id` | Whether the function that owns the export id reads is reachable. Named so |
| `dead_err` | `compiler/parse/mid/names/exports/reach/dead/more/report.id` | s1_of(id) and export_owner(s1_of_v) are read once and reused: nothing |
| `reach_range` | `compiler/parse/mid/names/exports/reach/dead/more/scan.id` |  |
| `scan_reach` | `compiler/parse/mid/names/exports/reach/dead/more/scan.id` | The leaf of the walk, doing both jobs so that one walker serves both passes. |
| `is_reach` | `compiler/parse/mid/names/exports/reach/dead/more/scan.id` |  |
| `reach_pass` | `compiler/parse/mid/names/exports/reach/dead/sweep.id` | Returns the set's size *before* the sweep, so reach_mark stops when a sweep |
| `reach_funcs` | `compiler/parse/mid/names/exports/reach/dead/sweep.id` | One sweep, and the walk it is built on. |
| `reach_func` | `compiler/parse/mid/names/exports/reach/dead/sweep.id` | One sweep, and the walk it is built on. |
| `exp_nested` | `compiler/parse/mid/names/exports/reach/nested.id` | Recurse into compound statements when collecting exports. |
| `exp_branches` | `compiler/parse/mid/names/exports/reach/nested.id` | Recurse into compound statements when collecting exports. |
| `exp_loop` | `compiler/parse/mid/names/exports/reach/nested.id` | Recurse into compound statements when collecting exports. |
| `check_shape` | `compiler/parse/mid/names/limits/expr/expr.id` | Two rules about the shape of an expression. Both say the same thing: a |
| `shape_node` | `compiler/parse/mid/names/limits/expr/expr.id` | Two rules about the shape of an expression. Both say the same thing: a |
| `chk_ret_name` | `compiler/parse/mid/names/limits/expr/expr.id` | ni1 is -1 for `} return void;`, which has no expression to judge. |
| `hc_kids` | `compiler/parse/mid/names/limits/expr/more/kind.id` | Descending into the kinds that have children, and naming a kind in English |
| `hc_more` | `compiler/parse/mid/names/limits/expr/more/kind.id` | Descending into the kinds that have children, and naming a kind in English |
| `hc_pair` | `compiler/parse/mid/names/limits/expr/more/kind.id` | Descending into the kinds that have children, and naming a kind in English |
| `hc_list` | `compiler/parse/mid/names/limits/expr/more/list.id` | The elements of a list literal, and what counts as plain enough to end a |
| `hc_at` | `compiler/parse/mid/names/limits/expr/more/list.id` | The elements of a list literal, and what counts as plain enough to end a |
| `is_plain` | `compiler/parse/mid/names/limits/expr/more/list.id` | The elements of a list literal, and what counts as plain enough to end a |
| `ret_shape_err` | `compiler/parse/mid/names/limits/expr/more/report.id` | The diagnostic for a return clause that is not a name or a literal. |
| `shape_word` | `compiler/parse/mid/names/limits/expr/more/report.id` | The diagnostic for a return clause that is not a name or a literal. |
| `flat_arg_count` | `compiler/parse/mid/names/limits/expr/more/report.id` | How many arguments a call's own argument list holds, named so the while |
| `chk_flat_args` | `compiler/parse/mid/names/limits/expr/nest.id` | A call's arguments, each of which must be free of calls. |
| `chk_flat_at` | `compiler/parse/mid/names/limits/expr/nest.id` | A call's arguments, each of which must be free of calls. |
| `has_call` | `compiler/parse/mid/names/limits/expr/nest.id` | A call's arguments, each of which must be free of calls. |
| `check_files` | `compiler/parse/mid/names/limits/report/file_count.id` | The 3-functions-per-file rule. The pipeline concatenates every source file |
| `check_file_at` | `compiler/parse/mid/names/limits/report/file_count.id` | Reported once per file rather than once per function over the limit. |
| `count_file` | `compiler/parse/mid/names/limits/report/file_count.id` | The 3-functions-per-file rule. The pipeline concatenates every source file |
| `file_hit` | `compiler/parse/mid/names/limits/report/more/file_funcs.id` |  |
| `first_of_file` | `compiler/parse/mid/names/limits/report/more/file_funcs.id` |  |
| `report_funcs` | `compiler/parse/mid/names/limits/report/more/file_funcs.id` |  |
| `first_fn` | `compiler/parse/mid/names/limits/report/more/loc/first_fn.id` | Locating a function by its position in `prog` rather than by its name. |
| `first_fn_at` | `compiler/parse/mid/names/limits/report/more/loc/first_fn.id` | Locating a function by its position in `prog` rather than by its name. |
| `prog_loc` | `compiler/parse/mid/names/limits/report/more/loc/first_fn.id` | Locating a function by its position in `prog` rather than by its name. |
| `func_file` | `compiler/parse/mid/names/limits/report/more/loc/func_file.id` | The file a function was declared in, from the parallel `pfile` list. |
| `func_file_at` | `compiler/parse/mid/names/limits/report/more/loc/func_file.id` | The file a function was declared in, from the parallel `pfile` list. |
| `func_line` | `compiler/parse/mid/names/limits/report/more/loc/func_line.id` | The line a function was declared on, from the parallel `pline` list. |
| `func_line_at` | `compiler/parse/mid/names/limits/report/more/loc/func_line.id` | The line a function was declared on, from the parallel `pline` list. |
| `loc_at` | `compiler/parse/mid/names/limits/report/more/prefix.id` | The `file:line: error: ` prefix for a diagnostic about function `fname`. |
| `check_plain_else` | `compiler/parse/mid/names/limits/report/report.id` | A trailing `else { ... }`, and the two diagnostics. |
| `report_actions` | `compiler/parse/mid/names/limits/report/report.id` | A trailing `else { ... }`, and the two diagnostics. |
| `report_deep` | `compiler/parse/mid/names/limits/report/report.id` | A trailing `else { ... }`, and the two diagnostics. |
| `count_actions` | `compiler/parse/mid/names/limits/shape/actions/count.id` | Counting the actions of a block, mirroring idc.py's block_actions. |
| `stmt_actions` | `compiler/parse/mid/names/limits/shape/actions/count.id` | Counting the actions of a block, mirroring idc.py's block_actions. |
| `count_elses` | `compiler/parse/mid/names/limits/shape/actions/count.id` | ni2 is 0 when the if had no else arm at all. An *empty* `else { }` has an |
| `chain_elses` | `compiler/parse/mid/names/limits/shape/actions/else_chain.id` | Following an `else if` chain. The else arm of an `else if` holds exactly one |
| `check_children` | `compiler/parse/mid/names/limits/shape/actions/else_chain.id` | Descending into the compound statements of a block that fits. |
| `check_stmt` | `compiler/parse/mid/names/limits/shape/actions/else_chain.id` | Following an `else if` chain. The else arm of an `else if` holds exactly one |
| `check_loop` | `compiler/parse/mid/names/limits/shape/actions/nested.id` | The bodies of the two compound statements. A loop body and each arm of an |
| `check_if` | `compiler/parse/mid/names/limits/shape/actions/nested.id` | The bodies of the two compound statements. A loop body and each arm of an |
| `check_else` | `compiler/parse/mid/names/limits/shape/actions/nested.id` | The bodies of the two compound statements. A loop body and each arm of an |
| `check_block` | `compiler/parse/mid/names/limits/shape/block/block.id` | `ln` is the line of the construct that *owns* this block -- the function's |
| `check_fits` | `compiler/parse/mid/names/limits/shape/block/block.id` | Checking one block, mirroring idc.py's check_block exactly. |
| `check_count` | `compiler/parse/mid/names/limits/shape/block/block.id` | Checking one block, mirroring idc.py's check_block exactly. |
| `check_failed` | `compiler/parse/mid/names/limits/shape/block/failed.id` | The "something was rejected" flag. id has no stderr and no early exit, so |
| `note_failure` | `compiler/parse/mid/names/limits/shape/block/failed.id` | Writing through an imported list needs the list as a parameter: an |
| `check_body_at` | `compiler/parse/mid/names/limits/shape/block/more/body.id` | Split from check_func_body so that block stays within the action limit: |
| `check_program` | `compiler/parse/mid/names/limits/shape/block/more/check.id` | The two structural rules of `id`, checked on the parsed AST: every block |
| `check_funcs` | `compiler/parse/mid/names/limits/shape/block/more/check.id` | The two structural rules of `id`, checked on the parsed AST: every block |
| `check_func_body` | `compiler/parse/mid/names/limits/shape/block/more/check.id` | A function's own body is the outermost block, at depth 0. Its name labels |
| `check_cases` | `compiler/parse/mid/names/limits/shape/cases/more/count.id` | The two-cases-per-function rule (docs/TESTS.md). |
| `check_cases_at` | `compiler/parse/mid/names/limits/shape/cases/more/count.id` | The two-cases-per-function rule (docs/TESTS.md). |
| `count_cases` | `compiler/parse/mid/names/limits/shape/cases/more/count.id` | The two-cases-per-function rule (docs/TESTS.md). |
| `check_dup_cases` | `compiler/parse/mid/names/limits/shape/cases/more/dup/dup.id` | Two identical cases under one function. |
| `dup_scan` | `compiler/parse/mid/names/limits/shape/cases/more/dup/dup.id` | Against every EARLIER case, so a duplicate is reported once, at the second |
| `dup_hit` | `compiler/parse/mid/names/limits/shape/cases/more/dup/dup.id` | Two identical cases under one function. |
| `case_eq` | `compiler/parse/mid/names/limits/shape/cases/more/dup/eq.id` | Whether two cases are the same case: equal token counts, then equal tokens. |
| `case_eq_run` | `compiler/parse/mid/names/limits/shape/cases/more/dup/eq.id` | `off` is b's start minus a's, so the second cursor is derived rather than |
| `tok_same` | `compiler/parse/mid/names/limits/shape/cases/more/dup/eq.id` | Whether two cases are the same case: equal token counts, then equal tokens. |
| `arg_reqtests` | `compiler/parse/mid/names/limits/shape/cases/more/flag/arg.id` | `--require-tests` anywhere in the arguments. Scanned rather than tested at a |
| `check_cases_maybe` | `compiler/parse/mid/names/limits/shape/cases/more/flag/arg.id` | Chained from check_scopes rather than check_program, whose block is full. |
| `reqtests_init` | `compiler/parse/mid/names/limits/shape/cases/more/flag/flag.id` | Whether this build asks for the two-cases-per-function rule. |
| `reqtests_set` | `compiler/parse/mid/names/limits/shape/cases/more/flag/flag.id` | An exported scalar may only be assigned by its declaring function, so the |
| `require_tests` | `compiler/parse/mid/names/limits/shape/cases/more/flag/flag.id` | Whether this build asks for the two-cases-per-function rule. |
| `cases_report` | `compiler/parse/mid/names/limits/shape/cases/print.id` | The two messages of report.id, split out so each reporting block stays |
| `dup_case_report` | `compiler/parse/mid/names/limits/shape/cases/print.id` | The two messages of report.id, split out so each reporting block stays |
| `case_hit` | `compiler/parse/mid/names/limits/shape/cases/report.id` | One entry of `casefn` against one function node. |
| `report_cases` | `compiler/parse/mid/names/limits/shape/cases/report.id` | idc.py's message, word for word -- tests/invalid.sh requires both compilers |
| `report_dup_case` | `compiler/parse/mid/names/limits/shape/cases/report.id` | Located by the case's own first token rather than by its function, because |
| `sym_func` | `compiler/parse/mid/names/symbols/build/add.id` | Record a function's return type, and the two table-append helpers. |
| `add_var` | `compiler/parse/mid/names/symbols/build/add.id` | The unit comes along because the table is keyed by it: a name may be an int |
| `add_func` | `compiler/parse/mid/names/symbols/build/add.id` | The node id comes along so that a call site can reach the parameter list |
| `check_scopes` | `compiler/parse/mid/names/symbols/build/scopes.id` | What check_program() calls. Its own block is already full -- the chkfail |
| `check_names` | `compiler/parse/mid/names/symbols/build/scopes.id` | Two passes, in idc.py's order: register every declaration (reporting what |
| `init_reg` | `compiler/parse/mid/names/symbols/build/scopes.id` | The declaration registry: one row per parameter and per declaration, in the |
| `build_syms` | `compiler/parse/mid/names/symbols/build/tables.id` | Build the symbol tables by scanning every AST node once: declarations and |
| `sym_one` | `compiler/parse/mid/names/symbols/build/tables.id` | Build the symbol tables by scanning every AST node once: declarations and |
| `is_vardecl` | `compiler/parse/mid/names/symbols/build/tables.id` | Build the symbol tables by scanning every AST node once: declarations and |
| `acc_assign` | `compiler/parse/mid/names/symbols/check/access/expr/assign.id` | The assignment target is a name in its own right: writing to a variable the |
| `acc_assign_val` | `compiler/parse/mid/names/symbols/check/access/expr/assign.id` | The assignment target is a name in its own right: writing to a variable the |
| `acc_stmt4` | `compiler/parse/mid/names/symbols/check/access/expr/assign.id` | What is left is `decl`, `iassign` and `exprstmt`. A declaration and an |
| `acc_elems` | `compiler/parse/mid/names/symbols/check/access/expr/expr.id` |  |
| `acc_expr` | `compiler/parse/mid/names/symbols/check/access/expr/expr.id` | Expression dispatch. Only two kinds name anything: `var` reads a variable of |
| `acc_expr2` | `compiler/parse/mid/names/symbols/check/access/expr/expr.id` |  |
| `acc_expr3` | `compiler/parse/mid/names/symbols/check/access/expr/more/expr_more.id` | A binary operator and an index both hold two expressions in the same two |
| `acc_pair` | `compiler/parse/mid/names/symbols/check/access/expr/more/expr_more.id` | A binary operator and an index both hold two expressions in the same two |
| `acc_expr4` | `compiler/parse/mid/names/symbols/check/access/expr/more/expr_more.id` | A binary operator and an index both hold two expressions in the same two |
| `acc_iassign` | `compiler/parse/mid/names/symbols/check/access/expr/more/iassign.id` | `base[index] = value`: the target was parsed as an index expression and its |
| `acc_iassign_rest` | `compiler/parse/mid/names/symbols/check/access/expr/more/iassign.id` | `base[index] = value`: the target was parsed as an index expression and its |
| `acc_iassign_elems` | `compiler/parse/mid/names/symbols/check/access/expr/more/iassign.id` | `base[index] = value`: the target was parsed as an index expression and its |
| `acc_pair2` | `compiler/parse/mid/names/symbols/check/access/expr/more/pair.id` | Split from acc_pair so that block stays within the action limit. |
| `report_bad_var` | `compiler/parse/mid/names/symbols/check/access/report/bad_var.id` | idc.py's explain_bad_var: a name nobody declared is a typo, and a name |
| `report_undef_var` | `compiler/parse/mid/names/symbols/check/access/report/bad_var.id` | idc.py's explain_bad_var: a name nobody declared is a typo, and a name |
| `report_owned_var` | `compiler/parse/mid/names/symbols/check/access/report/bad_var.id` | idc.py's explain_bad_var: a name nobody declared is a typo, and a name |
| `acc_callee` | `compiler/parse/mid/names/symbols/check/access/report/more/callvar/call_var.id` | Calling a variable. This is the last thing the registry can answer: a |
| `is_unknown_fn` | `compiler/parse/mid/names/symbols/check/access/report/more/callvar/call_var.id` | Not a function of this program and not a builtin. The builtins whose return |
| `report_call_var` | `compiler/parse/mid/names/symbols/check/access/report/more/callvar/call_var.id` | Calling a variable. This is the last thing the registry can answer: a |
| `is_call_var` | `compiler/parse/mid/names/symbols/check/access/report/more/callvar/check.id` | Naming the composition acc_callee's condition needs: is_unknown_fn's |
| `report_no_export` | `compiler/parse/mid/names/symbols/check/access/report/more/no_export.id` | The two ways an `import` can fail to resolve: the name exists as somebody's |
| `report_not_exported` | `compiler/parse/mid/names/symbols/check/access/report/more/no_export.id` | The two ways an `import` can fail to resolve: the name exists as somebody's |
| `var_report` | `compiler/parse/mid/names/symbols/check/access/report/more/no_export.id` | The last two steps of every report_var_* function: print the built message |
| `report_var_import` | `compiler/parse/mid/names/symbols/check/access/report/more/private.id` | The global is there for the taking; the reader just has to say so. |
| `report_var_private` | `compiler/parse/mid/names/symbols/check/access/report/more/private.id` | Nothing to import: the owner has to export it first. |
| `report_bad_import` | `compiler/parse/mid/names/symbols/check/access/report/more/private.id` | The global is there for the taking; the reader just has to say so. |
| `acc_expr5` | `compiler/parse/mid/names/symbols/check/access/report/name.id` | A call's arguments and an array literal's elements are the same shape -- a |
| `acc_name` | `compiler/parse/mid/names/symbols/check/access/report/name.id` | A name the function did not declare. Which of the three explanations it |
| `acc_import` | `compiler/parse/mid/names/symbols/check/access/report/name.id` | `(import name)` resolves against the export table and nothing else: a |
| `check_access` | `compiler/parse/mid/names/symbols/check/access/stmt/access.id` | The access pass: for every name a function mentions, is it a name that |
| `acc_func` | `compiler/parse/mid/names/symbols/check/access/stmt/access.id` | The access pass: for every name a function mentions, is it a name that |
| `acc_ret` | `compiler/parse/mid/names/symbols/check/access/stmt/access.id` | The return clause is not a statement of the body, so the body walk never |
| `acc_stmt2` | `compiler/parse/mid/names/symbols/check/access/stmt/more/stmt_more.id` |  |
| `acc_while` | `compiler/parse/mid/names/symbols/check/access/stmt/more/stmt_more.id` |  |
| `acc_stmt3` | `compiler/parse/mid/names/symbols/check/access/stmt/more/stmt_more.id` |  |
| `acc_func_body` | `compiler/parse/mid/names/symbols/check/access/stmt/more/tail.id` | The parts of acc_func, acc_if and acc_while moved out so each of those |
| `acc_if_body` | `compiler/parse/mid/names/symbols/check/access/stmt/more/tail.id` | The parts of acc_func, acc_if and acc_while moved out so each of those |
| `acc_if_else` | `compiler/parse/mid/names/symbols/check/access/stmt/more/tail.id` | The parts of acc_func, acc_if and acc_while moved out so each of those |
| `acc_while_body` | `compiler/parse/mid/names/symbols/check/access/stmt/more/while.id` | Split from acc_while so that block stays within the action limit. |
| `acc_list` | `compiler/parse/mid/names/symbols/check/access/stmt/stmt.id` | Statement dispatch. Each statement carries the line it began on in `nline`, |
| `acc_stmt` | `compiler/parse/mid/names/symbols/check/access/stmt/stmt.id` | Statement dispatch. Each statement carries the line it began on in `nline`, |
| `acc_if` | `compiler/parse/mid/names/symbols/check/access/stmt/stmt.id` | Statement dispatch. Each statement carries the line it began on in `nline`, |
| `reg_report` | `compiler/parse/mid/names/symbols/check/conflict/check.id` | The four things a declaration can conflict with, tested in idc.py's order. |
| `reg_report2` | `compiler/parse/mid/names/symbols/check/conflict/check.id` | The four things a declaration can conflict with, tested in idc.py's order. |
| `reg_report3` | `compiler/parse/mid/names/symbols/check/conflict/check.id` | The `export` declaration is the one that claims the name, so it is exempt; |
| `owner_at` | `compiler/parse/mid/names/symbols/check/conflict/more/first.id` | Both of these keep the *first* row that matches, so the guard is on the |
| `first_type` | `compiler/parse/mid/names/symbols/check/conflict/more/first.id` | The type a name was first given by a non-exporting declaration in this unit, |
| `first_type_at` | `compiler/parse/mid/names/symbols/check/conflict/more/first.id` | Both of these keep the *first* row that matches, so the guard is on the |
| `report_var_is_func` | `compiler/parse/mid/names/symbols/check/conflict/more/messages.id` | The messages a declaration can draw, word for word as idc.py writes them, |
| `report_var_twice` | `compiler/parse/mid/names/symbols/check/conflict/more/messages.id` | The messages a declaration can draw, word for word as idc.py writes them, |
| `report_var_exported` | `compiler/parse/mid/names/symbols/check/conflict/more/messages.id` | The messages a declaration can draw, word for word as idc.py writes them, |
| `report_var_type` | `compiler/parse/mid/names/symbols/check/conflict/more/owner.id` |  |
| `export_owner` | `compiler/parse/mid/names/symbols/check/conflict/more/owner.id` | The function that exported `name`, from the table collect_exports built. |
| `owner_of` | `compiler/parse/mid/names/symbols/check/conflict/more/owner.id` | The function a name belongs to: the first one to declare it, which is what |
| `reg_report4` | `compiler/parse/mid/names/symbols/check/conflict/type.id` | One name, one type -- within one compilation unit. The unit is the program's |
| `reg_push` | `compiler/parse/mid/names/symbols/check/conflict/type.id` | One name, one type -- within one compilation unit. The unit is the program's |
| `reg_push2` | `compiler/parse/mid/names/symbols/check/conflict/type.id` | One name, one type -- within one compilation unit. The unit is the program's |
| `reg_decls` | `compiler/parse/mid/names/symbols/check/declare/body.id` | The body walk: every declaration, including those nested in an if or a |
| `reg_stmt` | `compiler/parse/mid/names/symbols/check/declare/body.id` | The body walk: every declaration, including those nested in an if or a |
| `reg_nested` | `compiler/parse/mid/names/symbols/check/declare/body.id` | The body walk: every declaration, including those nested in an if or a |
| `reg_branches` | `compiler/parse/mid/names/symbols/check/declare/decl.id` | An else arm is a statement list whichever way it was written: a plain |
| `reg_loop` | `compiler/parse/mid/names/symbols/check/declare/decl.id` | An else arm is a statement list whichever way it was written: a plain |
| `reg_var` | `compiler/parse/mid/names/symbols/check/declare/decl.id` | One declaration: report whatever it conflicts with, then record it anyway. |
| `reg_decl_stmt` | `compiler/parse/mid/names/symbols/check/declare/more/decl_tail.id` | The parts of reg_stmt and reg_branches moved out so each of those blocks |
| `reg_decl_var` | `compiler/parse/mid/names/symbols/check/declare/more/decl_tail.id` | The parts of reg_stmt and reg_branches moved out so each of those blocks |
| `reg_l2_decls` | `compiler/parse/mid/names/symbols/check/declare/more/decl_tail.id` | Also the tail of reg_func: both just register whatever declarations sit in |
| `reg_func_params` | `compiler/parse/mid/names/symbols/check/declare/more/func_tail.id` | The rest of reg_func, split out so each of its blocks stays within the |
| `reg_param_at` | `compiler/parse/mid/names/symbols/check/declare/more/func_tail.id` | Split from reg_params so the while's own block stays within the limit. |
| `build_reg` | `compiler/parse/mid/names/symbols/check/declare/more/registry.id` | Filling the registry. The walk mirrors idc.py's register_var loop, and the |
| `reg_func` | `compiler/parse/mid/names/symbols/check/declare/more/registry.id` | Parameters are declarations too, and they are registered before the body's. |
| `reg_params` | `compiler/parse/mid/names/symbols/check/declare/more/registry.id` | Filling the registry. The walk mirrors idc.py's register_var loop, and the |
| `new_bucket` | `compiler/parse/mid/names/symbols/lookup/index/hash/bucket.id` | One empty bucket. A list literal builds a fresh list on every call, which is |
| `idx_n` | `compiler/parse/mid/names/symbols/lookup/index/hash/bucket.id` | A prime, so that the final `%` mixes all sixteen bits of the hash rather |
| `name_bucket` | `compiler/parse/mid/names/symbols/lookup/index/hash/bucket.id` | Which bucket a name belongs in -- not a hash. It was called str_hash, which |
| `hash_loop` | `compiler/parse/mid/names/symbols/lookup/index/hash/hash.id` | The usual polynomial string hash, kept inside 16 bits at every step so the |
| `hash_step` | `compiler/parse/mid/names/symbols/lookup/index/hash/hash.id` | The usual polynomial string hash, kept inside 16 bits at every step so the |
| `find_ix` | `compiler/parse/mid/names/symbols/lookup/index/hash/hash.id` | find_str's answer, over one bucket instead of the whole table. Same |
| `dv_count` | `compiler/parse/mid/names/symbols/lookup/index/hash/scan.id` | The three questions asked of one bucket instead of the whole table. All |
| `ft_scan` | `compiler/parse/mid/names/symbols/lookup/index/hash/scan.id` | The three questions asked of one bucket instead of the whole table. All |
| `vx_scan` | `compiler/parse/mid/names/symbols/lookup/index/hash/scan.id` | The three questions asked of one bucket instead of the whole table. All |
| `match_idx` | `compiler/parse/mid/names/symbols/lookup/index/match.id` | Keep the index of a name match (names are unique per type, so any match is |
| `find_vix` | `compiler/parse/mid/names/symbols/lookup/index/match.id` | find_ix, restricted to the rows declared in one unit -- with the whole table |
| `vx_hit` | `compiler/parse/mid/names/symbols/lookup/index/match.id` | Keep the index of a name match (names are unique per type, so any match is |
| `ix_hit` | `compiler/parse/mid/names/symbols/lookup/index/table/add.id` |  |
| `vidx_add` | `compiler/parse/mid/names/symbols/lookup/index/table/add.id` | Appending a name and indexing it are one operation, so the index cannot be |
| `fidx_add` | `compiler/parse/mid/names/symbols/lookup/index/table/add.id` |  |
| `init_didx` | `compiler/parse/mid/names/symbols/lookup/index/table/decls.id` | dvunit is declared here, beside the index rather than beside the other |
| `fill_didx` | `compiler/parse/mid/names/symbols/lookup/index/table/decls.id` | The declaration registry's index. dvname is the third table that is scanned |
| `didx_add` | `compiler/parse/mid/names/symbols/lookup/index/table/decls.id` | The declaration registry's index. dvname is the third table that is scanned |
| `push_fbucket` | `compiler/parse/mid/names/symbols/lookup/index/table/more/fbucket.id` | Split from push_buckets so that block stays within the action limit. |
| `init_idx` | `compiler/parse/mid/names/symbols/lookup/index/table/more/init.id` | A hash index over the two name tables that are asked about most. |
| `fill_idx` | `compiler/parse/mid/names/symbols/lookup/index/table/more/init.id` | A hash index over the two name tables that are asked about most. |
| `push_buckets` | `compiler/parse/mid/names/symbols/lookup/index/table/more/init.id` | Both indexes get their buckets in one loop: they are the same size and are |
| `vidx_push` | `compiler/parse/mid/names/symbols/lookup/index/table/more/push.id` | The two-push tail of vidx_add, fidx_add and didx_add, split out so each of |
| `fidx_push` | `compiler/parse/mid/names/symbols/lookup/index/table/more/push.id` | The two-push tail of vidx_add, fidx_add and didx_add, split out so each of |
| `didx_push` | `compiler/parse/mid/names/symbols/lookup/index/table/more/push.id` | The two-push tail of vidx_add, fidx_add and didx_add, split out so each of |
| `lookup_var` | `compiler/parse/mid/names/symbols/lookup/more/lookup.id` | Lookups: variable type, function return type, and a linear name search. |
| `lookup_func` | `compiler/parse/mid/names/symbols/lookup/more/lookup.id` | Lookups: variable type, function return type, and a linear name search. |
| `find_str` | `compiler/parse/mid/names/symbols/lookup/more/lookup.id` | Lookups: variable type, function return type, and a linear name search. |
| `vtype_at` | `compiler/parse/mid/names/symbols/lookup/more/types.id` | Split from lookup_var and lookup_func so each block stays within the |
| `fret_at` | `compiler/parse/mid/names/symbols/lookup/more/types.id` | Split from lookup_var and lookup_func so each block stays within the |
| `init_reg2` | `compiler/parse/mid/names/symbols/lookup/registry.id` | The other half of the registry, and the one question it is asked most. |
| `declared_in` | `compiler/parse/mid/names/symbols/lookup/registry.id` | How many registry rows declare `name` inside `owner`. One scan answers two |
| `dv_hit` | `compiler/parse/mid/names/symbols/lookup/registry.id` | The other half of the registry, and the one question it is asked most. |
| `chk_argtypes` | `compiler/parse/mid/types/check/expr/call/args/args.id` | One argument against one parameter. Reported at the argument's own line, |
| `chk_arg_at` | `compiler/parse/mid/types/check/expr/call/args/args.id` | One argument against one parameter. Reported at the argument's own line, |
| `param_name` | `compiler/parse/mid/types/check/expr/call/args/err/argmsg.id` | The parameter's name, read the same way as its type. |
| `arg_err_msg` | `compiler/parse/mid/types/check/expr/call/args/err/argmsg.id` | The parameter's name, read the same way as its type. |
| `arg_err_msg2` | `compiler/parse/mid/types/check/expr/call/args/err/argmsg.id` | The parameter's name, read the same way as its type. |
| `narrow_err_msg` | `compiler/parse/mid/types/check/expr/call/args/err/narrowmsg.id` |  |
| `narrow_err_msg2` | `compiler/parse/mid/types/check/expr/call/args/err/narrowmsg.id` |  |
| `param_type` | `compiler/parse/mid/types/check/expr/call/args/err/typecheck.id` | The parameter's declared type, for the bad-store test and reused as the |
| `chk_arg_typecheck` | `compiler/parse/mid/types/check/expr/call/args/err/typecheck.id` | The parameter's declared type, for the bad-store test and reused as the |
| `arg_err` | `compiler/parse/mid/types/check/expr/call/args/err/typecheck.id` | The parameter node holds its type in the first string field and its name in |
| `chk_narrow` | `compiler/parse/mid/types/check/expr/call/args/narrow.id` | docs/FRICTION.md 12: a narrowing conversion is written down, not inferred. |
| `narrow_err` | `compiler/parse/mid/types/check/expr/call/args/narrow.id` | docs/FRICTION.md 12: a narrowing conversion is written down, not inferred. |
| `is_peekfn` | `compiler/parse/mid/types/check/expr/call/builtin/args/args.id` | The peek family, which is the only group of builtins that takes one |
| `chk_bi_args` | `compiler/parse/mid/types/check/expr/call/builtin/args/args.id` | The peek family, which is the only group of builtins that takes one |
| `chk_bi_arg` | `compiler/parse/mid/types/check/expr/call/builtin/args/args.id` | A position with no requirement ("") is skipped, and so is a type the |
| `arg_type_at` | `compiler/parse/mid/types/check/expr/call/builtin/args/read.id` | Argument i's own type, read the same way a defined function's parameter |
| `bi_want_for` | `compiler/parse/mid/types/check/expr/call/builtin/args/read.id` | What position i of this builtin requires, by name. |
| `bi_wants` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants.id` | What argument `i` of a builtin must be, as a word this file's neighbours |
| `bi_wants2` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants.id` | charat's first argument was taken above, so what reaches here is its |
| `bi_wants3` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants.id` | What argument `i` of a builtin must be, as a word this file's neighbours |
| `bi_wants4` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants_more.id` |  |
| `bi_wants5` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants_more.id` |  |
| `bi_ok` | `compiler/parse/mid/types/check/expr/call/builtin/args/wants/wants_more.id` | Is `t` acceptable where `want` is required? A chain rather than one |
| `chk_bi` | `compiler/parse/mid/types/check/expr/call/builtin/arity/arity.id` | Calling a builtin: the right number of arguments, of the right types. |
| `chk_bi_arity` | `compiler/parse/mid/types/check/expr/call/builtin/arity/arity.id` | Arity first, and nothing else if it is wrong -- as at a call to a defined |
| `bi_arity_err` | `compiler/parse/mid/types/check/expr/call/builtin/arity/arity.id` | Two wordings, because idc.py has two: the flat-store and unsigned-word |
| `bi_plural` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/count.id` | The two spellings of a count. |
| `bi_count` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/count.id` | The two spellings of a count. |
| `is_sysbi` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/count.id` | The flat-store and unsigned-word builtins: idc.py's STORE_BUILTINS and |
| `bi_arity` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/table.id` | How many arguments each builtin takes. Two is the common case and so the |
| `bi_zero` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/table.id` | How many arguments each builtin takes. Two is the common case and so the |
| `bi_one` | `compiler/parse/mid/types/check/expr/call/builtin/arity/count/table.id` | How many arguments each builtin takes. Two is the common case and so the |
| `bi_arity_mismatch` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/mismatch.id` |  |
| `arity_ne` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/mismatch.id` |  |
| `bi_other_msg` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/other.id` |  |
| `bi_sys_msg` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/sys.id` |  |
| `bi_count_word` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/sys.id` |  |
| `bi_sys_msg2` | `compiler/parse/mid/types/check/expr/call/builtin/arity/msg/sys.id` |  |
| `bi_arg_err` | `compiler/parse/mid/types/check/expr/call/builtin/error/message.id` | The message. Three of the eight requirements are worded specially in |
| `bi_arg_err2` | `compiler/parse/mid/types/check/expr/call/builtin/error/message.id` | str_of_mem names neither the position nor the type: both of its arguments |
| `bi_arg_err3` | `compiler/parse/mid/types/check/expr/call/builtin/error/message.id` | The flat-store group is the only family that reports at the *argument's* |
| `bi_ok2` | `compiler/parse/mid/types/check/expr/call/builtin/error/ok.id` |  |
| `bi_ok3` | `compiler/parse/mid/types/check/expr/call/builtin/error/ok.id` |  |
| `bi_strlist` | `compiler/parse/mid/types/check/expr/call/builtin/error/ok.id` | `len` is the one builtin that accepts two unrelated shapes, because a |
| `bi_verb` | `compiler/parse/mid/types/check/expr/call/builtin/error/verb.id` | "a int" would be wrong and "a string or list" is not a type name, so the |
| `arity_err` | `compiler/parse/mid/types/check/expr/call/callsite/err.id` |  |
| `arity_err_msg` | `compiler/parse/mid/types/check/expr/call/callsite/err.id` |  |
| `arity_mismatch` | `compiler/parse/mid/types/check/expr/call/callsite/mismatch.id` |  |
| `ne_flag` | `compiler/parse/mid/types/check/expr/call/callsite/mismatch.id` |  |
| `chk_callsite` | `compiler/parse/mid/types/check/expr/call/callsite/site.id` | Calling a function this program defines: the right number of arguments, of |
| `func_node` | `compiler/parse/mid/types/check/expr/call/callsite/site.id` | The func node behind a called name, or -1 when there is none to check |
| `chk_arity` | `compiler/parse/mid/types/check/expr/call/callsite/site.id` | Arity first, and nothing else if it is wrong: pairing arguments off with |
| `tc_g4` | `compiler/parse/mid/types/check/expr/more/index/index.id` | Reading `base[index]`. The same two questions index-assignment asks, and |
| `chk_index` | `compiler/parse/mid/types/check/expr/more/index/index.id` | Reading `base[index]`. The same two questions index-assignment asks, and |
| `chk_index2` | `compiler/parse/mid/types/check/expr/more/index/index.id` | Reading `base[index]`. The same two questions index-assignment asks, and |
| `ix_err` | `compiler/parse/mid/types/check/expr/more/index/target.id` | Suppressing the read-index diagnostic on an index-assignment target. |
| `is_ia_target` | `compiler/parse/mid/types/check/expr/more/index/target.id` | Suppressing the read-index diagnostic on an index-assignment target. |
| `ia_hit` | `compiler/parse/mid/types/check/expr/more/index/target.id` | Suppressing the read-index diagnostic on an index-assignment target. |
| `want_ret` | `compiler/parse/mid/types/check/expr/more/index/want_ret.id` | The fifth and last place an expected type comes from: a return clause is |
| `fn_covers` | `compiler/parse/mid/types/check/expr/more/index/want_ret.id` | The fifth and last place an expected type comes from: a return clause is |
| `tc_line` | `compiler/parse/mid/types/check/expr/more/index/want_ret.id` | The line to report. The parser fills nline for statement nodes, which is |
| `is_ordop` | `compiler/parse/mid/types/check/expr/more/push/call.id` | The ordering operators, and the one builtin call with an element-type rule. |
| `chk_call` | `compiler/parse/mid/types/check/expr/more/push/call.id` | The ordering operators, and the one builtin call with an element-type rule. |
| `is_push2` | `compiler/parse/mid/types/check/expr/more/push/call.id` | A two-argument `push`. The arity is part of the test because a call written |
| `arr_ok` | `compiler/parse/mid/types/check/expr/more/push/empty_arr.id` | Where an empty list literal is allowed. idc.py hands an expected type down |
| `arr_ok_at` | `compiler/parse/mid/types/check/expr/more/push/empty_arr.id` | Where an empty list literal is allowed. idc.py hands an expected type down |
| `want_list` | `compiler/parse/mid/types/check/expr/more/push/empty_arr.id` | Where an empty list literal is allowed. idc.py hands an expected type down |
| `chk_push` | `compiler/parse/mid/types/check/expr/more/push/push.id` | push's element type, and the untyped empty list literal. |
| `chk_push2` | `compiler/parse/mid/types/check/expr/more/push/push.id` | Pushing onto something that is not a list is idc.py's "push expects a list" |
| `chk_arr` | `compiler/parse/mid/types/check/expr/more/push/push.id` | `[]` carries no element type, so it is only meaningful where something else |
| `var_type_at` | `compiler/parse/mid/types/check/expr/more/want/positions/ok.id` | The variable's declared type, read from the symbol table by name. |
| `want_elem_ok` | `compiler/parse/mid/types/check/expr/more/want/positions/ok.id` | The variable's declared type, read from the symbol table by name. |
| `want_elem_ok2` | `compiler/parse/mid/types/check/expr/more/want/positions/ok.id` | Whether a list-of-lists' element type is itself a list type. |
| `want_decl` | `compiler/parse/mid/types/check/expr/more/want/positions/pred.id` | The four positions that supply an expected type. Each asks "is node `i` a |
| `want_asg` | `compiler/parse/mid/types/check/expr/more/want/positions/pred.id` | The four positions that supply an expected type. Each asks "is node `i` a |
| `want_elem` | `compiler/parse/mid/types/check/expr/more/want/positions/pred.id` | The element of a list of lists: `xss[0] = []` is fine when xss is int[][]. |
| `want_push` | `compiler/parse/mid/types/check/expr/more/want/positions/push.id` |  |
| `want_push_ok` | `compiler/parse/mid/types/check/expr/more/want/positions/push.id` |  |
| `tc_err` | `compiler/parse/mid/types/check/expr/more/want/report.id` | Message text is idc.py's, word for word, so the two compilers explain a |
| `node_fn` | `compiler/parse/mid/types/check/expr/more/want/report.id` | Which function a node belongs to. Nodes are created in source order and a |
| `tc_loc` | `compiler/parse/mid/types/check/expr/more/want/report.id` | The reporting location: which function a node belongs to, and which line |
| `ret_err_msg` | `compiler/parse/mid/types/check/expr/more/want/ret/err.id` |  |
| `chk_retexpr` | `compiler/parse/mid/types/check/expr/more/want/ret/expr.id` | The return clause against the declared return type. |
| `chk_ret2` | `compiler/parse/mid/types/check/expr/more/want/ret/expr.id` | The return clause against the declared return type. |
| `ret_err` | `compiler/parse/mid/types/check/expr/more/want/ret/expr.id` | Reported at the return expression, not at the function heading: the clause |
| `chk_eq` | `compiler/parse/mid/types/check/expr/op/compare/compare/eq.id` | The two comparison checks. Equality accepts two strings or two numbers; |
| `eq_types` | `compiler/parse/mid/types/check/expr/op/compare/compare/eq.id` | The two comparison checks. Equality accepts two strings or two numbers; |
| `eq_report` | `compiler/parse/mid/types/check/expr/op/compare/compare/eq.id` | The two comparison checks. Equality accepts two strings or two numbers; |
| `chk_ord` | `compiler/parse/mid/types/check/expr/op/compare/compare/order.id` | Guarded first, so `+` and `&&` -- which reach here through chk_bin's else |
| `chk_ord2` | `compiler/parse/mid/types/check/expr/op/compare/compare/order.id` | Guarded first, so `+` and `&&` -- which reach here through chk_bin's else |
| `ord_types` | `compiler/parse/mid/types/check/expr/op/compare/compare/order.id` | Guarded first, so `+` and `&&` -- which reach here through chk_bin's else |
| `ord_report` | `compiler/parse/mid/types/check/expr/op/compare/compare/order_msg.id` |  |
| `chk_cmp` | `compiler/parse/mid/types/check/expr/op/compare/cond/cmp.id` |  |
| `chk_iassign3` | `compiler/parse/mid/types/check/expr/op/compare/cond/cond.id` | The element store, the void condition, and the split between the two |
| `chk_cond` | `compiler/parse/mid/types/check/expr/op/compare/cond/cond.id` | A condition that is a call to a void function is the one type error the |
| `chk_bin` | `compiler/parse/mid/types/check/expr/op/compare/cond/cond.id` | `==`, `!=` and id's bare `=` compare; `< <= > >=` order. is_eqop already |
| `cmp_bad` | `compiler/parse/mid/types/check/expr/op/compare/operands.id` | What each comparison family accepts, and the knownness guard they share. |
| `eq_ok` | `compiler/parse/mid/types/check/expr/op/compare/operands.id` | What each comparison family accepts, and the knownness guard they share. |
| `ord_ok` | `compiler/parse/mid/types/check/expr/op/compare/operands.id` | What each comparison family accepts, and the knownness guard they share. |
| `chk_exprstmt` | `compiler/parse/mid/types/check/expr/op/exprstmt.id` | A statement that compares instead of assigning. |
| `is_asgop` | `compiler/parse/mid/types/check/expr/op/exprstmt.id` | Both spellings. idc.py's parser folds `=` into `==` on the way in, so by the |
| `disc_hint` | `compiler/parse/mid/types/check/expr/op/exprstmt.id` | Naming the fix is the point of the diagnostic, so an index target -- the |
| `chk_bits` | `compiler/parse/mid/types/check/expr/op/unary_bits/bits/bits.id` | The operators that want integers on both sides: the bitwise group |
| `chk_bits2` | `compiler/parse/mid/types/check/expr/op/unary_bits/bits/bits.id` | The operators that want integers on both sides: the bitwise group |
| `bits_err` | `compiler/parse/mid/types/check/expr/op/unary_bits/bits/bits.id` | The logical pair names no types: `&&` on two lists is a mistake about what |
| `bits_types` | `compiler/parse/mid/types/check/expr/op/unary_bits/bits/types.id` |  |
| `chk_mod` | `compiler/parse/mid/types/check/expr/op/unary_bits/mod/mod.id` | `%` is the odd one: it is an arithmetic operator, so two numbers are the |
| `is_logop` | `compiler/parse/mid/types/check/expr/op/unary_bits/mod/mod.id` | `%` is the odd one: it is an arithmetic operator, so two numbers are the |
| `is_bitop` | `compiler/parse/mid/types/check/expr/op/unary_bits/mod/mod.id` | `%` is the odd one: it is an arithmetic operator, so two numbers are the |
| `mod_types` | `compiler/parse/mid/types/check/expr/op/unary_bits/mod/types.id` |  |
| `tc_g5` | `compiler/parse/mid/types/check/expr/op/unary_bits/unary.id` | The last link of the dispatch chain, and the unary operators. |
| `chk_un` | `compiler/parse/mid/types/check/expr/op/unary_bits/unary.id` | The last link of the dispatch chain, and the unary operators. |
| `chk_un2` | `compiler/parse/mid/types/check/expr/op/unary_bits/unary.id` | Negation is the one with its own wording in idc.py; `!` and `~` share |
| `chk_decl` | `compiler/parse/mid/types/check/store/assign/assign.id` | Declaration and assignment: does the value fit the slot it is going into? |
| `chk_assign` | `compiler/parse/mid/types/check/store/assign/assign.id` | Declaration and assignment: does the value fit the slot it is going into? |
| `bad_store` | `compiler/parse/mid/types/check/store/assign/assign.id` | The one place that decides "this value may not go here". An array literal |
| `decl_err` | `compiler/parse/mid/types/check/store/assign/err.id` |  |
| `assign_err` | `compiler/parse/mid/types/check/store/assign/err.id` |  |
| `bad_store2` | `compiler/parse/mid/types/check/store/assign/err.id` |  |
| `bad_init` | `compiler/parse/mid/types/check/store/known.id` | Compatibility, guarded by knownness. Every check in this directory is |
| `ty_known` | `compiler/parse/mid/types/check/store/known.id` | Is this a type at all? Asked of the registry, not of the string: type_of |
| `ty_pair_known` | `compiler/parse/mid/types/check/store/known.id` | Compatibility, guarded by knownness. Every check in this directory is |
| `arr_el_at` | `compiler/parse/mid/types/check/store/more/arr/elem.id` | bad_store rather than bad_init, so a list of lists recurses: the elements of |
| `arr_el_check` | `compiler/parse/mid/types/check/store/more/arr/elem.id` | bad_store rather than bad_init, so a list of lists recurses: the elements of |
| `bad_arr` | `compiler/parse/mid/types/check/store/more/arr/lit.id` | An array literal has no type of its own. |
| `arr_els` | `compiler/parse/mid/types/check/store/more/arr/lit.id` | Zero, because every disagreement here has already been reported against the |
| `chk_iassign` | `compiler/parse/mid/types/check/store/more/index_assign.id` | Index-assignment `base[index] = value`, in idc.py's order: the base must be |
| `chk_iassign2` | `compiler/parse/mid/types/check/store/more/index_assign.id` | `int` exactly, not "any integral type": idc.py indexes with a C int, so a |
| `base_type` | `compiler/parse/mid/types/check/store/more/read.id` | The base and index types an indexing operation reads twice over (once to |
| `index_type` | `compiler/parse/mid/types/check/store/more/read.id` | The base and index types an indexing operation reads twice over (once to |
| `check_rest` | `compiler/parse/mid/types/check/walk/check.id` | The entry point. check_program()'s own block is full -- the chkfail export |
| `tc_all` | `compiler/parse/mid/types/check/walk/check.id` | The type checks: idc.py's assertions from gen_stmt/gen_expr/gen_binop, |
| `tc_nodes` | `compiler/parse/mid/types/check/walk/check.id` | The type checks: idc.py's assertions from gen_stmt/gen_expr/gen_binop, |
| `tc_node` | `compiler/parse/mid/types/check/walk/dispatch.id` | Dispatch for one node. Written as independent guards rather than the usual |
| `tc_g1` | `compiler/parse/mid/types/check/walk/dispatch.id` | Dispatch for one node. Written as independent guards rather than the usual |
| `tc_g2` | `compiler/parse/mid/types/check/walk/dispatch.id` | The two conditions differ only in their message, so they share a check. |
| `void_arr_pair` | `compiler/parse/mid/types/check/walk/void_arr/pair.id` |  |
| `tc_g3` | `compiler/parse/mid/types/check/walk/void_arr/void_arr.id` | The rest of the dispatch, and the `void[]` rule. |
| `chk_voidarr` | `compiler/parse/mid/types/check/walk/void_arr/void_arr.id` | idc.py rejects `void[]` in parse_type, so it applies to every written type: |
| `void_arr_type` | `compiler/parse/mid/types/check/walk/void_arr/void_arr.id` | The rest of the dispatch, and the `void[]` rule. |
| `init_tyreg` | `compiler/parse/mid/types/registry/init.id` | The type registry: id's types as DATA rather than as if-chains. |
| `init_ty_cols` | `compiler/parse/mid/types/registry/init.id` | The four columns. An `export` is a statement, so the columns come into |
| `init_ty_rows` | `compiler/parse/mid/types/registry/init.id` | The type registry: id's types as DATA rather than as if-chains. |
| `ty_row2` | `compiler/parse/mid/types/registry/lookup/find.id` | The numeric half of a row, and the two structural questions about a type |
| `ty_find` | `compiler/parse/mid/types/registry/lookup/find.id` | The row index of a type name, or -1. Every other lookup goes through here, |
| `ty_is_list` | `compiler/parse/mid/types/registry/lookup/find.id` | A list type is derived, not registered: `T[]` for any T. 93 is `]`. The |
| `ty_base` | `compiler/parse/mid/types/registry/lookup/kind.id` | The scalar a list type is ultimately made of: "int[][]" -> "int". Used to |
| `ty_kind` | `compiler/parse/mid/types/registry/lookup/kind.id` | The kind of a type: "int", "float", "ref", "unit" from the registry, "list" |
| `ty_row_kind` | `compiler/parse/mid/types/registry/lookup/kind.id` | Stripping list levels, and the kind column. |
| `ty_num` | `compiler/parse/mid/types/registry/lookup/num/compat.id` | idc.py's is_numeric. |
| `ty_int` | `compiler/parse/mid/types/registry/lookup/num/compat.id` | idc.py's is_integral: the same question with "float" left out, which is |
| `ty_compat` | `compiler/parse/mid/types/registry/lookup/num/compat.id` | idc.py's compatible(): the same type, or two numbers (which widen into each |
| `ty_narrows` | `compiler/parse/mid/types/registry/lookup/num/narrow.id` | Does passing a `got` where a `want` is asked for lose information? |
| `ty_width` | `compiler/parse/mid/types/registry/lookup/num/widen.id` | The widening rule, driven by the registry instead of by name tests. |
| `ty_rank` | `compiler/parse/mid/types/registry/lookup/num/widen.id` | The widening rule, driven by the registry instead of by name tests. |
| `ty_wider` | `compiler/parse/mid/types/registry/lookup/num/widen.id` | The widening rule, driven by the registry instead of by name tests. |
| `init_ty_rows2` | `compiler/parse/mid/types/registry/rows.id` | The remaining rows, and the row constructor. Adding a type to id is adding |
| `init_ty_rows3` | `compiler/parse/mid/types/registry/rows.id` | `string` is a reference, so its width is a pointer's, not a character |
| `ty_row` | `compiler/parse/mid/types/registry/rows.id` | The remaining rows, and the row constructor. Adding a type to id is adding |
| `tb2` | `compiler/parse/mid/types/type_of/binary/arith/arith.id` | The type of a binary expression that is not a concatenation. Comparisons |
| `is_arith` | `compiler/parse/mid/types/type_of/binary/arith/arith.id` | The operators whose result type follows their operands. `&& ||` are not |
| `arith_result` | `compiler/parse/mid/types/type_of/binary/arith/arith.id` | Widening order is int < word < float, so mixing an index with an address |
| `tb2_arith` | `compiler/parse/mid/types/type_of/binary/arith/operands.id` |  |
| `tb2_arith2` | `compiler/parse/mid/types/type_of/binary/arith/operands.id` |  |
| `type_arr` | `compiler/parse/mid/types/type_of/binary/array.id` | What an array literal answers when something asks for its type without |
| `is_concat` | `compiler/parse/mid/types/type_of/binary/concat.id` | Concatenation test: whether `+` is string concatenation rather than |
| `one_is_str` | `compiler/parse/mid/types/type_of/binary/concat.id` | Concatenation test: whether `+` is string concatenation rather than |
| `is_str_pair` | `compiler/parse/mid/types/type_of/binary/concat.id` | Concatenation test: whether `+` is string concatenation rather than |
| `is_pokefn` | `compiler/parse/mid/types/type_of/call/builtin/poke.id` | The flat-store writes. They return nothing, unlike every other systems |
| `bt4` | `compiler/parse/mid/types/type_of/call/builtin/sys.id` | Return types of the systems builtins: the flat store, the unsigned-word |
| `bt5` | `compiler/parse/mid/types/type_of/call/builtin/sys.id` | Return types of the systems builtins: the flat store, the unsigned-word |
| `is_wordfn` | `compiler/parse/mid/types/type_of/call/builtin/sys.id` | Builtins yielding a machine word. `ult` is not among them -- it answers a |
| `bt2` | `compiler/parse/mid/types/type_of/call/builtin/types.id` | Remaining builtin return types, and the type of a binary expression. |
| `bt3` | `compiler/parse/mid/types/type_of/call/builtin/types.id` | Remaining builtin return types, and the type of a binary expression. |
| `type_bin` | `compiler/parse/mid/types/type_of/call/builtin/types.id` | `+` with a string operand is concatenation (-> string); anything else is an |
| `type_of4` | `compiler/parse/mid/types/type_of/call/call.id` | call/bin dispatch, and the return type of a called name. |
| `type_call` | `compiler/parse/mid/types/type_of/call/call.id` | call/bin dispatch, and the return type of a called name. |
| `builtin_type` | `compiler/parse/mid/types/type_of/call/call.id` | return type of a builtin, or "?" if `name` is not a builtin |
| `lit_type` | `compiler/parse/mid/types/type_of/call/lit.id` | The type of an integer literal: `word` when it does not fit an int (the |
| `call_type` | `compiler/parse/mid/types/type_of/call/lit.id` | A call's result type. Every builtin's type follows from its name except |
| `pop_elem` | `compiler/parse/mid/types/type_of/call/lit.id` | The type of an integer literal: `word` when it does not fit an int (the |
| `type_of5` | `compiler/parse/mid/types/type_of/dispatch/index.id` | type_of for the array/index nodes, extending the dispatch chain |
| `type_of6` | `compiler/parse/mid/types/type_of/dispatch/index.id` | type_of for the array/index nodes, extending the dispatch chain |
| `type_index` | `compiler/parse/mid/types/type_of/dispatch/index.id` | type_of for the array/index nodes, extending the dispatch chain |
| `is_nameref` | `compiler/parse/mid/types/type_of/dispatch/more/name.id` | var and import both name a variable; their type is the variable's type. |
| `type_of_float` | `compiler/parse/mid/types/type_of/dispatch/more/name.id` | Extends the type_of dispatch chain (type_of2 -> here -> type_of3) with the |
| `type_of7` | `compiler/parse/mid/types/type_of/dispatch/more/unary.id` | Tail of the type_of dispatch chain (... -> type_of6 -> here): a unary node |
| `type_un` | `compiler/parse/mid/types/type_of/dispatch/more/unary.id` | `-x` and `~x` keep the operand's type; `!x` is a truth value, so int. |
| `type_of` | `compiler/parse/mid/types/type_of/dispatch/type_of.id` | type_of(id): the id type of an expression node. Dispatch on kind. |
| `type_of2` | `compiler/parse/mid/types/type_of/dispatch/type_of.id` | type_of(id): the id type of an expression node. Dispatch on kind. |
| `type_of3` | `compiler/parse/mid/types/type_of/dispatch/type_of.id` | type_of(id): the id type of an expression node. Dispatch on kind. |
| `canon_expr` | `compiler/parse/mid/unique/canon/expr/expr.id` | Canonicalising an expression, mirroring idc.py's _canon_expr. |
| `ce2` | `compiler/parse/mid/unique/canon/expr/expr.id` | A literal is kept exactly as it was written, which is why `1` and `0x1` are |
| `ce3` | `compiler/parse/mid/unique/canon/expr/expr.id` | An imported global keeps its name: two functions that differ only by which |
| `ce4` | `compiler/parse/mid/unique/canon/expr/expr_more.id` | The rest of the expression kinds. ce5 ends the chain, so its default is the |
| `ce5` | `compiler/parse/mid/unique/canon/expr/expr_more.id` | The end of the chain. Its default is reached only by an expression kind |
| `canon_callee` | `compiler/parse/mid/unique/canon/expr/expr_more.id` | A callee keeps its name -- except a call to the function being canonicalised |
| `canon_pair` | `compiler/parse/mid/unique/canon/expr/more/args.id` | The two-operand shape shared by a binary operator and an index, and the |
| `canon_args` | `compiler/parse/mid/unique/canon/expr/more/args.id` | The two-operand shape shared by a binary operator and an index, and the |
| `sep_at` | `compiler/parse/mid/unique/canon/expr/more/args.id` | The separator before element i of a list: nothing before the first. |
| `canon_bin` | `compiler/parse/mid/unique/canon/expr/more/binop.id` | A binary operator. |
| `bin_op` | `compiler/parse/mid/unique/canon/expr/more/binop.id` | A binary operator. |
| `bin_pair` | `compiler/parse/mid/unique/canon/expr/more/binop.id` | A binary operator. |
| `canon_func` | `compiler/parse/mid/unique/canon/func/more/func.id` | A whole function's fingerprint: its signature and its logic, and nothing of |
| `canon_params` | `compiler/parse/mid/unique/canon/func/more/func.id` | A whole function's fingerprint: its signature and its logic, and nothing of |
| `canon_param` | `compiler/parse/mid/unique/canon/func/more/func.id` | A parameter contributes its type, which is part of the signature, and its |
| `canon_func_body` | `compiler/parse/mid/unique/canon/func/more/tail.id` | The rest of canon_func, split so that block stays within the action limit. |
| `canon_func_tail` | `compiler/parse/mid/unique/canon/func/more/tail.id` | The rest of canon_func, split so that block stays within the action limit. |
| `cn` | `compiler/parse/mid/unique/canon/func/names.id` | The position map: parameters and locals of the function being canonicalised, |
| `cn_add` | `compiler/parse/mid/unique/canon/func/names.id` | The position map: parameters and locals of the function being canonicalised, |
| `canon_ret` | `compiler/parse/mid/unique/canon/func/state.id` | The return clause, and the two pieces of state one canonicalisation needs. |
| `uq_begin` | `compiler/parse/mid/unique/canon/func/state.id` | Start canonicalising the function called `name`: an empty position map, and |
| `cur_self` | `compiler/parse/mid/unique/canon/func/state.id` | The return clause, and the two pieces of state one canonicalisation needs. |
| `canon_else2` | `compiler/parse/mid/unique/canon/stmt/block.id` | The else arm, and a statement list. |
| `canon_else3` | `compiler/parse/mid/unique/canon/stmt/block.id` | An empty `else { }` adds nothing, matching idc.py, where an empty else list |
| `canon_body` | `compiler/parse/mid/unique/canon/stmt/block.id` | Statements in order, separated by `;`. The order matters twice over: it is |
| `canon_decl` | `compiler/parse/mid/unique/canon/stmt/more/decl.id` | Declaration, and index-assignment. |
| `decl_name` | `compiler/parse/mid/unique/canon/stmt/more/decl.id` | The declared name: positional for a local, verbatim for an export, since an |
| `canon_iassign` | `compiler/parse/mid/unique/canon/stmt/more/decl.id` | `base[index] = value`. The parser folds the parsed index target into the |
| `decl_tag` | `compiler/parse/mid/unique/canon/stmt/more/decl_tail.id` | The rest of canon_decl, and the first step of canon_iassign, split so each |
| `decl_tail` | `compiler/parse/mid/unique/canon/stmt/more/decl_tail.id` | The rest of canon_decl, and the first step of canon_iassign, split so each |
| `iassign_tail` | `compiler/parse/mid/unique/canon/stmt/more/decl_tail.id` | The rest of canon_decl, and the first step of canon_iassign, split so each |
| `iassign_val` | `compiler/parse/mid/unique/canon/stmt/more/if_tail.id` | The last step of canon_iassign, and the "if" arm of cst3, split so each of |
| `cst_if` | `compiler/parse/mid/unique/canon/stmt/more/if_tail.id` | The last step of canon_iassign, and the "if" arm of cst3, split so each of |
| `cst_if_tail` | `compiler/parse/mid/unique/canon/stmt/more/if_tail.id` | The last step of canon_iassign, and the "if" arm of cst3, split so each of |
| `canon_stmt` | `compiler/parse/mid/unique/canon/stmt/stmt.id` | Canonicalising a statement, mirroring idc.py's _canon_stmt. Same eager |
| `cst2` | `compiler/parse/mid/unique/canon/stmt/stmt.id` | Canonicalising a statement, mirroring idc.py's _canon_stmt. Same eager |
| `cst3` | `compiler/parse/mid/unique/canon/stmt/stmt.id` | The two compound statements. An if carries its else arm (which may be |
| `check_bodies` | `compiler/parse/mid/unique/scan/check.id` | What check_program() calls in place of check_funcs(): the per-function body |
| `check_unique` | `compiler/parse/mid/unique/scan/check.id` | Function-logic uniqueness: two functions with the same signature and the |
| `uq_fill` | `compiler/parse/mid/unique/scan/check.id` | Fingerprint every function once. Comparing is quadratic in the number of |
| `uq_scan` | `compiler/parse/mid/unique/scan/compare.id` | Comparing the fingerprints. |
| `uq_at` | `compiler/parse/mid/unique/scan/compare.id` | Comparing the fingerprints. |
| `find_dup` | `compiler/parse/mid/unique/scan/compare.id` | The first earlier function with the same fingerprint, or -1. The search |
| `chk_dupfn` | `compiler/parse/mid/unique/scan/more/dup/dup_name.id` | The other "no two functions may be the same" rule: two functions may not |
| `dupfn_at` | `compiler/parse/mid/unique/scan/more/dup/dup_name.id` | Reported against every definition after the first, so three functions of |
| `dupfn_err` | `compiler/parse/mid/unique/scan/more/dup/dup_name.id` | The other "no two functions may be the same" rule: two functions may not |
| `dupfn_check` | `compiler/parse/mid/unique/scan/more/dup/report.id` | Split from dupfn_at and dupfn_err so each of those blocks stays within the |
| `dupfn_print` | `compiler/parse/mid/unique/scan/more/dup/report.id` | Split from dupfn_at and dupfn_err so each of those blocks stays within the |
| `dup_report` | `compiler/parse/mid/unique/scan/more/report/loc.id` | The i-side of report_dup's message: the location the diagnostic points at, |
| `dup_loc` | `compiler/parse/mid/unique/scan/more/report/loc.id` | The i-side of report_dup's message: the location the diagnostic points at, |
| `dup_probe` | `compiler/parse/mid/unique/scan/more/report/report.id` | One comparison, and the diagnostic. |
| `report_dup` | `compiler/parse/mid/unique/scan/more/report/report.id` | One comparison, and the diagnostic. |
| `fname_at` | `compiler/parse/mid/unique/scan/more/report/report.id` | One comparison, and the diagnostic. |
| `dup_tail` | `compiler/parse/mid/unique/scan/more/report/tail.id` | The j-side of report_dup's message: where the earlier, colliding function |
| `dup_tail2` | `compiler/parse/mid/unique/scan/more/report/tail.id` | The j-side of report_dup's message: where the earlier, colliding function |
| `is_reserved_name` | `compiler/parse/mid/unique/scan/more/reserved/check.id` | Named so resv_at's condition does not put a call (resv_names(), s1_of(...)) |
| `resv_print` | `compiler/parse/mid/unique/scan/more/reserved/check.id` | Named so resv_at's condition does not put a call (resv_names(), s1_of(...)) |
| `resv_at` | `compiler/parse/mid/unique/scan/more/reserved/reserved.id` | Not asked of the runtime itself: `--runtime` says this project IS the |
| `resv_err` | `compiler/parse/mid/unique/scan/more/reserved/reserved.id` | A function may not be named after a runtime helper. |

## back -- the C target

| function | file | what |
| --- | --- | --- |
| `dce_prune` | `compiler/parse/back/drive/run/check/dce/dce.id` | Dead-code elimination: emit only what the program can reach. |
| `dce_pack` | `compiler/parse/back/drive/run/check/dce/dce.id` | Compact the program list over itself, keeping the reachable functions in |
| `dce_keep` | `compiler/parse/back/drive/run/check/dce/dce.id` | Keep slot i by writing it at n, the next free slot. n only advances when |
| `guarded_emit` | `compiler/parse/back/drive/run/check/dce/guarded_emit.id` | Emission happens only if every check passed. |
| `guarded_prune` | `compiler/parse/back/drive/run/check/dce/guarded_emit.id` | A freestanding object keeps every function, exactly as a project with no |
| `push_asm_sym` | `compiler/parse/back/drive/run/check/emit_checked.id` | Registered once per name; a platform overload does not add a second row. |
| `push_sym_row` | `compiler/parse/back/drive/run/check/emit_checked.id` | -1 for the node id: an asm function is not in the node store at all, and |
| `emit_checked` | `compiler/parse/back/drive/run/check/emit_checked.id` | Everything the checks need that is not in the AST is set up first -- see |
| `arg_setup` | `compiler/parse/back/drive/run/check/flags/args.id` | What the checks need set up before they run: the asm symbols, the extern |
| `arg_flags` | `compiler/parse/back/drive/run/check/flags/args.id` | Both build flags, set together: this block has the room that arg_setup's |
| `arg_flags_tail` | `compiler/parse/back/drive/run/check/flags/args.id` | What the checks need set up before they run: the asm symbols, the extern |
| `arg_flags2` | `compiler/parse/back/drive/run/check/flags/args2.id` | The rest of arg_flags2, one flag at a time. |
| `arg_flags3` | `compiler/parse/back/drive/run/check/flags/args2.id` | The rest of arg_flags2, one flag at a time. |
| `arg_flags4` | `compiler/parse/back/drive/run/check/flags/args2.id` | The rest of arg_flags2, one flag at a time. |
| `arg_flag` | `compiler/parse/back/drive/run/check/flags/xok.id` | Any flag that is just its own presence, anywhere in the arguments. Scanned |
| `arg_xok` | `compiler/parse/back/drive/run/check/flags/xok.id` | `--extern-ok` anywhere in the arguments. Scanned rather than tested at a |
| `main` | `compiler/parse/back/drive/run/main/main.id` | The exit code is whether the program broke a rule of the language: 1 if a |
| `run` | `compiler/parse/back/drive/run/main/main.id` | Driver: set up state, load the token stream, parse all functions, then emit. |
| `finish` | `compiler/parse/back/drive/run/main/main.id` | Driver: set up state, load the token stream, parse all functions, then emit. |
| `main_tail` | `compiler/parse/back/drive/run/main/tail.id` | The tail of main, once setup and load have already run. |
| `emit` | `compiler/parse/back/drive/run/output_mode.id` | Output mode: the AST dump when given `ast`, else whichever code generator |
| `arg_triple` | `compiler/parse/back/drive/run/output_mode.id` | The target triple: `idparse --triple T`, else the host default. Kept here |
| `register_asm_syms` | `compiler/parse/back/drive/run/output_mode.id` | asm functions are not in the program list, so build_syms never sees them. |
| `emit_line` | `compiler/parse/back/drive/sink/emit_line.id` | The one place emitted code leaves the compiler. |
| `emit_target` | `compiler/parse/back/drive/target/target.id` | Which code generator a build uses. |
| `emit_target2` | `compiler/parse/back/drive/target/target.id` | `crt` is not a code generator: it prints the C runtime with external |
| `arg_target` | `compiler/parse/back/drive/target/target.id` | `--target NAME`, scanned rather than read at a fixed position: the driver |
| `lw_expr` | `compiler/parse/back/ir/build/expr/atom/expr.id` | Expression lowering. Every stage tests the kind first and recurses only in |
| `le2` | `compiler/parse/back/ir/build/expr/atom/expr.id` | Expression lowering. Every stage tests the kind first and recurses only in |
| `le3` | `compiler/parse/back/ir/build/expr/atom/expr.id` | Expression lowering. Every stage tests the kind first and recurses only in |
| `lw_cast2` | `compiler/parse/back/ir/build/expr/atom/lit/conv/cast.id` | A conversion between two numbers is a real instruction; anything else -- |
| `lw_castop` | `compiler/parse/back/ir/build/expr/atom/lit/conv/cast.id` | A conversion between two numbers is a real instruction; anything else -- |
| `lw_castop2` | `compiler/parse/back/ir/build/expr/atom/lit/conv/cast.id` | A conversion between two numbers is a real instruction; anything else -- |
| `lw_as` | `compiler/parse/back/ir/build/expr/atom/lit/conv/conv.id` | Lower an expression and convert it to the type the context wants. C's |
| `lw_conv` | `compiler/parse/back/ir/build/expr/atom/lit/conv/conv.id` | Lower an expression and convert it to the type the context wants. C's |
| `lw_cast` | `compiler/parse/back/ir/build/expr/atom/lit/conv/conv.id` | `bool` is the IR's own type -- the i1 a comparison produces. It never |
| `lw_narrow` | `compiler/parse/back/ir/build/expr/atom/lit/conv/narrow.id` | Both types are integral, so the direction is the only question and the |
| `lw_lit` | `compiler/parse/back/ir/build/expr/atom/lit/lit.id` | A literal is a constant, and a constant is not an instruction anybody emits: |
| `lw_const` | `compiler/parse/back/ir/build/expr/atom/lit/lit.id` | A literal is a constant, and a constant is not an instruction anybody emits: |
| `lw_name` | `compiler/parse/back/ir/build/expr/atom/lit/lit.id` | Reading a name: the address, then a load from it. |
| `lw_name_addr` | `compiler/parse/back/ir/build/expr/atom/lit/more/addr.id` | The stack slot a name reads from or writes to. |
| `lw_ety` | `compiler/parse/back/ir/build/expr/atom/lit/more/ety.id` | The type of an expression. type_of answers it for everything the checks see, |
| `lw_load_at` | `compiler/parse/back/ir/build/expr/atom/lit/more/ety.id` | The type of an expression. type_of answers it for everything the checks see, |
| `lw_tmp` | `compiler/parse/back/ir/build/expr/atom/lit/more/ety.id` | An unnamed stack slot, for a value the source never named -- the result of a |
| `le4` | `compiler/parse/back/ir/build/expr/atom/tail.id` |  |
| `le5` | `compiler/parse/back/ir/build/expr/atom/tail.id` |  |
| `le6` | `compiler/parse/back/ir/build/expr/atom/tail.id` |  |
| `lw_args_more` | `compiler/parse/back/ir/build/expr/call/core/args.id` |  |
| `lw_call` | `compiler/parse/back/ir/build/expr/call/core/call.id` | A call. A builtin becomes whatever the runtime spells it as; anything else |
| `lw_ucall` | `compiler/parse/back/ir/build/expr/call/core/call.id` | A call. A builtin becomes whatever the runtime spells it as; anything else |
| `lw_args` | `compiler/parse/back/ir/build/expr/call/core/call.id` | A call's arguments, each converted to the parameter type the callee |
| `lw_call_dispatch` | `compiler/parse/back/ir/build/expr/call/core/dispatch.id` | A resolved call goes to the runtime spelling; anything else is an ordinary |
| `lw_ucall_name` | `compiler/parse/back/ir/build/expr/call/core/dispatch.id` | A resolved call goes to the runtime spelling; anything else is an ordinary |
| `lw_ucall_rest` | `compiler/parse/back/ir/build/expr/call/core/dispatch.id` | A resolved call goes to the runtime spelling; anything else is an ordinary |
| `lw_arr` | `compiler/parse/back/ir/build/expr/call/list/arr.id` | A list literal: a new list, then one push per element. An empty literal is |
| `lw_arr_els` | `compiler/parse/back/ir/build/expr/call/list/arr.id` | A list literal: a new list, then one push per element. An empty literal is |
| `lw_arr_one` | `compiler/parse/back/ir/build/expr/call/list/arr.id` | A list literal: a new list, then one push per element. An empty literal is |
| `lw_index` | `compiler/parse/back/ir/build/expr/call/list/idx.id` | Reading and writing a list. A list is a runtime object reached only through |
| `lw_index2` | `compiler/parse/back/ir/build/expr/call/list/idx.id` | Reading and writing a list. A list is a runtime object reached only through |
| `lw_iassign` | `compiler/parse/back/ir/build/expr/call/list/idx.id` | Reading and writing a list. A list is a runtime object reached only through |
| `lw_index_base` | `compiler/parse/back/ir/build/expr/call/list/more/at.id` | The pieces of `xs[i]`: the base list, the index, and the element type an |
| `lw_index_idx` | `compiler/parse/back/ir/build/expr/call/list/more/at.id` | The pieces of `xs[i]`: the base list, the index, and the element type an |
| `lw_index_elem` | `compiler/parse/back/ir/build/expr/call/list/more/at.id` | The pieces of `xs[i]`: the base list, the index, and the element type an |
| `lw_iset` | `compiler/parse/back/ir/build/expr/call/list/more/set.id` |  |
| `lw_new` | `compiler/parse/back/ir/build/expr/call/list/more/set.id` | A fresh list. Its own function because the runtime call takes no arguments |
| `lw_arr_new` | `compiler/parse/back/ir/build/expr/call/list/more/val/init.id` | The empty list a literal fills, and the element type its members convert |
| `lw_arr_elem` | `compiler/parse/back/ir/build/expr/call/list/more/val/init.id` | The empty list a literal fills, and the element type its members convert |
| `lw_iset_val` | `compiler/parse/back/ir/build/expr/call/list/more/val/iset.id` | The value half of a list-element assignment: converted, then boxed. The |
| `lw_arr_push` | `compiler/parse/back/ir/build/expr/call/list/more/val/push.id` | One element of a list literal: converted to the element type, then boxed |
| `lw_arr_push2` | `compiler/parse/back/ir/build/expr/call/list/more/val/push.id` | One element of a list literal: converted to the element type, then boxed |
| `lw_box` | `compiler/parse/back/ir/build/expr/call/more/arg/cell/box.id` | A cell of a list is a uniform 64-bit word, so a value entering one is |
| `lw_box2` | `compiler/parse/back/ir/build/expr/call/more/arg/cell/box.id` | A cell of a list is a uniform 64-bit word, so a value entering one is |
| `lw_unbox` | `compiler/parse/back/ir/build/expr/call/more/arg/cell/box.id` | A cell of a list is a uniform 64-bit word, so a value entering one is |
| `lw_unbox2` | `compiler/parse/back/ir/build/expr/call/more/arg/cell/unbox.id` |  |
| `lw_callf` | `compiler/parse/back/ir/build/expr/call/more/arg/emit.id` |  |
| `lw_arg_at` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/args.id` |  |
| `lw_pty` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/args.id` | The declared type of a callee's i-th parameter, or the argument's own type |
| `lw_pty2` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/args.id` | The two halves of the guard are functions rather than names, because `&&` |
| `lw_apty` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/asm.id` | An asm function has no AST node -- its rows are their own table -- so its |
| `lw_arg_pty` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/more/pty.id` | Argument i converted to the type it must arrive as, and the fallback type |
| `lw_arg_conv` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/more/pty.id` | Argument i converted to the type it must arrive as, and the fallback type |
| `lw_pty_base` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/more/pty.id` | Argument i converted to the type it must arrive as, and the fallback type |
| `lw_arg_val` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/more/val.id` |  |
| `lw_has_par` | `compiler/parse/back/ir/build/expr/call/more/arg/ty/more/val.id` | Whether function node f declares an i-th parameter. Reached only after f |
| `lw_builtin` | `compiler/parse/back/ir/build/expr/call/more/bi/builtin.id` | Builtins. Each becomes the runtime call the C target spells for it, with its |
| `lb_b2` | `compiler/parse/back/ir/build/expr/call/more/bi/builtin.id` | Builtins. Each becomes the runtime call the C target spells for it, with its |
| `lb_b3` | `compiler/parse/back/ir/build/expr/call/more/bi/builtin.id` | Builtins. Each becomes the runtime call the C target spells for it, with its |
| `lb_b4` | `compiler/parse/back/ir/build/expr/call/more/bi/list.id` |  |
| `lw_len` | `compiler/parse/back/ir/build/expr/call/more/bi/list.id` | `len` is two different runtime calls: a string knows its own length and a |
| `lw_lenfn` | `compiler/parse/back/ir/build/expr/call/more/bi/list.id` |  |
| `lw_push` | `compiler/parse/back/ir/build/expr/call/more/bi/more/cell.id` |  |
| `lw_push2` | `compiler/parse/back/ir/build/expr/call/more/bi/more/cell.id` |  |
| `lw_pop` | `compiler/parse/back/ir/build/expr/call/more/bi/more/cell.id` |  |
| `lw_pop_mid` | `compiler/parse/back/ir/build/expr/call/more/bi/more/pop.id` | The rest of lw_pop, once the popped list's element type is known. |
| `lw_pop_tail` | `compiler/parse/back/ir/build/expr/call/more/bi/more/pop.id` | The rest of lw_pop, once the popped list's element type is known. |
| `lw_len_tail` | `compiler/parse/back/ir/build/expr/call/more/bi/more/pop.id` | The rest of lw_len, once the counted list's element type is known. |
| `lw_push_tail` | `compiler/parse/back/ir/build/expr/call/more/bi/more/push.id` | The rest of lw_push, once the pushed list's element type is known. |
| `lw_push2_tail` | `compiler/parse/back/ir/build/expr/call/more/bi/more/push.id` | The rest of lw_push2, once the value to push is converted. |
| `lw_bargs` | `compiler/parse/back/ir/build/expr/call/more/plain/args.id` |  |
| `lw_barg_at` | `compiler/parse/back/ir/build/expr/call/more/plain/args.id` |  |
| `lw_bpty` | `compiler/parse/back/ir/build/expr/call/more/plain/args.id` | The type a builtin's i-th argument reaches its runtime helper as. "" means |
| `lw_plain_mid` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/more.id` | The rest of lw_plain, once the runtime symbol's own return type is known. |
| `lw_plain_tail` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/more.id` | The rest of lw_plain, once the runtime symbol's own return type is known. |
| `lw_barg_push` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/more.id` | The rest of lw_barg_val, once the argument is converted. |
| `lw_plain` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/plain.id` | Every other builtin is a plain call to the runtime helper of the same name. |
| `lw_bret` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/plain.id` | Every other builtin is a plain call to the runtime helper of the same name. |
| `lw_bret2` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/plain.id` | The real-time I/O builtins have no entry in builtin_type: their result is |
| `lw_sysret` | `compiler/parse/back/ir/build/expr/call/more/plain/ret/sysret.id` | The flat store works in machine words, so its helpers return one -- a write |
| `lw_bpty2` | `compiler/parse/back/ir/build/expr/call/more/plain/want.id` | The flat store and the unsigned operations work in machine words, so every |
| `lw_bpty3` | `compiler/parse/back/ir/build/expr/call/more/plain/want.id` | The flat store and the unsigned operations work in machine words, so every |
| `lw_barg_val` | `compiler/parse/back/ir/build/expr/call/more/plain/want.id` | The rest of lw_barg_at, once the argument's own position is known. |
| `lw_bin` | `compiler/parse/back/ir/build/expr/op/bin/bin.id` | A binary expression. Four shapes, the same four the C target has: a |
| `lb2` | `compiler/parse/back/ir/build/expr/op/bin/bin.id` | A binary expression. Four shapes, the same four the C target has: a |
| `lb3` | `compiler/parse/back/ir/build/expr/op/bin/bin.id` | `&&` and `||` emit as C infix in the C target, so they short-circuit there. |
| `lw_bin_op` | `compiler/parse/back/ir/build/expr/op/bin/disp.id` | The first of the four shapes: whether the operator is a comparison at all. |
| `lw_ispred` | `compiler/parse/back/ir/build/expr/op/cmp/cmp.id` | A comparison. Its operands widen against each other -- `int` against `word` |
| `lw_cmp` | `compiler/parse/back/ir/build/expr/op/cmp/cmp.id` | A comparison produces the IR's own `bool`, and `id` says the expression is |
| `lw_cmp2` | `compiler/parse/back/ir/build/expr/op/cmp/cmp.id` | A comparison. Its operands widen against each other -- `int` against `word` |
| `lw_cmp_t2` | `compiler/parse/back/ir/build/expr/op/cmp/wide.id` | Continues lw_cmp: the left operand's type is known, so the right operand's |
| `lw_cmp_wide` | `compiler/parse/back/ir/build/expr/op/cmp/wide.id` | Continues lw_cmp: the left operand's type is known, so the right operand's |
| `lw_arith` | `compiler/parse/back/ir/build/expr/op/more/arith/arith.id` | Ordinary arithmetic. The result type is the widened operand type, and both |
| `lw_arith2` | `compiler/parse/back/ir/build/expr/op/more/arith/arith.id` | Ordinary arithmetic. The result type is the widened operand type, and both |
| `lw_arith3` | `compiler/parse/back/ir/build/expr/op/more/arith/arith.id` | Ordinary arithmetic. The result type is the widened operand type, and both |
| `lw_hcall` | `compiler/parse/back/ir/build/expr/op/more/arith/hcall.id` | The shifts and the checked divisions are runtime calls, exactly as they are |
| `lw_hcall2` | `compiler/parse/back/ir/build/expr/op/more/arith/hcall.id` | The shifts and the checked divisions are runtime calls, exactly as they are |
| `lw_hcall2_out` | `compiler/parse/back/ir/build/expr/op/more/arith/hcall.id` | The shifts and the checked divisions are runtime calls, exactly as they are |
| `lw_hcall_lhs` | `compiler/parse/back/ir/build/expr/op/more/arith/more/conv.id` | Continues lw_hcall: the two widenings still run in source order across |
| `lw_hcall_rhs` | `compiler/parse/back/ir/build/expr/op/more/arith/more/conv.id` | Continues lw_hcall: the two widenings still run in source order across |
| `lw_helper` | `compiler/parse/back/ir/build/expr/op/more/arith/more/helper.id` | Which runtime helper an operator needs, or "" when it is an instruction. |
| `lw_divfn` | `compiler/parse/back/ir/build/expr/op/more/arith/more/helper.id` | `int` and `word` division both go through a checked helper; float division |
| `lw_hty` | `compiler/parse/back/ir/build/expr/op/more/arith/more/helper.id` | The shift helpers work on a machine word; the division helpers work at the |
| `lw_arith_lhs` | `compiler/parse/back/ir/build/expr/op/more/arith/more/op.id` | The left operand, converted to the widened type -- split out so lw_arith |
| `lw_arith3_op` | `compiler/parse/back/ir/build/expr/op/more/arith/more/op.id` | Continues lw_arith3: whether the operator is an instruction or a runtime |
| `lw_iop_bits` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/bits.id` |  |
| `lw_iop_int` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/int.id` |  |
| `lw_iop_int2` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/int.id` |  |
| `lw_irop` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/irop.id` | The IR opcode for an operator at a type. Float arithmetic is its own set of |
| `lw_iop_f` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/irop.id` | The IR opcode for an operator at a type. Float arithmetic is its own set of |
| `lw_iop_f2` | `compiler/parse/back/ir/build/expr/op/more/ctrl/irop/irop.id` | The IR opcode for an operator at a type. Float arithmetic is its own set of |
| `lw_logic` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/logic.id` | `&&` and `||`, short-circuiting. The result lands in an unnamed slot rather |
| `lw_ini` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/logic.id` | `a && b` starts at 0 and only the right-hand side can raise it; `a || b` |
| `lw_logic2` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/logic.id` | `&&` and `||`, short-circuiting. The result lands in an unnamed slot rather |
| `lw_logic2_join` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/blocks.id` | Continues lw_logic2: the join block, once the rhs block already exists. |
| `lw_short_term` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/blocks.id` | Continues lw_short: the branch out of the condition block, once the |
| `lw_logic_seed` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/seed.id` | Continues lw_logic: the seed the slot starts at, and storing it, split |
| `lw_logic_store` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/seed.id` | Continues lw_logic: the seed the slot starts at, and storing it, split |
| `lw_logic3` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/short.id` |  |
| `lw_short` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/short.id` |  |
| `lw_arms` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/more/short.id` |  |
| `lw_rhs` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/rhs.id` |  |
| `lw_rhs_val` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/rhs.id` | Continues lw_rhs: the right-hand side's value, once its block is current. |
| `lw_rhs_store` | `compiler/parse/back/ir/build/expr/op/more/ctrl/logic/rhs.id` | Continues lw_rhs_val: store the value, then join the way lb2/lw_logic2 |
| `lw_un2_op` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/tail.id` | Continues lw_un2's instruction arm: the operator's IR name, then the |
| `lw_un2_apply` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/tail.id` | Continues lw_un2's instruction arm: the operator's IR name, then the |
| `lw_not_val` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/tail.id` | Continues lw_not: the operand's type is known, so its value is fetched |
| `lw_un` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/un.id` | Unary `-`, `~` and `!`. The first two keep their operand's type; `!` is a |
| `lw_un2` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/un.id` | Unary `-`, `~` and `!`. The first two keep their operand's type; `!` is a |
| `lw_unop` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/un.id` | Unary `-`, `~` and `!`. The first two keep their operand's type; `!` is a |
| `lw_iszero` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/zero.id` |  |
| `lw_iszero_cmp` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/zero.id` | Continues lw_iszero: the comparison operator is known, so the zero |
| `lw_not` | `compiler/parse/back/ir/build/expr/op/more/ctrl/un/zero.id` |  |
| `lw_ncmp_b` | `compiler/parse/back/ir/build/expr/op/more/str/more/num.id` | Continues lw_ncmp: the right operand, widened the same way as the left, |
| `lw_ncmp_op` | `compiler/parse/back/ir/build/expr/op/more/str/more/num.id` | Continues lw_ncmp: the right operand, widened the same way as the left, |
| `lw_scmp_b` | `compiler/parse/back/ir/build/expr/op/more/str/more/tail.id` | Continues lw_scmp: the right operand, once the left is already computed, |
| `lw_scmp_call` | `compiler/parse/back/ir/build/expr/op/more/str/more/tail.id` | Continues lw_scmp: the right operand, once the left is already computed, |
| `lw_concat_b` | `compiler/parse/back/ir/build/expr/op/more/str/more/tail.id` | Continues lw_concat: the right operand, once the left is already in |
| `lw_tostr` | `compiler/parse/back/ir/build/expr/op/more/str/more/tostr.id` | An operand in string context: the runtime call that turns a value of some |
| `lw_tostr_at` | `compiler/parse/back/ir/build/expr/op/more/str/more/tostr.id` | An operand in string context: the runtime call that turns a value of some |
| `lw_strfn` | `compiler/parse/back/ir/build/expr/op/more/str/more/tostr.id` | An operand in string context: the runtime call that turns a value of some |
| `lw_ncmp` | `compiler/parse/back/ir/build/expr/op/more/str/ncmp.id` | A numeric comparison: both operands widen to the type they were compared |
| `lw_strcmp` | `compiler/parse/back/ir/build/expr/op/more/str/ncmp.id` | libc's strcmp when there is a libc, and the runtime's own when there is not. |
| `lw_scmp` | `compiler/parse/back/ir/build/expr/op/more/str/str.id` | String equality is strcmp against zero, as it is in the C target. strcmp is |
| `lw_scmp2` | `compiler/parse/back/ir/build/expr/op/more/str/str.id` | String equality is strcmp against zero, as it is in the C target. strcmp is |
| `lw_concat` | `compiler/parse/back/ir/build/expr/op/more/str/str.id` | String `+` builds a new string; each operand reaches it already converted to |
| `lw_fin` | `compiler/parse/back/ir/build/func/lower/ends.id` | The body, then the return clause, then the entry block's branch into the |
| `lw_ret` | `compiler/parse/back/ir/build/func/lower/ends.id` | `id` has no return statement: the return clause is an expression after the |
| `lw_ret_val` | `compiler/parse/back/ir/build/func/lower/ends.id` | The body, then the return clause, then the entry block's branch into the |
| `lw_locals` | `compiler/parse/back/ir/build/func/lower/loc/list.id` | Every declaration in the body gets its slot before any statement is lowered. |
| `lw_loc_list` | `compiler/parse/back/ir/build/func/lower/loc/list.id` | Every declaration in the body gets its slot before any statement is lowered. |
| `lw_loc_one` | `compiler/parse/back/ir/build/func/lower/loc/list.id` | Every declaration in the body gets its slot before any statement is lowered. |
| `lw_loc_loop` | `compiler/parse/back/ir/build/func/lower/loc/loop.id` |  |
| `lw_loc_decl` | `compiler/parse/back/ir/build/func/lower/loc/nest.id` | An exported declaration is a global, which needs no slot. |
| `lw_loc_nest` | `compiler/parse/back/ir/build/func/lower/loc/nest.id` | An exported declaration is a global, which needs no slot. |
| `lw_loc_branch` | `compiler/parse/back/ir/build/func/lower/loc/nest.id` | An exported declaration is a global, which needs no slot. |
| `lw_fin_tail` | `compiler/parse/back/ir/build/func/lower/more/ends_tail.id` | The rest of lw_fin: the statements, once the parameters have their slots. |
| `lw_ret_tail` | `compiler/parse/back/ir/build/func/lower/more/ends_tail.id` | The rest of lw_ret, once the entry block and the branch into the body are |
| `lw_func_mid` | `compiler/parse/back/ir/build/func/lower/more/func_tail.id` | The rest of lw_func, once the IR function itself exists. |
| `lw_func_tail` | `compiler/parse/back/ir/build/func/lower/more/func_tail.id` | The rest of lw_func, once the IR function itself exists. |
| `ir_lower` | `compiler/parse/back/ir/build/func/lower/more/lower.id` | AST to IR, one function at a time. |
| `lw_func` | `compiler/parse/back/ir/build/func/lower/more/lower.id` | AST to IR, one function at a time. |
| `lw_open` | `compiler/parse/back/ir/build/func/lower/more/lower.id` | AST to IR, one function at a time. |
| `lw_bind` | `compiler/parse/back/ir/build/func/slot/map.id` |  |
| `lw_slot` | `compiler/parse/back/ir/build/func/slot/map.id` |  |
| `lw_addr` | `compiler/parse/back/ir/build/func/slot/map.id` | Where a name lives: an alloca for a local, the global itself for an export. |
| `lw_vty` | `compiler/parse/back/ir/build/func/slot/more/alloca.id` | The declared type of an exported name, or "" when the name is not exported. |
| `lw_alloca` | `compiler/parse/back/ir/build/func/slot/more/alloca.id` | A local's stack slot, created in the entry block so that it dominates every |
| `lw_store_at` | `compiler/parse/back/ir/build/func/slot/more/alloca.id` | The declared type of an exported name, or "" when the name is not exported. |
| `lw_param_mid` | `compiler/parse/back/ir/build/func/slot/more/param.id` | The rest of lw_param, once the parameter's type and name are in hand. |
| `lw_param_tail` | `compiler/parse/back/ir/build/func/slot/more/param.id` | The rest of lw_param, once the parameter's type and name are in hand. |
| `lw_alloca_tail` | `compiler/parse/back/ir/build/func/slot/more/tail.id` | The rest of lw_alloca, once the entry block and the value itself exist. |
| `lw_pslot_tail` | `compiler/parse/back/ir/build/func/slot/more/tail.id` | The rest of lw_pslot, once the parameter's type and name are in hand. |
| `lw_params` | `compiler/parse/back/ir/build/func/slot/par.id` |  |
| `lw_param` | `compiler/parse/back/ir/build/func/slot/par.id` |  |
| `lw_pslot` | `compiler/parse/back/ir/build/func/slot/par.id` | A parameter is copied into a stack slot like any other local, so that an |
| `lw_brto` | `compiler/parse/back/ir/build/func/state/emit/br.id` | A branch built rather than emitted: it goes at the end of a block the |
| `lw_ent` | `compiler/parse/back/ir/build/func/state/emit/emit.id` |  |
| `lw_emit` | `compiler/parse/back/ir/build/func/state/emit/emit.id` | Create an instruction and append it to the block being built. Everything |
| `lw_reset` | `compiler/parse/back/ir/build/func/state/emit/emit.id` |  |
| `lw_op` | `compiler/parse/back/ir/build/func/state/emit/op.id` | The three shapes an instruction is built in. An empty list literal needs a |
| `lw_term` | `compiler/parse/back/ir/build/func/state/emit/op.id` | A terminator: no value, no operand list, and the blocks it can reach. |
| `ir_leaf` | `compiler/parse/back/ir/build/func/state/emit/op.id` | A leaf that lives in no block -- a constant, a global, an alloca, a |
| `init_lw` | `compiler/parse/back/ir/build/func/state/more/cell.id` | Lowering state. One function is lowered at a time, so this is a handful of |
| `init_lw2` | `compiler/parse/back/ir/build/func/state/more/cell.id` | Lowering state. One function is lowered at a time, so this is a handful of |
| `init_lw3` | `compiler/parse/back/ir/build/func/state/more/cell.id` | Lowering state. One function is lowered at a time, so this is a handful of |
| `lw_open2_tail` | `compiler/parse/back/ir/build/func/state/more/open.id` | The rest of lw_open2, once the entry block itself exists. |
| `lw_fn` | `compiler/parse/back/ir/build/func/state/place.id` |  |
| `lw_go` | `compiler/parse/back/ir/build/func/state/place.id` |  |
| `lw_open2` | `compiler/parse/back/ir/build/func/state/place.id` | Two blocks, not one: the entry block holds nothing but allocas and the |
| `lw_stmts` | `compiler/parse/back/ir/build/stmt/decl/stmt.id` | Statement lowering. The dispatch tests the kind first and only then recurses, |
| `lw_stmt` | `compiler/parse/back/ir/build/stmt/decl/stmt.id` | Statement lowering. The dispatch tests the kind first and only then recurses, |
| `lw_decl` | `compiler/parse/back/ir/build/stmt/decl/stmt.id` | An exported declaration writes the global; a plain one writes its slot. |
| `lw_decl_tail` | `compiler/parse/back/ir/build/stmt/decl/tail.id` |  |
| `lw_decl_tail2` | `compiler/parse/back/ir/build/stmt/decl/tail.id` |  |
| `lw_arm` | `compiler/parse/back/ir/build/stmt/flow/cond/arm.id` | One arm of a branch, or the body of a loop: a block, its statements, and the |
| `lw_head` | `compiler/parse/back/ir/build/stmt/flow/cond/arm.id` | The loop test. Written out rather than folded into the cbr call because the |
| `lw_truth_tail` | `compiler/parse/back/ir/build/stmt/flow/cond/more/tail.id` |  |
| `lw_cond` | `compiler/parse/back/ir/build/stmt/flow/cond/more/truth.id` | A condition in `id` is any integral value, true when it is nonzero, so a |
| `lw_truth` | `compiler/parse/back/ir/build/stmt/flow/cond/more/truth.id` | A condition in `id` is any integral value, true when it is nonzero, so a |
| `lw_zero` | `compiler/parse/back/ir/build/stmt/flow/cond/more/truth.id` | A condition in `id` is any integral value, true when it is nonzero, so a |
| `lw_cmpop` | `compiler/parse/back/ir/build/stmt/flow/cond/op.id` | icmp or fcmp: which comparison an operand type takes. The predicate itself |
| `lw_if_arms` | `compiler/parse/back/ir/build/stmt/flow/if/arms.id` |  |
| `lw_if_arms2` | `compiler/parse/back/ir/build/stmt/flow/if/arms.id` |  |
| `lw_if` | `compiler/parse/back/ir/build/stmt/flow/if/if.id` | if / else if / else. Three blocks: the two arms and the join they both |
| `lw_if_thn` | `compiler/parse/back/ir/build/stmt/flow/if/if.id` | if / else if / else. Three blocks: the two arms and the join they both |
| `lw_if2` | `compiler/parse/back/ir/build/stmt/flow/if/if.id` | if / else if / else. Three blocks: the two arms and the join they both |
| `lw_if_join` | `compiler/parse/back/ir/build/stmt/flow/if/join.id` |  |
| `lw_if3` | `compiler/parse/back/ir/build/stmt/flow/if/join.id` |  |
| `lw_while_ex` | `compiler/parse/back/ir/build/stmt/flow/loop/body.id` |  |
| `lw_while3` | `compiler/parse/back/ir/build/stmt/flow/loop/body.id` |  |
| `lw_while_body` | `compiler/parse/back/ir/build/stmt/flow/loop/body.id` |  |
| `lw_while` | `compiler/parse/back/ir/build/stmt/flow/loop/loop.id` | while. Four blocks in the usual shape: a head that tests, a body that runs |
| `lw_while_go` | `compiler/parse/back/ir/build/stmt/flow/loop/loop.id` | while. Four blocks in the usual shape: a head that tests, a body that runs |
| `lw_while2` | `compiler/parse/back/ir/build/stmt/flow/loop/loop.id` | while. Four blocks in the usual shape: a head that tests, a body that runs |
| `ls2` | `compiler/parse/back/ir/build/stmt/more/assign.id` |  |
| `lw_assign` | `compiler/parse/back/ir/build/stmt/more/assign.id` |  |
| `lw_aty` | `compiler/parse/back/ir/build/stmt/more/assign.id` | The declared type of an assignment's target: an export's if it is one, and |
| `lw_assign_tail` | `compiler/parse/back/ir/build/stmt/more/store.id` |  |
| `lw_assign_tail2` | `compiler/parse/back/ir/build/stmt/more/store.id` |  |
| `ls3` | `compiler/parse/back/ir/build/stmt/more/tail.id` |  |
| `ls4` | `compiler/parse/back/ir/build/stmt/more/tail.id` |  |
| `ls5` | `compiler/parse/back/ir/build/stmt/more/tail.id` |  |
| `blk_count` | `compiler/parse/back/ir/core/read/count.id` | Composed counts, for the many passes that walk a function's blocks or a |
| `insn_count` | `compiler/parse/back/ir/core/read/count.id` | Composed counts, for the many passes that walk a function's blocks or a |
| `blk_fn` | `compiler/parse/back/ir/core/read/frame/block.id` |  |
| `blk_ins` | `compiler/parse/back/ir/core/read/frame/block.id` |  |
| `blk_lbl` | `compiler/parse/back/ir/core/read/frame/block.id` |  |
| `irf_name` | `compiler/parse/back/ir/core/read/frame/func.id` |  |
| `irf_ret` | `compiler/parse/back/ir/core/read/frame/func.id` |  |
| `irf_par` | `compiler/parse/back/ir/core/read/frame/func.id` |  |
| `irf_blk` | `compiler/parse/back/ir/core/read/frame/more.id` |  |
| `irf_node` | `compiler/parse/back/ir/core/read/frame/more.id` |  |
| `blk_pre` | `compiler/parse/back/ir/core/read/frame/more.id` |  |
| `ir_op` | `compiler/parse/back/ir/core/read/insn/insn.id` | Reading an instruction. Every pass and the printer go through these rather |
| `ir_ty` | `compiler/parse/back/ir/core/read/insn/insn.id` | Reading an instruction. Every pass and the printer go through these rather |
| `ir_a` | `compiler/parse/back/ir/core/read/insn/insn.id` | Reading an instruction. Every pass and the printer go through these rather |
| `ir_b` | `compiler/parse/back/ir/core/read/insn/more.id` |  |
| `ir_s` | `compiler/parse/back/ir/core/read/insn/more.id` |  |
| `ir_l` | `compiler/parse/back/ir/core/read/insn/more.id` |  |
| `ir_bl` | `compiler/parse/back/ir/core/read/insn/place.id` |  |
| `ir_home` | `compiler/parse/back/ir/core/read/insn/place.id` |  |
| `ir_isconst` | `compiler/parse/back/ir/core/read/insn/place.id` | A constant is not an instruction anybody emits: it prints as its literal at |
| `ir_add` | `compiler/parse/back/ir/core/store/edit/place.id` | Putting an instruction into a block, and taking one out again. |
| `ir_kill` | `compiler/parse/back/ir/core/store/edit/place.id` | Putting an instruction into a block, and taking one out again. |
| `ir_set_op` | `compiler/parse/back/ir/core/store/edit/place.id` | Putting an instruction into a block, and taking one out again. |
| `ir_set_ty` | `compiler/parse/back/ir/core/store/edit/set.id` | Rewriting an instruction's fields. A pass that folds `1 + 2` does it by |
| `ir_set_a` | `compiler/parse/back/ir/core/store/edit/set.id` | Rewriting an instruction's fields. A pass that folds `1 + 2` does it by |
| `ir_set_b` | `compiler/parse/back/ir/core/store/edit/set.id` | Rewriting an instruction's fields. A pass that folds `1 + 2` does it by |
| `ir_rauw_l` | `compiler/parse/back/ir/core/store/edit/use/one.id` |  |
| `ir_rauw_one` | `compiler/parse/back/ir/core/store/edit/use/one.id` |  |
| `op_rauw` | `compiler/parse/back/ir/core/store/edit/use/rauw.id` | Replace every use of one value with another, across one function. Passes |
| `op_rauw_b` | `compiler/parse/back/ir/core/store/edit/use/rauw.id` | Replace every use of one value with another, across one function. Passes |
| `ir_rauw_at` | `compiler/parse/back/ir/core/store/edit/use/rauw.id` | Replace every use of one value with another, across one function. Passes |
| `ir_set_s` | `compiler/parse/back/ir/core/store/edit/use/sub.id` |  |
| `ir_set_bl` | `compiler/parse/back/ir/core/store/edit/use/sub.id` |  |
| `ir_set_l` | `compiler/parse/back/ir/core/store/edit/use/sub.id` |  |
| `init_ir4` | `compiler/parse/back/ir/core/store/init/frame.id` | The block and function columns. A block is a straight line of instructions |
| `init_ir5` | `compiler/parse/back/ir/core/store/init/frame.id` | The block and function columns. A block is a straight line of instructions |
| `init_ir6` | `compiler/parse/back/ir/core/store/init/frame.id` | The block and function columns. A block is a straight line of instructions |
| `init_ir` | `compiler/parse/back/ir/core/store/init/insn.id` | The IR store, in the same parallel-list style as the AST -- and for the same |
| `init_ir2` | `compiler/parse/back/ir/core/store/init/insn.id` | The IR store, in the same parallel-list style as the AST -- and for the same |
| `init_ir3` | `compiler/parse/back/ir/core/store/init/insn.id` | The IR store, in the same parallel-list style as the AST -- and for the same |
| `init_ir7` | `compiler/parse/back/ir/core/store/init/more.id` | The columns left over once the other init functions took two apiece: the |
| `init_ir8` | `compiler/parse/back/ir/core/store/init/more.id` | The columns left over once the other init functions took two apiece: the |
| `init_ir9` | `compiler/parse/back/ir/core/store/init/more.id` | The columns left over once the other init functions took two apiece: the |
| `ir_block` | `compiler/parse/back/ir/core/store/make/block/block.id` | A block belongs to a function from the moment it exists and is appended to |
| `ir_blk2` | `compiler/parse/back/ir/core/store/make/block/block.id` | A block belongs to a function from the moment it exists and is appended to |
| `ir_blk3` | `compiler/parse/back/ir/core/store/make/block/block.id` | A block belongs to a function from the moment it exists and is appended to |
| `ir_blk4` | `compiler/parse/back/ir/core/store/make/block/more.id` | The new block's own index. Its predecessors, instructions and label are |
| `ir_func` | `compiler/parse/back/ir/core/store/make/func/func.id` | An IR function. Its block list is empty at this point; the lowering fills it. |
| `ir_func2` | `compiler/parse/back/ir/core/store/make/func/func.id` | The parameter list starts empty and lw_param fills it: a function's |
| `ir_func3` | `compiler/parse/back/ir/core/store/make/func/func.id` | An IR function. Its block list is empty at this point; the lowering fills it. |
| `ir_func4` | `compiler/parse/back/ir/core/store/make/func/more.id` | The new function's own index, once its name, parameters and node are |
| `ir_col3` | `compiler/parse/back/ir/core/store/make/insn/cols.id` | The last two instruction columns. An instruction starts with no home block: |
| `ir_insn_fin` | `compiler/parse/back/ir/core/store/make/insn/cols.id` | The new instruction's own id, once every column has a row for it: the id |
| `ir_insn` | `compiler/parse/back/ir/core/store/make/insn/insn.id` | Building an instruction. The returned value id IS the instruction's index -- |
| `ir_col1` | `compiler/parse/back/ir/core/store/make/insn/insn.id` | Building an instruction. The returned value id IS the instruction's index -- |
| `ir_col2` | `compiler/parse/back/ir/core/store/make/insn/insn.id` | Building an instruction. The returned value id IS the instruction's index -- |
| `op_dce` | `compiler/parse/back/ir/opt/cfg/dce/dce.id` | Dead code elimination: an instruction with no users and no effect is |
| `dce_sweep` | `compiler/parse/back/ir/opt/cfg/dce/dce.id` | Dead code elimination: an instruction with no users and no effect is |
| `dce_blk` | `compiler/parse/back/ir/opt/cfg/dce/dce.id` | Dead code elimination: an instruction with no users and no effect is |
| `dce_one` | `compiler/parse/back/ir/opt/cfg/dce/one.id` |  |
| `dce_pure` | `compiler/parse/back/ir/opt/cfg/dce/one.id` | What may not be removed for having no users. A call is here because nothing |
| `dce_used` | `compiler/parse/back/ir/opt/cfg/dce/one.id` | How many instructions of this function name the value. An SSA value is |
| `dce_lists` | `compiler/parse/back/ir/opt/cfg/dce/use/find.id` | Whether `id` appears among an instruction's auxiliary values -- a call's |
| `dce_used_b` | `compiler/parse/back/ir/opt/cfg/dce/use/use.id` |  |
| `dce_refs` | `compiler/parse/back/ir/opt/cfg/dce/use/use.id` | A killed instruction keeps its operands, so it is skipped rather than read: |
| `dce_names` | `compiler/parse/back/ir/opt/cfg/dce/use/use.id` |  |
| `pred_wipe` | `compiler/parse/back/ir/opt/cfg/more/more.id` |  |
| `pred_of` | `compiler/parse/back/ir/opt/cfg/more/more.id` | A block's successors are the blocks its terminator names, and its terminator |
| `pred_succ` | `compiler/parse/back/ir/opt/cfg/more/more.id` |  |
| `succ_count` | `compiler/parse/back/ir/opt/cfg/more/succ.id` | A terminator's targets and what each one gets: `b` pushed onto that |
| `pred_succ_at` | `compiler/parse/back/ir/opt/cfg/more/succ.id` | A terminator's targets and what each one gets: `b` pushed onto that |
| `pred_clear_at` | `compiler/parse/back/ir/opt/cfg/pred/more.id` | One block's predecessor list, wiped before the rescan refills it. |
| `op_preds` | `compiler/parse/back/ir/opt/cfg/pred/pred.id` | The predecessors of every block, computed rather than maintained. |
| `pred_clear` | `compiler/parse/back/ir/opt/cfg/pred/pred.id` | The predecessors of every block, computed rather than maintained. |
| `pred_scan` | `compiler/parse/back/ir/opt/cfg/pred/pred.id` | The predecessors of every block, computed rather than maintained. |
| `m2r_rows` | `compiler/parse/back/ir/opt/mem/init/blank/blank.id` |  |
| `m2r_row` | `compiler/parse/back/ir/opt/mem/init/blank/blank.id` |  |
| `m2r_blank` | `compiler/parse/back/ir/opt/mem/init/blank/blank.id` |  |
| `m2r_row2` | `compiler/parse/back/ir/opt/mem/init/blank/more.id` | The blank row for `outv`, pushed after `phiof`'s: they are the same shape |
| `op_mem2reg` | `compiler/parse/back/ir/opt/mem/init/init.id` | mem2reg: stack slots become SSA values. |
| `m2r_init` | `compiler/parse/back/ir/opt/mem/init/init.id` | mem2reg: stack slots become SSA values. |
| `m2r_reset` | `compiler/parse/back/ir/opt/mem/init/init.id` | mem2reg: stack slots become SSA values. |
| `entry_of` | `compiler/parse/back/ir/opt/mem/init/rows/more.id` | The entry block, and what `m2r_collect` needs of it: only the entry ever |
| `entry_count` | `compiler/parse/back/ir/opt/mem/init/rows/more.id` | The entry block, and what `m2r_collect` needs of it: only the entry ever |
| `m2r_collect_at` | `compiler/parse/back/ir/opt/mem/init/rows/more.id` | The entry block, and what `m2r_collect` needs of it: only the entry ever |
| `m2r_wipe` | `compiler/parse/back/ir/opt/mem/init/rows/rows.id` |  |
| `m2r_collect` | `compiler/parse/back/ir/opt/mem/init/rows/rows.id` | The slots, in the order they were allocated -- which is the entry block, in |
| `m2r_take` | `compiler/parse/back/ir/opt/mem/init/rows/rows.id` |  |
| `m2r_front` | `compiler/parse/back/ir/opt/mem/phi/front.id` | A phi has to be the first instruction of its block, and a list has no |
| `m2r_shift` | `compiler/parse/back/ir/opt/mem/phi/front.id` | A phi has to be the first instruction of its block, and a list has no |
| `m2r_swap` | `compiler/parse/back/ir/opt/mem/phi/front.id` | A phi has to be the first instruction of its block, and a list has no |
| `m2r_front_fin` | `compiler/parse/back/ir/opt/mem/phi/more.id` | The tail of `m2r_front`: once the phi is on the end of the block, put its |
| `m2r_phi_make` | `compiler/parse/back/ir/opt/mem/phi/more.id` | The tail of `m2r_phi_one`, split at the point where the phi has a value id |
| `m2r_phi_place` | `compiler/parse/back/ir/opt/mem/phi/more.id` | The tail of `m2r_front`: once the phi is on the end of the block, put its |
| `m2r_phis` | `compiler/parse/back/ir/opt/mem/phi/phi.id` | A phi for every slot in every block but the entry, before anything is |
| `m2r_phi_blk` | `compiler/parse/back/ir/opt/mem/phi/phi.id` | A phi for every slot in every block but the entry, before anything is |
| `m2r_phi_one` | `compiler/parse/back/ir/opt/mem/phi/phi.id` | A phi for every slot in every block but the entry, before anything is |
| `m2r_load` | `compiler/parse/back/ir/opt/mem/walk/body/scan/ldst.id` |  |
| `m2r_store` | `compiler/parse/back/ir/opt/mem/walk/body/scan/ldst.id` |  |
| `m2r_kept` | `compiler/parse/back/ir/opt/mem/walk/body/scan/ldst.id` |  |
| `m2r_scan` | `compiler/parse/back/ir/opt/mem/walk/body/scan/scan.id` | One block, in order: a load of a slot becomes the value the slot holds here, |
| `m2r_step` | `compiler/parse/back/ir/opt/mem/walk/body/scan/scan.id` | One block, in order: a load of a slot becomes the value the slot holds here, |
| `m2r_slot` | `compiler/parse/back/ir/opt/mem/walk/body/scan/scan.id` | One block, in order: a load of a slot becomes the value the slot holds here, |
| `m2r_seed` | `compiler/parse/back/ir/opt/mem/walk/body/seed.id` | The value each slot holds on entry to a block: the phi placed for it, or -- |
| `m2r_seed1` | `compiler/parse/back/ir/opt/mem/walk/body/seed.id` | The value each slot holds on entry to a block: the phi placed for it, or -- |
| `m2r_in` | `compiler/parse/back/ir/opt/mem/walk/body/seed.id` | The value each slot holds on entry to a block: the phi placed for it, or -- |
| `m2r_pass2` | `compiler/parse/back/ir/opt/mem/walk/body/walk.id` |  |
| `m2r_walk` | `compiler/parse/back/ir/opt/mem/walk/body/walk.id` |  |
| `m2r_blk` | `compiler/parse/back/ir/opt/mem/walk/body/walk.id` |  |
| `m2r_after` | `compiler/parse/back/ir/opt/mem/walk/clean/clean.id` |  |
| `m2r_clean` | `compiler/parse/back/ir/opt/mem/walk/clean/clean.id` | A phi that turned out to be one value is that value. Iterated, because |
| `m2r_end` | `compiler/parse/back/ir/opt/mem/walk/clean/clean.id` |  |
| `ir_l_count` | `compiler/parse/back/ir/opt/mem/walk/clean/same/count.id` | A phi's incoming-value count, composed so `ir_l` -- a call -- does not sit |
| `m2r_same` | `compiler/parse/back/ir/opt/mem/walk/clean/same/same.id` | The single value a phi is, or -1 if it has none yet and -2 if it has more |
| `m2r_pick` | `compiler/parse/back/ir/opt/mem/walk/clean/same/same.id` | The single value a phi is, or -1 if it has none yet and -2 if it has more |
| `m2r_drop` | `compiler/parse/back/ir/opt/mem/walk/clean/same/same.id` | Replace every use of a value with another and remove what defined it. The |
| `m2r_sweep` | `compiler/parse/back/ir/opt/mem/walk/clean/sweep.id` |  |
| `m2r_sweep_b` | `compiler/parse/back/ir/opt/mem/walk/clean/sweep.id` |  |
| `m2r_try` | `compiler/parse/back/ir/opt/mem/walk/clean/sweep.id` |  |
| `m2r_fill` | `compiler/parse/back/ir/opt/mem/walk/phi/fill.id` | Every phi's incoming edges, once every block's outgoing values are known. |
| `m2r_fill_b` | `compiler/parse/back/ir/opt/mem/walk/phi/fill.id` | Every phi's incoming edges, once every block's outgoing values are known. |
| `m2r_fill_one` | `compiler/parse/back/ir/opt/mem/walk/phi/fill.id` | Every phi's incoming edges, once every block's outgoing values are known. |
| `m2r_pre_at` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/at.id` | The block a phi's `i`th incoming edge comes from, and where it sits in |
| `m2r_pos` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/at.id` | The block a phi's `i`th incoming edge comes from, and where it sits in |
| `m2r_incs` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/incs.id` |  |
| `m2r_pre_count` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/incs.id` | A block's predecessor count, composed so `blk_pre` -- a call -- does not |
| `m2r_inc` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/incs.id` |  |
| `m2r_inc_at` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/push.id` | A phi's incoming edge, in two halves: the value the predecessor left this |
| `m2r_inc_val` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/push.id` | A phi's incoming edge, in two halves: the value the predecessor left this |
| `m2r_inc_blk` | `compiler/parse/back/ir/opt/mem/walk/phi/incs/push.id` | A phi's incoming edge, in two halves: the value the predecessor left this |
| `init_opt` | `compiler/parse/back/ir/opt/run/cell.id` | What a pass needs to know beyond the function it is given. |
| `arg_opt` | `compiler/parse/back/ir/opt/run/cell.id` | The optimisation level, from `--opt N`. Level 0 emits the lowering as it |
| `fold_cmp` | `compiler/parse/back/ir/opt/run/fold/cmp/cmp.id` |  |
| `fold_iszext` | `compiler/parse/back/ir/opt/run/fold/cmp/cmp.id` |  |
| `fold_zero` | `compiler/parse/back/ir/opt/run/fold/cmp/cmp.id` |  |
| `fold_isbool` | `compiler/parse/back/ir/opt/run/fold/cmp/more.id` | The two operands `fold_iszext` and `fold_cmp` test on the right of an |
| `fold_zero_of` | `compiler/parse/back/ir/opt/run/fold/cmp/more.id` | The two operands `fold_iszext` and `fold_cmp` test on the right of an |
| `op_fold` | `compiler/parse/back/ir/opt/run/fold/fold.id` | The folds mem2reg makes visible. |
| `fold_blk` | `compiler/parse/back/ir/opt/run/fold/fold.id` | The folds mem2reg makes visible. |
| `fold_one` | `compiler/parse/back/ir/opt/run/fold/fold.id` | The folds mem2reg makes visible. |
| `fold_kill` | `compiler/parse/back/ir/opt/run/fold/kill.id` |  |
| `opt_clean` | `compiler/parse/back/ir/opt/run/pipe/clean.id` | Folding and sweeping, in that order and twice: a fold turns a comparison |
| `opt_clean2` | `compiler/parse/back/ir/opt/run/pipe/clean.id` | Folding and sweeping, in that order and twice: a fold turns a comparison |
| `ir_opt` | `compiler/parse/back/ir/opt/run/pipe/run.id` | The pass pipeline. |
| `opt_func` | `compiler/parse/back/ir/opt/run/pipe/run.id` | The pass pipeline. |
| `opt_round` | `compiler/parse/back/ir/opt/run/pipe/run.id` | The pass pipeline. |
| `pe_args` | `compiler/parse/back/tgt/ast/expr/args.id` | Render a child-id list as space-prefixed printed expressions. |
| `print_expr` | `compiler/parse/back/tgt/ast/expr/expr.id` | Expression printer (S-expression). Dispatch on node kind via a helper chain. |
| `pe_op` | `compiler/parse/back/tgt/ast/expr/expr.id` | Expression printer (S-expression). Dispatch on node kind via a helper chain. |
| `pe_atom` | `compiler/parse/back/tgt/ast/expr/expr.id` | Expression printer (S-expression). Dispatch on node kind via a helper chain. |
| `print_program` | `compiler/parse/back/tgt/ast/prog/func.id` | Program printer: render each parsed function as a nested S-expression. |
| `show_func` | `compiler/parse/back/tgt/ast/prog/func.id` | Program printer: render each parsed function as a nested S-expression. |
| `print_func` | `compiler/parse/back/tgt/ast/prog/func.id` | Program printer: render each parsed function as a nested S-expression. |
| `print_list` | `compiler/parse/back/tgt/ast/prog/node.id` | List joiner (params or statements), node routing, and the first statement |
| `print_node` | `compiler/parse/back/tgt/ast/prog/node.id` | List joiner (params or statements), node routing, and the first statement |
| `print_stmt` | `compiler/parse/back/tgt/ast/prog/node.id` | List joiner (params or statements), node routing, and the first statement |
| `print_func_tail` | `compiler/parse/back/tgt/ast/prog/tail.id` | The tail of print_func: the body and return clause, once the signature is |
| `print_ret` | `compiler/parse/back/tgt/ast/stmt/ret.id` | The return clause: `(return EXPR)`, or `(return void)` (recorded as -1). |
| `ps2` | `compiler/parse/back/tgt/ast/stmt/stmt.id` | Remaining statement forms. |
| `ps3` | `compiler/parse/back/tgt/ast/stmt/stmt.id` | Remaining statement forms. |
| `ps4` | `compiler/parse/back/tgt/ast/stmt/stmt.id` | Remaining statement forms. |
| `ps4_while` | `compiler/parse/back/tgt/ast/stmt/while_if.id` | The while form of ps4, tried before the if form takes over. |
| `ps4_if` | `compiler/parse/back/tgt/ast/stmt/while_if.id` | The if form of ps4, once the condition and the then-branch are known. |
| `emit_asm_all` | `compiler/parse/back/tgt/c/emit/asm/def/emit_all.id` | Emission of asm functions for the selected platform, and the error for a |
| `emit_asm_row` | `compiler/parse/back/tgt/c/emit/asm/def/emit_all.id` | Emission of asm functions for the selected platform, and the error for a |
| `asm_check` | `compiler/parse/back/tgt/c/emit/asm/def/emit_all.id` | A row written for another platform is fine, as long as some row of the same |
| `asm_first` | `compiler/parse/back/tgt/c/emit/asm/def/first_row.id` |  |
| `asm_prev` | `compiler/parse/back/tgt/c/emit/asm/def/first_row.id` |  |
| `asm_match2` | `compiler/parse/back/tgt/c/emit/asm/def/first_row.id` |  |
| `asm_stmt_lines` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/lines.id` | The three lines of one asm function's body, once every piece of the |
| `emit_asm_one` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/sig.id` | Signature and return-variable declaration of one asm function's C body. |
| `emit_asm_header` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/sig.id` | Signature and return-variable declaration of one asm function's C body. |
| `emit_asm_decl` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/sig.id` | Signature and return-variable declaration of one asm function's C body. |
| `asm_stmt` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/stmt.id` | GCC extended asm. Every block is volatile with a memory clobber: that is |
| `asm_stmt2` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/stmt.id` | GCC extended asm. Every block is volatile with a memory clobber: that is |
| `asm_stmt3` | `compiler/parse/back/tgt/c/emit/asm/def/more/body/stmt.id` | GCC extended asm. Every block is volatile with a memory clobber: that is |
| `asm_text` | `compiler/parse/back/tgt/c/emit/asm/def/more/operands.id` | The instruction lines are stored with their quotes, so they are emitted as |
| `asm_sep` | `compiler/parse/back/tgt/c/emit/asm/def/more/operands.id` | The instruction lines are stored with their quotes, so they are emitted as |
| `asm_in_ops` | `compiler/parse/back/tgt/c/emit/asm/def/more/operands.id` | Parameters become named input operands, register-allocated. |
| `emit_asm_then_main` | `compiler/parse/back/tgt/c/emit/asm/def/more/order.id` | asm functions are emitted *before* the ordinary definitions, so a call to |
| `asm_error` | `compiler/parse/back/tgt/c/emit/asm/def/more/order.id` | asm functions are emitted *before* the ordinary definitions, so a call to |
| `asm_count` | `compiler/parse/back/tgt/c/emit/asm/select.id` | Platform selection for asm functions, and the diagnostic when a target has |
| `asm_row` | `compiler/parse/back/tgt/c/emit/asm/select.id` | Row of the body for `name` on `triple`, or -1. |
| `asm_match` | `compiler/parse/back/tgt/c/emit/asm/select.id` | Platform selection for asm functions, and the diagnostic when a target has |
| `asm_triples` | `compiler/parse/back/tgt/c/emit/asm/triples.id` | Every triple this name is defined for, for the error message. |
| `asm_tsep` | `compiler/parse/back/tgt/c/emit/asm/triples.id` | Every triple this name is defined for, for the error message. |
| `asm_comma` | `compiler/parse/back/tgt/c/emit/asm/triples.id` | Every triple this name is defined for, for the error message. |
| `hop_dw` | `compiler/parse/back/tgt/c/emit/op/helper/div_name.id` | Which checked division helper a `/` or `%` uses. Four names, two of which |
| `div_pfx` | `compiler/parse/back/tgt/c/emit/op/helper/div_name.id` | Which checked division helper a `/` or `%` uses. Four names, two of which |
| `div_sfx` | `compiler/parse/back/tgt/c/emit/op/helper/div_name.id` | Which checked division helper a `/` or `%` uses. Four names, two of which |
| `emit_op3` | `compiler/parse/back/tgt/c/emit/op/helper/helper_op.id` | Binary operators that emit as a call to a runtime helper rather than as C |
| `emit_helper` | `compiler/parse/back/tgt/c/emit/op/helper/helper_op.id` | Binary operators that emit as a call to a runtime helper rather than as C |
| `helper_op` | `compiler/parse/back/tgt/c/emit/op/helper/helper_op.id` | The C helper for this node, or "" when the operator emits as plain infix. |
| `hop_shift` | `compiler/parse/back/tgt/c/emit/op/helper/shift_div.id` | The shift helpers, and the gate on the division helpers. |
| `hop_sar` | `compiler/parse/back/tgt/c/emit/op/helper/shift_div.id` | `>>` is the arithmetic shift; `ushr` is the logical one. |
| `hop_div` | `compiler/parse/back/tgt/c/emit/op/helper/shift_div.id` | `int` and `word` division both go through the checked helpers; float |
| `ee4` | `compiler/parse/back/tgt/c/emit/op/index/dispatch.id` | Expression emission for the array/index nodes, extending the emit_expr |
| `ee5` | `compiler/parse/back/tgt/c/emit/op/index/dispatch.id` | Expression emission for the array/index nodes, extending the emit_expr |
| `emit_index` | `compiler/parse/back/tgt/c/emit/op/index/get.id` | An index reads a cell and unboxes it to the element type. |
| `index_elem` | `compiler/parse/back/tgt/c/emit/op/index/get.id` | An index reads a cell and unboxes it to the element type. |
| `index_get` | `compiler/parse/back/tgt/c/emit/op/index/get.id` | An index reads a cell and unboxes it to the element type. |
| `ee6` | `compiler/parse/back/tgt/c/emit/op/unary.id` | Unary-expression emission: `(OP operand)`, e.g. `(-1)`. Extends the |
| `emit_un` | `compiler/parse/back/tgt/c/emit/op/unary.id` | Unary-expression emission: `(OP operand)`, e.g. `(-1)`. Extends the |
| `hoist` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist.id` | Hoist locals: id is function-scoped (a var declared in a branch may be used |
| `hoist_list` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist.id` | Hoist locals: id is function-scoped (a var declared in a branch may be used |
| `hoist_one` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist.id` | Hoist locals: id is function-scoped (a var declared in a branch may be used |
| `hoist_loop` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist_loop.id` | Hoist locals declared inside a while body. |
| `hoist_decl_line` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist_loop.id` | The tail of hoist_decl: the C type is derived from the declared type only |
| `hoist_decl` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist_nested.id` | exported decls are C globals (emitted separately), so only non-exported |
| `hoist_nested` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist_nested.id` | Recurse into the bodies of compound statements when hoisting. |
| `hoist_branches` | `compiler/parse/back/tgt/c/emit/prog/code/body/hoist/hoist_nested.id` | Recurse into the bodies of compound statements when hoisting. |
| `emit_block` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/block/block.id` | Emit a block of statements, and the start of the statement dispatch. |
| `emit_stmt` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/block/block.id` | Emit a block of statements, and the start of the statement dispatch. |
| `emit_decl_line` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/block/block.id` | The tail of a decl statement, once its target name and initializer are known. |
| `es2` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/block/es2.id` |  |
| `emit_if_head` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/if/at.id` | The rest of emit_if_at: the opening line, then the body and its close. |
| `emit_if_tail` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/if/at.id` | The rest of emit_if_at: the opening line, then the body and its close. |
| `end_if` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/if/if.id` | Close an if: emit the else arm only when there is one (a non-empty else |
| `else_arm` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/if/if.id` | An else arm holding exactly one if is an `else if`. idc.py emits those by |
| `emit_if_at` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/if/if.id` | The body of an if, with its opening text supplied by the caller so that the |
| `es3` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/stmt.id` | Expression statement, then the if/while split. |
| `es4` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/stmt.id` | Expression statement, then the if/while split. |
| `emit_if` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt/stmt.id` | Expression statement, then the if/while split. |
| `emit_else` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/else_while/else.id` | The else arm. |
| `emit_body_close` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/else_while/else.id` | The closing lines a block shares with a while loop: the body, then the |
| `emit_ret` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/else_while/ret.id` | The return clause. |
| `emit_while` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/else_while/while.id` | The while loop. |
| `emit_while_head` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/else_while/while.id` | The while loop. |
| `emit_iassign` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/iassign.id` | Index-assignment emission: `id_list_set(base, idx, box(value));`. |
| `iassign2` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/iassign.id` | Index-assignment emission: `id_list_set(base, idx, box(value));`. |
| `iassign3` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/iassign.id` | Index-assignment emission: `id_list_set(base, idx, box(value));`. |
| `iassign4` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/tail.id` | The last step of emit_iassign, and the dispatch hook that routes an |
| `iset_code` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/tail.id` | The last step of emit_iassign, and the dispatch hook that routes an |
| `es3b` | `compiler/parse/back/tgt/c/emit/prog/code/body/stmt_more/index_assign/tail.id` | The last step of emit_iassign, and the dispatch hook that routes an |
| `emit_expr` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/expr.id` | Emit an expression as C. int -> digits; var/str -> the text; bin -> infix |
| `ee2` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/expr.id` | Compute the recursive bin form only when the node is a bin -- evaluating it |
| `ee3` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/expr.id` | Emit an expression as C. int -> digits; var/str -> the text; bin -> infix |
| `int_code` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/int_lit.id` | An integer literal emits as the digits the source wrote; one too wide for a |
| `to_str` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/to_str.id` | String-context coercion (idc.py's to_string): the C call that turns a |
| `to_str2` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/to_str.id` | A float in string context becomes id_str_of_float; a word id_str_of_word |
| `to_str3` | `compiler/parse/back/tgt/c/emit/prog/code/expr/atom/to_str.id` | A word prints as %lld and an int as %d, so they need different helpers. |
| `emit_bin` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/bin.id` | Binary expression. Four forms: id_concat for string `+`, a strcmp for |
| `emit_op2` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/bin.id` | Binary expression. Four forms: id_concat for string `+`, a strcmp for |
| `emit_infix` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/bin.id` | Binary expression. Four forms: id_concat for string `+`, a strcmp for |
| `both_str` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/both_str.id` | Both operands are strings? |
| `is_i2_str` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/both_str.id` | The second operand's type, read and compared inside one function so that |
| `infix_line` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/both_str.id` | The tail of emit_infix, once the left operand and the operator are known. |
| `is_strcmp` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/strcmp/eq.id` |  |
| `is_eqop` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/strcmp/eq.id` |  |
| `emit_strcmp` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/strcmp/strcmp.id` | String equality: `a == b` on strings becomes (strcmp(a, b) == 0), matching |
| `strcmp_line` | `compiler/parse/back/tgt/c/emit/prog/code/expr/binop/strcmp/strcmp.id` | String equality: `a == b` on strings becomes (strcmp(a, b) == 0), matching |
| `emit_args` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/args.id` | Comma-separated argument list, the separator helper (shared with params), |
| `sep_arg` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/args.id` | Comma-separated argument list, the separator helper (shared with params), |
| `map_op` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/args.id` | Comma-separated argument list, the separator helper (shared with params), |
| `emit_call` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/call.id` | Call emission: a normal call is id_NAME(args); print coerces its single |
| `emit_print` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/call.id` | Call emission: a normal call is id_NAME(args); print coerces its single |
| `emit_concat` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/concat.id` | String concatenation, and an operand coerced to string context. |
| `arg_str` | `compiler/parse/back/tgt/c/emit/prog/code/expr/call/concat.id` | String concatenation, and an operand coerced to string context. |
| `c_type` | `compiler/parse/back/tgt/c/emit/prog/code/func/ctype/ctype.id` | id type -> C type. Any list type (ending in `]`) is the generic IdList*; |
| `c_scalar` | `compiler/parse/back/tgt/c/emit/prog/code/func/ctype/ctype.id` | A type with no registry row has no spelling either. It answers `int` for |
| `init_tyc` | `compiler/parse/back/tgt/c/emit/prog/code/func/ctype/type_column.id` | The C target's spelling column. |
| `init_tyc_rows` | `compiler/parse/back/tgt/c/emit/prog/code/func/ctype/type_column.id` | The C target's spelling column. |
| `init_tyc_rows2` | `compiler/parse/back/tgt/c/emit/prog/code/func/ctype/type_column.id` | `string` is a char*, and `void` is C's own void. `float` is a double: id's |
| `emit_def` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/def/def.id` | A function definition: signature, hoisted locals, body, return. |
| `emit_def_body` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/def/def.id` | A function definition: signature, hoisted locals, body, return. |
| `emit_tail` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/def/def.id` | A function definition: signature, hoisted locals, body, return. |
| `emit_tail_end` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/def/tail.id` |  |
| `sig` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/def/tail.id` | `c_rettype id_NAME(params)` |
| `fwd_all` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/loops.id` | Forward-declaration and definition loops over the program's functions. |
| `def_all` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/loops.id` | Forward-declaration and definition loops over the program's functions. |
| `emit_def_nl` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/loops.id` | each definition is followed by a blank line (idc.py joins bodies that way) |
| `param_str` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/params.id` | Parameter list for a signature: "void" if none, else "c_type name, ...". |
| `pstr` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/params.id` | Parameter list for a signature: "void" if none, else "c_type name, ...". |
| `list_params` | `compiler/parse/back/tgt/c/emit/prog/code/func/def/params.id` | Parameter list for a signature: "void" if none, else "c_type name, ...". |
| `main_at` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/main/main_at.id` | Helper for find_main: keep the func id whose name is "main". |
| `main_body` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/main/main_body.id` | The call body inside the wrapper: `return id_main(...);` for an int return, |
| `main_call` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/main/main_body.id` | The call body inside the wrapper: `return id_main(...);` for an int return, |
| `find_main` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/main/main_body.id` | the func node named "main", or -1 |
| `wrap_body_tail` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/main/wrap_tail.id` | The tail of wrap_body, once main's args are already marshalled. |
| `emit_marshal` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/marshal.id` | argv marshalling (only when main takes parameters), matching idc.py. |
| `print_marshal` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/marshal.id` | argv marshalling (only when main takes parameters), matching idc.py. |
| `has_args` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/marshal.id` | argv marshalling (only when main takes parameters), matching idc.py. |
| `emit_main_wrapper` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/wrapper.id` | A project with no main compiles to a .o and gets no entry point -- but |
| `emit_wrapper` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/wrapper.id` | The real C entry point wrapping id's main(). Matches idc.py: marshals C argv |
| `wrap_body` | `compiler/parse/back/tgt/c/emit/prog/code/func/entry/wrapper.id` | The real C entry point wrapping id's main(). Matches idc.py: marshals C argv |
| `const_decl` | `compiler/parse/back/tgt/c/emit/prog/head/const/find.id` | The declaration node a constant's name came from. Looked up by name rather |
| `const_pick` | `compiler/parse/back/tgt/c/emit/prog/head/const/find.id` | The declaration node a constant's name came from. Looked up by name rather |
| `export_line` | `compiler/parse/back/tgt/c/emit/prog/head/const/line.id` | One line of the exported-variable block. |
| `const_line` | `compiler/parse/back/tgt/c/emit/prog/head/const/line.id` | One line of the exported-variable block. |
| `const_line_tail` | `compiler/parse/back/tgt/c/emit/prog/head/const/line.id` | One line of the exported-variable block. |
| `emit_exports` | `compiler/parse/back/tgt/c/emit/prog/head/decl/exports.id` | Emit the `/* exported variables */` block (only when there are exports), |
| `emit_export_block` | `compiler/parse/back/tgt/c/emit/prog/head/decl/exports.id` | Emit the `/* exported variables */` block (only when there are exports), |
| `emit_export_lines` | `compiler/parse/back/tgt/c/emit/prog/head/decl/exports.id` | Emit the `/* exported variables */` block (only when there are exports), |
| `emit_externs` | `compiler/parse/back/tgt/c/emit/prog/head/decl/externs.id` | The `/* functions not defined in any input file */` block, and the one |
| `emit_extern_block` | `compiler/parse/back/tgt/c/emit/prog/head/decl/externs.id` | The `/* functions not defined in any input file */` block, and the one |
| `emit_extern_lines` | `compiler/parse/back/tgt/c/emit/prog/head/decl/externs.id` | The `/* functions not defined in any input file */` block, and the one |
| `emit_fwds` | `compiler/parse/back/tgt/c/emit/prog/head/decl/fwd.id` | The forward-declaration section header and the blank line after it. |
| `emit_program` | `compiler/parse/back/tgt/c/emit/prog/head/program.id` | C emitter (stage 3). Walks the AST and prints C, mirroring idc.py: |
| `emit_head` | `compiler/parse/back/tgt/c/emit/prog/head/program.id` | runtime prelude, a blank line, then the declarations section, matching |
| `emit_decls` | `compiler/parse/back/tgt/c/emit/prog/head/program.id` | the link-time externs (if any), the forward declarations, then the |
| `arr_cells` | `compiler/parse/back/tgt/c/out/builtin/arr/arr_cells.id` | The boxed, comma-separated cell list for an array literal. |
| `emit_arr` | `compiler/parse/back/tgt/c/out/builtin/arr/arr_lit.id` | Array-literal emission: `id_list_lit(0)` for an empty list, otherwise |
| `emit_arr_n` | `compiler/parse/back/tgt/c/out/builtin/arr/arr_lit.id` | Array-literal emission: `id_list_lit(0)` for an empty list, otherwise |
| `arr_lit` | `compiler/parse/back/tgt/c/out/builtin/arr/arr_lit.id` | Array-literal emission: `id_list_lit(0)` for an empty list, otherwise |
| `ec2` | `compiler/parse/back/tgt/c/out/builtin/call/dispatch/dispatch.id` | Call emission dispatch for the list builtins, extending emit_call: |
| `ec3` | `compiler/parse/back/tgt/c/out/builtin/call/dispatch/dispatch.id` | Call emission dispatch for the list builtins, extending emit_call: |
| `emit_gcall` | `compiler/parse/back/tgt/c/out/builtin/call/dispatch/dispatch.id` | Call emission dispatch for the list builtins, extending emit_call: |
| `emit_gcall_tail` | `compiler/parse/back/tgt/c/out/builtin/call/dispatch/tail.id` | The tail of emit_gcall: the generic id_NAME(args) form, overridden with a |
| `emit_len` | `compiler/parse/back/tgt/c/out/builtin/call/len_push/len_push.id` | len picks id_len for a string and id_list_len for a list; push boxes its |
| `emit_push` | `compiler/parse/back/tgt/c/out/builtin/call/len_push/len_push.id` | len picks id_len for a string and id_list_len for a list; push boxes its |
| `emit_push_tail` | `compiler/parse/back/tgt/c/out/builtin/call/len_push/tail.id` |  |
| `push_code` | `compiler/parse/back/tgt/c/out/builtin/call/len_push/tail.id` |  |
| `ec4` | `compiler/parse/back/tgt/c/out/builtin/call/pop.id` | pop(xs) -- removes and returns the last cell, so like every list read it |
| `emit_pop` | `compiler/parse/back/tgt/c/out/builtin/call/pop.id` | pop(xs) -- removes and returns the last cell, so like every list read it |
| `emit_pop_tail` | `compiler/parse/back/tgt/c/out/builtin/call/pop.id` | pop(xs) -- removes and returns the last cell, so like every list read it |
| `sys_helper` | `compiler/parse/back/tgt/c/out/builtin/syscall/helper_name.id` | C helper name for a systems builtin: `id_` + the builtin's name, except for |
| `sys_helper2` | `compiler/parse/back/tgt/c/out/builtin/syscall/helper_name.id` | C helper name for a systems builtin: `id_` + the builtin's name, except for |
| `emit_syscall` | `compiler/parse/back/tgt/c/out/builtin/syscall/syscall.id` | The flat-store and unsigned-word builtins. They all take machine words, so |
| `cast_args` | `compiler/parse/back/tgt/c/out/builtin/syscall/syscall.id` | The flat-store and unsigned-word builtins. They all take machine words, so |
| `is_syscall` | `compiler/parse/back/tgt/c/out/builtin/syscall/syscall.id` | The flat-store and unsigned-word builtins. They all take machine words, so |
| `box` | `compiler/parse/back/tgt/c/out/cell/box.id` | A `word` boxes exactly like an `int` -- the cast is what widens a narrower |
| `box2` | `compiler/parse/back/tgt/c/out/cell/box.id` | Boxing a value into a uniform list cell (a long long), mirroring idc.py's |
| `elem_type` | `compiler/parse/back/tgt/c/out/cell/box.id` | Boxing a value into a uniform list cell (a long long), mirroring idc.py's |
| `unbox` | `compiler/parse/back/tgt/c/out/cell/unbox.id` | Reading a list cell back as its element type, mirroring idc.py's unbox(): |
| `unbox2` | `compiler/parse/back/tgt/c/out/cell/unbox.id` | Reading a list cell back as its element type, mirroring idc.py's unbox(): |
| `unbox3` | `compiler/parse/back/tgt/c/out/cell/unbox.id` | Reading a list cell back as its element type, mirroring idc.py's unbox(): |
| `unbox4` | `compiler/parse/back/tgt/c/out/cell/unbox_more.id` | The last unbox case: a `word` cell is already 8 bytes wide, so reading it |
| `emit_crt` | `compiler/parse/back/tgt/c/runtime/extern.gen.id` | GENERATED by tools/gen_runtime_id.py from idc.py's RUNTIME -- do |
| `resv_names` | `compiler/parse/back/tgt/c/runtime/reserved.gen.id` | The list is built into a local rather than returned directly: |
| `emit_runtime` | `compiler/parse/back/tgt/c/runtime/runtime.id` | GENERATED by tools/gen_runtime_id.py from idc.py's RUNTIME -- do |
| `ll_asm_all` | `compiler/parse/back/tgt/ll/fn/asm/def/asm.id` | `asm` functions, as LLVM inline assembly. |
| `ll_asm_row` | `compiler/parse/back/tgt/ll/fn/asm/def/asm.id` | `asm` functions, as LLVM inline assembly. |
| `ll_asm_one` | `compiler/parse/back/tgt/ll/fn/asm/def/asm.id` | noinline, and it matters: the block clobbers whatever registers its |
| `ll_asm_par` | `compiler/parse/back/tgt/ll/fn/asm/def/body.id` |  |
| `ll_asm_body` | `compiler/parse/back/tgt/ll/fn/asm/def/body.id` | Volatile with a memory clobber on every block, as in the C target: a wrong |
| `ll_asm_cons` | `compiler/parse/back/tgt/ll/fn/asm/def/body.id` | One `=r` for the result, one `r` per parameter, then the memory clobber. |
| `ll_asm_one_out` | `compiler/parse/back/tgt/ll/fn/asm/def/more.id` | The rest of `ll_asm_one`: the define line and its close, around the body. |
| `ll_asm_body2` | `compiler/parse/back/tgt/ll/fn/asm/def/more.id` | The rest of `ll_asm_body`: the text and constraint string, then the two |
| `ll_asm_body3` | `compiler/parse/back/tgt/ll/fn/asm/def/more.id` | The rest of `ll_asm_one`: the define line and its close, around the body. |
| `ll_asm_ix` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/ix.id` | Which numbered operand a name is. The result is operand 0 -- LLVM counts |
| `ll_asm_pix` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/ix.id` | Which numbered operand a name is. The result is operand 0 -- LLVM counts |
| `ll_asm_pm` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/ix.id` | Which numbered operand a name is. The result is operand 0 -- LLVM counts |
| `ll_asm_tok_sel` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/sel.id` | The rest of `ll_asm_tok`: which of the three token shapes this one is. |
| `ll_asm_ref2_nm` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/sel.id` | Where the operand name in `%[name]` (or `%x[name]`) runs, so `ll_asm_ref2` |
| `ll_asm_tok` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/tok.id` | `$` is literal in GCC's asm and special in LLVM's, and `%` is the other way |
| `ll_asm_ref` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/tok.id` | `$` is literal in GCC's asm and special in LLVM's, and `%` is the other way |
| `ll_asm_ref2` | `compiler/parse/back/tgt/ll/fn/asm/text/ref/tok.id` | `%[name]` is operand N; `%x[name]` is operand N under LLVM's modifier x, |
| `ll_asm_step` | `compiler/parse/back/tgt/ll/fn/asm/text/scan.id` | How far a token reaches. An operand reference runs to its closing bracket; |
| `ll_asm_end` | `compiler/parse/back/tgt/ll/fn/asm/text/scan.id` | How far a token reaches. An operand reference runs to its closing bracket; |
| `ll_asm_nm` | `compiler/parse/back/tgt/ll/fn/asm/text/scan.id` | Where the operand name starts: past `%[`, or past `%x[` when a modifier |
| `ll_asm_text` | `compiler/parse/back/tgt/ll/fn/asm/text/text.id` | The instruction lines, joined with a newline and a tab, translated from the |
| `ll_asm_sep` | `compiler/parse/back/tgt/ll/fn/asm/text/text.id` | The instruction lines, joined with a newline and a tab, translated from the |
| `ll_asm_line` | `compiler/parse/back/tgt/ll/fn/asm/text/text.id` | A line arrives with its quotes, so the scan runs between them. |
| `ll_blocks` | `compiler/parse/back/tgt/ll/fn/body/blk.id` |  |
| `ll_insns` | `compiler/parse/back/tgt/ll/fn/body/blk.id` |  |
| `ll_insn` | `compiler/parse/back/tgt/ll/fn/body/blk.id` | A parameter is spelled in the signature and a nop is what a pass left |
| `ll_pstr` | `compiler/parse/back/tgt/ll/fn/body/entry/entry.id` | The parameter list of a define: `<ty> %v<id>`, in order, which is also how |
| `ll_sep` | `compiler/parse/back/tgt/ll/fn/body/entry/entry.id` | The parameter list of a define: `<ty> %v<id>`, in order, which is also how |
| `ll_block` | `compiler/parse/back/tgt/ll/fn/body/entry/entry.id` | The parameter list of a define: `<ty> %v<id>`, in order, which is also how |
| `ll_pcount` | `compiler/parse/back/tgt/ll/fn/body/entry/more.id` | How many parameters a function has, named for the same reason as |
| `ll_pstr_step` | `compiler/parse/back/tgt/ll/fn/body/entry/more.id` | The rest of `ll_pstr`: one parameter's text. |
| `ll_wrap_end` | `compiler/parse/back/tgt/ll/fn/body/main/end.id` | The rest of `ll_wrap_tail`: the two lines that close the block, after the |
| `ll_main` | `compiler/parse/back/tgt/ll/fn/body/main/find.id` |  |
| `ll_main_at` | `compiler/parse/back/tgt/ll/fn/body/main/find.id` |  |
| `ll_mcall` | `compiler/parse/back/tgt/ll/fn/body/main/find.id` | A main taking no arguments is called with none; the marshalled list is |
| `ll_entry` | `compiler/parse/back/tgt/ll/fn/body/main/wrap.id` | Freestanding has no host to be called from: the boot code jumps straight to |
| `ll_wrapper` | `compiler/parse/back/tgt/ll/fn/body/main/wrap.id` | The C-ABI entry point: marshal argv into an `id` string list and call |
| `ll_wrap_tail` | `compiler/parse/back/tgt/ll/fn/body/main/wrap.id` | The C-ABI entry point: marshal argv into an `id` string list and call |
| `ll_funcs` | `compiler/parse/back/tgt/ll/fn/fn/fn.id` |  |
| `ll_ends` | `compiler/parse/back/tgt/ll/fn/fn/fn.id` |  |
| `ll_func` | `compiler/parse/back/tgt/ll/fn/fn/fn.id` |  |
| `ll_func_out` | `compiler/parse/back/tgt/ll/fn/fn/more.id` | The rest of `ll_func`: the name and parameter string, then the define. |
| `ll_func_emit` | `compiler/parse/back/tgt/ll/fn/fn/more.id` | The rest of `ll_func`: the name and parameter string, then the define. |
| `ll_head` | `compiler/parse/back/tgt/ll/mod/head.id` | The module preamble, and the three sections that make up its body. |
| `ll_head_triple` | `compiler/parse/back/tgt/ll/mod/head.id` | The module preamble, and the three sections that make up its body. |
| `ll_body` | `compiler/parse/back/tgt/ll/mod/head.id` | The module preamble, and the three sections that make up its body. |
| `emit_llvm` | `compiler/parse/back/tgt/ll/mod/module.id` | The LLVM IR target: a printer over the IR, not a second walk of the AST. |
| `ll_emit` | `compiler/parse/back/tgt/ll/mod/module.id` | The LLVM IR target: a printer over the IR, not a second walk of the AST. |
| `ll_declares` | `compiler/parse/back/tgt/ll/mod/sect/decl.id` | The runtime symbols the module calls. They are declarations, never |
| `ll_decl_block` | `compiler/parse/back/tgt/ll/mod/sect/decl.id` | One line is not in the block above, because it is the only symbol whose |
| `ll_decl_cmp` | `compiler/parse/back/tgt/ll/mod/sect/decl.id` | The runtime symbols the module calls. They are declarations, never |
| `ll_globals` | `compiler/parse/back/tgt/ll/mod/sect/glob.id` | An exported variable is a module-level global. A constant from conf.id |
| `ll_global_at` | `compiler/parse/back/tgt/ll/mod/sect/glob.id` | An exported variable is a module-level global. A constant from conf.id |
| `ll_init` | `compiler/parse/back/tgt/ll/mod/sect/glob.id` | An exported variable is a module-level global. A constant from conf.id |
| `ll_str_emit` | `compiler/parse/back/tgt/ll/mod/sect/str/more.id` | The rest of `ll_str_at`: the length and bytes, then the constant line. |
| `ll_str_emit2` | `compiler/parse/back/tgt/ll/mod/sect/str/more.id` | The rest of `ll_str_at`: the length and bytes, then the constant line. |
| `ll_zero` | `compiler/parse/back/tgt/ll/mod/sect/str/str.id` | A zero of a type, for a global with no initialiser. |
| `ll_strs` | `compiler/parse/back/tgt/ll/mod/sect/str/str.id` | String literals become private globals, emitted after the functions that |
| `ll_str_at` | `compiler/parse/back/tgt/ll/mod/sect/str/str.id` | A zero of a type, for a global with no initialiser. |
| `ll_text` | `compiler/parse/back/tgt/ll/val/insn/insn.id` | One instruction, as LLVM text. A dispatch chain in the order the shapes |
| `ll_ret` | `compiler/parse/back/tgt/ll/val/insn/insn.id` | One instruction, as LLVM text. A dispatch chain in the order the shapes |
| `ll_t2` | `compiler/parse/back/tgt/ll/val/insn/insn.id` | One instruction, as LLVM text. A dispatch chain in the order the shapes |
| `ll_t3` | `compiler/parse/back/tgt/ll/val/insn/mem.id` |  |
| `ll_t4` | `compiler/parse/back/tgt/ll/val/insn/mem.id` |  |
| `ll_call` | `compiler/parse/back/tgt/ll/val/insn/mem.id` | A call that produces nothing is a statement; one that produces a value names |
| `ll_t5_isc` | `compiler/parse/back/tgt/ll/val/insn/ops/build.id` | The rest of `ll_t5` and `ll_t6`: whether an opcode is a cast, and the two |
| `ll_t5_cmp` | `compiler/parse/back/tgt/ll/val/insn/ops/build.id` | The rest of `ll_t5` and `ll_t6`: whether an opcode is a cast, and the two |
| `ll_t6_bin` | `compiler/parse/back/tgt/ll/val/insn/ops/build.id` | The rest of `ll_t5` and `ll_t6`: whether an opcode is a cast, and the two |
| `ll_iscast` | `compiler/parse/back/tgt/ll/val/insn/ops/more/cast.id` |  |
| `ll_negop` | `compiler/parse/back/tgt/ll/val/insn/ops/more/cast.id` |  |
| `ll_phis` | `compiler/parse/back/tgt/ll/val/insn/ops/more/cast.id` |  |
| `ll_pred` | `compiler/parse/back/tgt/ll/val/insn/ops/more/pred.id` | A comparison carries the `id` operator, and the operand type decides how |
| `ll_ipred` | `compiler/parse/back/tgt/ll/val/insn/ops/more/pred.id` | A comparison carries the `id` operator, and the operand type decides how |
| `ll_fpred` | `compiler/parse/back/tgt/ll/val/insn/ops/more/pred.id` | A comparison carries the `id` operator, and the operand type decides how |
| `ll_rel` | `compiler/parse/back/tgt/ll/val/insn/ops/more/rel.id` | The relational half of a predicate: `lt`, `gt`, `le`, `ge`. Signedness and |
| `ll_rel2` | `compiler/parse/back/tgt/ll/val/insn/ops/more/rel.id` | The relational half of a predicate: `lt`, `gt`, `le`, `ge`. Signedness and |
| `ll_t5` | `compiler/parse/back/tgt/ll/val/insn/ops/op.id` |  |
| `ll_t6` | `compiler/parse/back/tgt/ll/val/insn/ops/op.id` | `neg` and `not` have no LLVM opcode of their own: negation is a subtraction |
| `ll_bytes_acc` | `compiler/parse/back/tgt/ll/val/str/bytes/acc.id` | The rest of `ll_bytes`: the accumulating loop, kept apart because a |
| `ll_bytes` | `compiler/parse/back/tgt/ll/val/str/bytes/bytes.id` | A string literal, as LLVM wants it: every byte written as an escape, so no |
| `ll_piece` | `compiler/parse/back/tgt/ll/val/str/bytes/bytes.id` | A string literal, as LLVM wants it: every byte written as an escape, so no |
| `ll_step` | `compiler/parse/back/tgt/ll/val/str/bytes/bytes.id` | A backslash escape spells one byte out of two source characters. |
| `ll_escbyte` | `compiler/parse/back/tgt/ll/val/str/esc.id` | The byte a backslash escape denotes. |
| `ll_escbyte2` | `compiler/parse/back/tgt/ll/val/str/esc.id` | The byte a backslash escape denotes. |
| `ll_blen` | `compiler/parse/back/tgt/ll/val/str/esc.id` | The count of bytes the constant holds, which is what the array type needs. |
| `ll_hex` | `compiler/parse/back/tgt/ll/val/str/hex.id` | One byte as `\XX`. Uppercase, two digits, always -- LLVM accepts a literal |
| `ll_nib` | `compiler/parse/back/tgt/ll/val/str/hex.id` | One byte as `\XX`. Uppercase, two digits, always -- LLVM accepts a literal |
| `ll_isstr` | `compiler/parse/back/tgt/ll/val/str/hex.id` | A string constant needs a private global to point at. A value of string type |
| `ll_lit` | `compiler/parse/back/tgt/ll/val/ty/lit.id` |  |
| `ll_op` | `compiler/parse/back/tgt/ll/val/ty/lit.id` | A typed operand: `<ty> <value>`, the form LLVM wants nearly everywhere. |
| `ll_args` | `compiler/parse/back/tgt/ll/val/ty/lit.id` |  |
| `ll_ty` | `compiler/parse/back/tgt/ll/val/ty/ty.id` | An `id` type in LLVM's spelling. Every reference type -- a string, a list of |
| `ll_ty2` | `compiler/parse/back/tgt/ll/val/ty/ty.id` | An `id` type in LLVM's spelling. Every reference type -- a string, a list of |
| `ll_ty3` | `compiler/parse/back/tgt/ll/val/ty/ty.id` | An `id` type in LLVM's spelling. Every reference type -- a string, a list of |
| `ll_val` | `compiler/parse/back/tgt/ll/val/ty/val.id` | How a value is written where it is used. A constant prints as its literal |
| `ll_val2` | `compiler/parse/back/tgt/ll/val/ty/val.id` | How a value is written where it is used. A constant prints as its literal |
| `ll_vty` | `compiler/parse/back/tgt/ll/val/ty/val.id` | The type of a value, safe on an absent operand for the same reason. |
