// String concatenation, and an operand coerced to string context.

emit_concat(int id) {
  string s = "id_concat(" + arg_str(i1_of(id)) + ", " + arg_str(i2_of(id)) + ")";
} return string s;

arg_str(int id) {
} return string to_str(emit_expr(id), type_of(id));
