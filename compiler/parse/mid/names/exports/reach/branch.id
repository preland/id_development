// The else side of an if, split from exp_branches so that block stays
// within the action limit.
exp_else(int id, string owner) {
  int[] l2_of_v = l2_of(id);
  exp_list(l2_of_v, owner);
} return void;
