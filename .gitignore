# build artifacts
*.o
*.gen.c
*.pyc
__pycache__/

# VS Code extension build
editors/vscode-id/node_modules/
*.vsix

# compiled program binaries (named after the file or project; no extension).
# Builds land in build/ now, so this list only covers binaries left over from
# before that change and from an explicit -o.
/adv
/calc
/engine
/moonbuggy
/solitaire
/hello
/adventure
/idlex
/idparse
/idcalc
/gfxdemo
/flyover
/fpsmaze
/galaxy
/id
/wl

# autonomous-run build outputs
/out/
build/
*.ll
*.wat
*.wasm

# self-hosted driver bootstrap cache
/.idc-cache/

# direnv
/.direnv/

# Pending extraction into their own repositories (see docs/GAPS.md).
# Committed by accident in 627f793 and untracked again immediately after.
/flappy/
/nativeapp/
/webdemo/
