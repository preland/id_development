# direnv: drop into the flake dev shell automatically on `cd` into this repo.
# Requires `direnv` + nix flakes (both present on the dev machine). Run
# `direnv allow` once to enable. This makes graphics/LLVM/WASM builds "just
# work" — the same toolchain as `nix develop` and `tools/devshell.sh`.
use flake
