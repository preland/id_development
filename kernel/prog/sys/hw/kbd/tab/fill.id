// The high table, cut out here because kb_fill already spent its own budget
// filling the low one.
kb_fill_hi() {
  string kb_high_v = kb_high();
  kb_hex((import kb_hi), kb_high_v, 0);
} return void;
