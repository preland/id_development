// The version read, cut out here because vbe_ok already spent its own budget
// selecting the register before it can be read.
vbe_read_ok() {
  word in16_v = in16(0x1cf);
  int ok = vbe_ok2(in16_v);
} return int ok;
