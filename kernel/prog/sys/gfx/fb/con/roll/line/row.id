// The row advance half of con_nl, cut out here because con_nl already spent
// its own budget zeroing the column.
con_nl_row() {
  word con_at_v = con_at(1);
  wset((import con_st), 1, con_at_v + 1);
  con_roll();
} return void;
