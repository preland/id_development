// The name at `at` runs to the next bar, cut out here because trap_name
// already spent its own budget finding where it starts.
trap_name_cut(string trap_names_v, int at) {
  int bar_at_v = bar_at(trap_names_v, at);
  string ret_s = cut(trap_names_v, at, bar_at_v);
} return string ret_s;
