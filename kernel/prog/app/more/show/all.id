// The order the demonstrations run in. Split across three functions only
// because a block performs three actions, which is also why the runtime below
// looks the way it does.

show_all() {
  show_arith();
  show_div();
  show_rest();
} return void;

show_rest() {
  show_shift();
  show_str();
  show_more();
} return void;

show_more() {
  show_list();
  show_store();
} return void;
