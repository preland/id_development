// The read half of one turn, cut out here because sh_once already spent its
// own budget writing the prompt.
sh_once_go(string t) {
  string sh_line_v = sh_line();
  sh_go(t, sh_line_v);
} return void;
