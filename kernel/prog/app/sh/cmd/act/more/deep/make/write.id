// The text half of `write` is cut out here rather than in sh_write, which
// already spent its own budget getting the name half named.
sh_write_body(string t, int at, string cut_v) {
  string cut_v2 = sh_tail(t, at);
  fs_put(cut_v, cut_v2);
} return void;
