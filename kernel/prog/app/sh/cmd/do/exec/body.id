// The argument-text half of sh_exec, cut out here because sh_exec already
// spent its own budget getting the command name named.
sh_exec_body(string s, int at, string cut_v) {
  string cut_v2 = sh_tail(s, at);
  sh_pick(cut_v, cut_v2);
} return void;
