// The final path segment, the same cut `fs_make` and `fs_make2` both need to
// name the node they are creating -- one function rather than two that agree.
fs_last_seg(string t) {
  int fs_last_v = fs_last(t);
  int len_v = len(t);
  string ret_s = cut(t, fs_last_v, len_v);
} return string ret_s;
