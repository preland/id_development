tt_ends_loop(int at, int b, int n) {
  int i = 0;
  while(i < n) {
    i = tt_end1(at, b, i);
  }
} return void;

tt_ends_after(int at, int n) {
  int ret_i = at + n * 2 + 2 + rd_be16((import ttbuf), at + n * 2);
} return int ret_i;
