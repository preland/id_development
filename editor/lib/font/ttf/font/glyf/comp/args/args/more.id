tt_args_flags(int[] row) {
  int rd_be16_v = rd_be16((import ttbuf), row[0] + 2);
  lset(row, 2, rd_be16_v);
  lset(row, 0, row[0] + 4);
} return void;

tt_args3a(int[] row, int k, int n) {
  int tt_sarg_v = tt_sarg(row[0], n, k);
  lset(row, 3, tt_sarg_v);
} return void;

tt_args3b(int[] row, int k, int n) {
  int tt_sarg_v2 = tt_sarg(row[0] + n, n, k);
  lset(row, 4, tt_sarg_v2);
} return void;
