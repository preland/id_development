tt_shift(int b, int x, int y) {
  int i = b;
  while(i < len((import ttpx))) {
    i = tt_shift1(i, x, y);
  }
} return void;

tt_shift1(int i, int x, int y) {
  lset((import ttpx), i, (import ttpx)[i] + x);
  lset((import ttpy), i, (import ttpy)[i] + y);
} return int i + 1;
