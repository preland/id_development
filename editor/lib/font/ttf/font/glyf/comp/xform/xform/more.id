tt_xform1_go(int[] row, int i) {
  int x = (import ttpx)[i];
  int y = (import ttpy)[i];
  tt_xput(row, i, x, y);
} return void;

tt_xput_x(int[] row, int i, int x, int y) {
  int tt_fix_v = tt_fix(row[5] * x + row[7] * y);
  lset((import ttpx), i, tt_fix_v);
} return void;

tt_xput_y(int[] row, int i, int x, int y) {
  int tt_fix_v2 = tt_fix(row[6] * x + row[8] * y);
  lset((import ttpy), i, tt_fix_v2);
} return void;
