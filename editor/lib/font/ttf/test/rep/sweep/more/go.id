tt_swpg_ends(int[] tab) {
  int len_v = len((import ttends));
  lset(tab, 1, tab[1] + len_v);
} return void;

tt_swpg_pts(int[] tab) {
  int len_v2 = len((import ttpx));
  lset(tab, 2, tab[2] + len_v2);
} return void;

tt_swp2_go(int[] tab, int i) {
  lset(tab, 0, (tab[0] * 31 + (import ttpx)[i] + 1000003) % 1000003);
  lset(tab, 0, (tab[0] * 31 + (import ttpy)[i] + 1000003) % 1000003);
  lset(tab, 0, (tab[0] * 31 + (import ttpon)[i] + 1000003) % 1000003);
} return void;
