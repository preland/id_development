// The version tag at the very start of the file, and whether it is one of
// the two this parser accepts.
tt_begin_check(int[] xs) {
  word rd_be32_v = rd_be32(xs, 0);
  int ok = tt_check(rd_be32_v);
} return int ok;
