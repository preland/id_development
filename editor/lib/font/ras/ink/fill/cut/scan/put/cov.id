// Column x of row r's accumulator, scaled into the coverage bitmap.
ras_put1_cov(int r, int x, int[] row) {
  int ras_w_v = ras_w();
  lset((import rascov), r * ras_w_v + x, row[x] * 255 / 256);
} return void;
