ras_ctr_open(int[] buf, int k) {
  int ras_lo_v = ras_lo(k);
  ras_open(buf, ras_lo_v, (import ttends)[k]);
} return void;

ras_ctr_run(int[] buf, int k) {
  ras_run(buf);
  int ret_i = k + 1;
} return int ret_i;
