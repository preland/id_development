// One character: an entity reference, or a byte that stands for itself.
xml_dec4(int[] row, int hi, word a) {
  int c = xml_ch(row[0]);
  if(c == 38) {
    xml_ent(row, hi, a);
  } else {
    xml_put(row, a, c);
  }
} return void;
