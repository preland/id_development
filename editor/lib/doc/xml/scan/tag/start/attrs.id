// The rest of xml_tag3, once the name is already cut and stored: the
// attribute list is emptied before it is read into, in that order.
xml_tag4(int j) {
  xml_clear();
  int ret_i = xml_attrs(j);
} return int ret_i;
