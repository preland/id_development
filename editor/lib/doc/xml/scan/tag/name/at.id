// Whether the byte at a position is a name byte, without naming that byte
// itself: a `while` reads this once per iteration, and a name for the byte
// would be stale after the first one moves past it.
xml_isname_at(int i) {
  int c = xml_ch(i);
  int ok = xml_isname(c);
} return int ok;
