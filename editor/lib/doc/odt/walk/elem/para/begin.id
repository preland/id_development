// The other half of odt_begin: the row is only pushed after both ends are
// known, and marking the state open comes last, after both writes have
// happened.
odt_begin2() {
  int len_v2 = len((import odt_txts));
  push((import odt_phi), len_v2);
  lset((import odt_st), 0, 1);
} return void;
