// The three properties odt_props2 folds into row i, one statement group each.

odt_props_font(int i) {
  string xml_attr_v = xml_attr("style:font-name");
  string odt_or_v = odt_or(xml_attr_v, (import odt_fonts)[i]);
  sset((import odt_fonts), i, odt_or_v);
} return void;

odt_props_size(int i) {
  string xml_attr_v2 = xml_attr("fo:font-size");
  int odt_pt_v = odt_pt(xml_attr_v2, (import odt_nums)[i]);
  lset((import odt_nums), i, odt_pt_v);
} return void;

odt_props_flags(int i) {
  int odt_wt_v = odt_wt();
  lset((import odt_flags), i, (import odt_flags)[i] | odt_wt_v);
} return void;
