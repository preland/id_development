// Every ZIP record starts with a four-byte signature, and checking it is what
// turns a wrong offset into a message instead of a plausible-looking entry
// made of somebody's document text.
zip_sig(int n, int k) {
  if (n != k) {
    inf_fail("zip: bad record signature");
  }
} return void;
