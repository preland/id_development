// The laid-out page, drawn.
ed_draw(int w, int h) {
  ed_page(w, h);
  ed_all(0);
} return void;

ed_all(int i) {
  while(i < len((import lay_g))) {
    ed_glyph(i);
    i = i + 1;
  }
} return void;
