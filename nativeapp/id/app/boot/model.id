// Model startup: create the (empty) todo lists and reset the typed field and the
// selection cursor.

init_model() {
  init();
  reset_ui();
} return void;

reset_ui() {
  buf_reset();
  sel_set(0);
} return void;
