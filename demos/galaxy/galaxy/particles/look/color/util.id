// clamp255 keeps a wobbled channel in 0..255. The pack into one 0xRRGGBB int
// is idstd's d2_pack, which gfxdemo, idml and id_nativeapp also carried.

clamp255(int v) {
  int r = v;
  if (v < 0) { r = 0; }
  else if (v > 255) { r = 255; }
} return int r;
