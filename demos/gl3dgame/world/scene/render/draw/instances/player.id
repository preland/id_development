// The player is always drawn at the view's own origin (0,0,0) -- the chase
// camera follows it, so it never needs its own translate.

draw_player(int view) {
  gl_set_modelview(view);
  gl_draw_tris((import player_verts), (import player_colors), 12);
} return void;
