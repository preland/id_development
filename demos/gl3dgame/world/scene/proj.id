// render/frame.id's build_proj matrix has exactly one reader, the native
// projection slot -- folding the handoff in here keeps render() from
// needing a name for a value nothing else ever looks at.
apply_proj() {
  int proj = build_proj();
  gl_set_projection(proj);
} return void;
