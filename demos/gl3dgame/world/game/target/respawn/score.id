// score_and_respawn's before-and-after: bump the counter, then report it,
// split out so score_and_respawn itself stays under the action limit.

bump_score() {
  int gs_get_v = gs_get(4);
  gs_set(4, gs_get_v + 1);
} return void;

report_score() {
  int gs_get_v2 = gs_get(4);
  print("score: " + gs_get_v2);
} return void;
