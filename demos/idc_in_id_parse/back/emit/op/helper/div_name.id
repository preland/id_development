// Which checked division helper a word-typed `/` or `%` uses.

hop_dw(string op) {
  string h = hop_mod(op);
  if(op == "/") {
    h = "id_sdiv";
  }
} return string h;

hop_mod(string op) {
  string h = "";
  if(op == "%") {
    h = "id_smod";
  }
} return string h;
