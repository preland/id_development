// The else arm, while loops, and the return clause.

emit_else(int id, string ind) {
  print(ind + "} else {");
  emit_block(l2_of(id), ind + "    ");
  print(ind + "}");
} return void;

emit_while(int id, string ind) {
  print(ind + "while (" + emit_expr(i1_of(id)) + ") {");
  emit_block(l1_of(id), ind + "    ");
  print(ind + "}");
} return void;

emit_ret(int id) {
  if(i1_of(id) >= 0) {
    print("    return " + emit_expr(i1_of(id)) + ";");
  } else {
    print("    return;");
  }
} return void;
