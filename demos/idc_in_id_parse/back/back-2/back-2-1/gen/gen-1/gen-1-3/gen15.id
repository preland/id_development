// argv marshalling (only when main takes parameters), matching idc.py.

emit_marshal(int m) {
  if(has_args(m)) {
    print_marshal();
  }
} return void;

print_marshal() {
  print("    IdList* id_args = id_list_new();");
  print("    for (int i = 0; i < argc; i++)");
  print("        id_list_push(id_args, (long long)(intptr_t)argv[i]);");
} return void;

has_args(int m) {
  int ok = 0;
  if(len(l1_of(m)) > 0) {
    ok = 1;
  }
} return int ok;
