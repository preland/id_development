// The real C entry point, wrapping id's main(). Assumes main() returns int and
// takes no parameters (the runnable-demo shape).

emit_main_wrapper() {
  print("int main(int argc, char** argv) { (void)argc; (void)argv; return id_main(); }");
} return void;
