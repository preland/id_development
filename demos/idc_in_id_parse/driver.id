// TEMPORARY driver: parse a single expression and print it, to validate the
// expression layer. Replaced by program parsing once statements/functions land.

main(int argc, string[] argv) {
  setup();
  load();
  run();
} return int 0;

run() {
  int[] pos = [0];
  int root = parse_expr(pos);
  print(print_expr(root));
} return void;
