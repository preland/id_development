// Driver: set up state, load the token stream, parse all functions in the
// file, then print the program's structure.

main(int argc, string[] argv) {
  setup();
  load();
  run();
} return int 0;

run() {
  int[] pos = [0];
  parse_program(pos);
  print_program();
} return void;
