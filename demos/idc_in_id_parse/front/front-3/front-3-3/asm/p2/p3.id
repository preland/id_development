asm_tail(int[] pos) {
  advance(pos);
  push((import afret), parse_type(pos));
  push((import afretn), cur_text(pos));
} return int asm_fin(pos);

asm_fin(int[] pos) {
  advance(pos);
  skip_semi(pos);
} return int 0;

init_asm_all() {
  init_asm();
  init_asm2();
  init_files();
} return void;
