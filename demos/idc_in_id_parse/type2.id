// call/bin dispatch, and the return type of a called name.

type_of4(int id) {
  string t = type_bin(id);
  if(k_of(id) == "call") {
    t = type_call(s1_of(id));
  }
} return string t;

type_call(string name) {
  string t = builtin_type(name);
  if(t == "?") {
    t = lookup_func(name);
  }
} return string t;

// return type of a builtin, or "?" if `name` is not a builtin
builtin_type(string name) {
  string t = "?";
  if(name == "print") {
    t = "void";
  } else {
    t = bt2(name);
  }
} return string t;
