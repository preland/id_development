// type_of(id): the id type of an expression node. Dispatch on kind.
//   int -> int    str -> string    float -> float    var -> its declared type
//   call -> callee/builtin return type    bin -> int, or string for concat

type_of(int id) {
  string t = "int";
  if(k_of(id) != "int") {
    t = type_of2(id);
  }
} return string t;

type_of2(int id) {
  string t = "string";
  if(k_of(id) != "str") {
    t = type_of_float(id);
  }
} return string t;

type_of3(int id) {
  string t = lookup_var(s1_of(id));
  if(is_nameref(id) == 0) {
    t = type_of4(id);
  }
} return string t;
