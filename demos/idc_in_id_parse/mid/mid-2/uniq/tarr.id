// The type of an array literal: its first element's type plus "[]". An empty
// literal's type is never consulted (it only appears as a typed decl's rhs).

type_arr(int id) {
  string t = "int[]";
  if(len(l1_of(id)) > 0) {
    t = type_of(l1_of(id)[0]) + "[]";
  }
} return string t;
