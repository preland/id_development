// Driver: set up state, load the token stream, parse one expression, then
// print and evaluate the resulting AST.

main(int argc, string[] argv) {
  setup();
  load();
  calc();
} return int 0;

calc() {
  int[] pos = [0];
  int root = parse_expr(pos);
  show(root);
} return void;

show(int root) {
  string print_ast_v = print_ast(root);
  print(print_ast_v);
  show_tail(root);
} return void;
