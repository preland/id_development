// parse_int already spends its 3 actions naming the token text and its
// value, so advancing past it and building the node is a tail call.
parse_int_tail(int[] pos, int value) {
  advance(pos);
  int ret_i = node_int(value);
} return int ret_i;
