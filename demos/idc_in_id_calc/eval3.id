// Last operator helper: '/' is integer division, otherwise '%' remainder.

apply4(string op, int l, int r) {
  int v = 0;
  if(op == "/") {
    v = l / r;
  } else {
    v = l % r;
  }
} return int v;
