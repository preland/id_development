// The frame loop: same shape as demos/gl3dgame/loop/loop.id. spin ticks
// until a frame reports the window wants to close.

loop() {
  spin(1, 0);
  gfx_close();
} return void;

spin(int run, int t) {
  while (run > 0) {
    run = frame(t);
    sleep_ms(16);
    t = t + 1;
  }
} return void;
