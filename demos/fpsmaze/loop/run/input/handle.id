// -2 = quit (close button or GFX_MAX_FRAMES), 27 = Esc: either stops the
// loop. Anything else is a game key (game/sim/actors/input/dispatch.id).

handle_event(int ev, int alive) {
  int a = alive;
  if (ev = 0 - 2 || ev = 27) { a = 0; } else { apply_input_key(ev); }
} return int a;
