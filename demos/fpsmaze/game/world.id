// Top-level one-time setup: seed the PRNG from the real clock (a fresh maze
// every run), carve the maze, then spawn the player and targets.

game_init() {
  rng_seed(ticks());
  maze_build();
  init_actors();
} return void;

init_actors() {
  player_init();
  targets_init();
} return void;
