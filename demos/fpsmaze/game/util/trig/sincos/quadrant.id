// Split out of sincos.id -- that file was already at 3 functions once
// sin_deg needed a helper for its own action count.

sin_by_quadrant(int q, int r) {
  int v = 0;
  if (q < 2) { v = sin_q01(q, r); } else { v = sin_q23(q, r); }
} return int v;
