// The sine table and its quadrant fold live in idstd now (fx_sin_deg,
// fx_cos_deg); what stays is the yaw normaliser the player's turn uses.
// Fold any integer degree (negative or >359) into 0..359.
norm_deg(int d) {
  int r = d % 360;
  if (r < 0) { r = r + 360; }
} return int r;
