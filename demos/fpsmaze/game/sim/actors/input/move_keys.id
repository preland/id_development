// w=119 forward, s=115 back, a=97 strafe left, d=100 strafe right.

try_move_fwd(int ev) {
  if (ev = 119) { move_forward(); } else if (ev = 115) { move_backward(); }
} return void;

try_move_strafe(int ev) {
  if (ev = 97) { strafe_left(); } else if (ev = 100) { strafe_right(); }
} return void;
