// The step counter half of ray_advance (../logic/step.id) -- ray_loop's
// 60-step cap reads this back through rc_get(2).

ray_advance_step() {
  int rc_get_v3 = rc_get(2);
  rc_set(2, rc_get_v3 + 1);
} return void;
