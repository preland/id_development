// The new target's random room position: split out of add_one_target (and
// split again per axis) because the two pushes each need their own random
// draw, which already fills a 3-action budget on its own.

add_target_pos() {
  add_target_x();
  add_target_z();
} return void;

add_target_x() {
  int rand_room_x_v = rand_room_x();
  push((import tgx), rand_room_x_v);
} return void;

add_target_z() {
  int rand_room_z_v = rand_room_z();
  push((import tgz), rand_room_z_v);
} return void;
