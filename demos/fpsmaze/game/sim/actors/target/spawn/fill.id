fill_targets(int i) {
  while (i < num_targets()) {
    i = add_one_target(i);
  }
} return void;

add_one_target(int i) {
  push((import tgx), rand_room_x());
  push((import tgz), rand_room_z());
} return int i + 1;
