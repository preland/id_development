// gen_step's own tail: gathering which neighbors are still carvable and
// handing them to do_step, split out because run.id was already full at
// gen_maze/run_gen/gen_step.

gen_step_at(int x, int z) {
  int[] gather_valid_v = gather_valid(x, z);
  do_step(x, z, gather_valid_v);
} return void;
