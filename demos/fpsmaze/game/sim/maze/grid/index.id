// Flatten a (x,z) grid coordinate into an index into the exported `mgrid`
// list (row-major, z*width+x). Grid values: 1 = wall/blocked, 0 = open floor.

grid_index(int x, int z) {
} return int z * grid_w() + x;
