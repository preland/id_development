// One-time GPU mesh setup: wall/target cube geometry (the floor is cheap
// enough to rebuild every frame -- see mesh/objects/floor).

scene_init() {
  init_wall_mesh();
  init_target_mesh();
} return void;
