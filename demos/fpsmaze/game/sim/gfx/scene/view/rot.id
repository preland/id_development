// yaw is stored as plain integer degrees (game/util/trig) so it's scaled by
// 1000 only here, at the native seam.

view_rot() {
  int ps_get_v = ps_get(2);
  int ret_i = gl_mat_rotate_y(0 - ps_get_v * 1000);
} return int ret_i;
