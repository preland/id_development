// The target cube's mesh, built once at startup: a smaller bright-red cube,
// easy to pick out against the gray maze.

init_target_mesh() {
  export int[] target_verts = d3_cube_verts(400);
  export int[] target_colors = d3_cube_colors(255 * 65536 + 60 * 256 + 60);
} return void;
