// Top of the per-frame draw chain: clear, floor, walls, targets, present.

draw_frame(int view) {
  gl_begin_frame(10, 10, 20);
  draw_floor(view);
  draw_all_objects(view);
} return void;

draw_all_objects(int view) {
  draw_walls(view);
  draw_targets(view);
  gl_end_frame();
} return void;
