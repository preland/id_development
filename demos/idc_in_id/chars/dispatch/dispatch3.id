// decimal number, or hand off
scan_decop(string src, int i) {
  int ni = 0;
  if(is_digit(charat(src, i))) {
    ni = scan_number(src, i);
  } else {
    ni = scan_strop(src, i);
  }
} return int ni;
