// The translate half of build_view's modelview: lift to cruise altitude and
// slide the world forward past the camera by flown(t).

forward_translate(int t) {
  int alt = altitude(t);
  int flown_v = flown(t);
  int ret_i = gl_mat_translate(0, 0 - alt, flown_v);
} return int ret_i;
