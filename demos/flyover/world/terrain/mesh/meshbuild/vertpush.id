// Push one vertex slot's 3 coordinates onto (import verts). Split from
// fill_verts (vertbuild.id) to keep each function's action count at or under
// 3 -- same decomposition demos/gl3d/scene/mesh/meshbuild/vertpush.id uses.

push_vertex(int slot) {
  int c = slot_corner(slot);
  push_xyz(c);
} return void;

push_xyz(int c) {
  push((import verts), corner_x(c));
  push((import verts), corner_y(c));
  push((import verts), corner_z(c));
} return void;
