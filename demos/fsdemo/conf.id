// Dependencies for fsdemo. The filesystem backend (open/read/write/close) is
// attached here rather than with a --backend flag, so the whole build is just:
//     bin/idc demos/fsdemo -o fsdemo
import "../../backends/fs"
