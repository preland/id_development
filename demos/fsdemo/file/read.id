// Reading: ask how big the file is, size a buffer to match, fill it.
show(string path) {
  int fs_size_v = fs_size(path);
  int[] buf = zeros(fs_size_v);
  show_tail(path, buf);
} return void;

render(int h, int[] buf) {
  int len_v = len(buf);
  int n = fs_read(h, buf, len_v);
  render_tail(h, n, buf);
} return void;
