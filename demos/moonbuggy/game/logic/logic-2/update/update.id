// Input + world update. 'q' quits at any time; otherwise the playing state
// advances the world (and reads jump input), while the crashed state only
// listens for 'r' to restart.

update(int k) {
  if(k == 113) {
    gs_set(7, 1);
  } else {
    update2(k);
  }
} return void;

update2(int k) {
  if(gs_get(3) == 1) {
    update_play(k);
  } else {
    update_dead(k);
  }
} return void;
