// bump_score already spends its 3 actions crediting the distance score, so
// crediting the tick counter is a tail call.
bump_tick() {
  int gs_get_v2 = gs_get(4);
  gs_set(4, gs_get_v2 + 1);
} return void;
