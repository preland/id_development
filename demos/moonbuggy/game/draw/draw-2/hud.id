// The heads-up display: score line on top, controls hint on the bottom row.

draw_hud() {
  int gs_get_v = gs_get(0);
  draw_text(1, 0, "MOON BUGGY    score: " + gs_get_v, 8);
  draw_text(1, (import scrh) - 1, "[space] jump   [q] quit", 6);
} return void;
