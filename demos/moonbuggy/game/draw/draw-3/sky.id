// The backdrop: a moon near the top-right and a sparse star field.

draw_sky() {
  term_text((import term_w) - 6, 1, "(  )", 8);
  draw_stars(3);
} return void;

draw_stars(int x) {
  while(x < (import term_w)) {
    term_set(x, 2, 46, 6);
    x = x + 7;
  }
} return void;
