// The backdrop: a moon near the top-right and a sparse star field.

draw_sky() {
  draw_text((import scrw) - 6, 1, "(  )", 8);
  draw_stars(3);
} return void;

draw_stars(int x) {
  while(x < (import scrw)) {
    set_cell(x, 2, 46, 6);
    x = x + 7;
  }
} return void;
