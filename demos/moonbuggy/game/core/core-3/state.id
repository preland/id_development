// Game-state accessors over the exported `mb` list, plus the two fixed
// geometry constants: the buggy's column and the ground row.

gs_get(int i) {
} return int (import mb)[i];

gs_set(int i, int v) {
  lset((import mb), i, v);
} return void;

bx() {
} return int 10;
