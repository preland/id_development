// One frame: read the latest key, update the world, repaint. wait_cont sleeps
// for the frame interval and reports whether to keep looping (stops on quit).

frame() {
  int k = last_key();
  update(k);
  paint();
} return int wait_cont(k);

wait_cont(int k) {
  sleep_ms(70);
} return int 1 - gs_get(7);
