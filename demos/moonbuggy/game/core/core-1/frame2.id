// frame() already spends its 3 actions reading the key and updating the
// world, so painting and waiting is a tail call.
frame_tail(int k) {
  paint();
  int ret_i = wait_cont(k);
} return int ret_i;
