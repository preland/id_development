// Printing the table.

report() {
  int i = 0;
  while(i < len((import words))) {
    line(i);
    i = i + 1;
  }
} return void;

line(int i) {
  print((import words)[i] + " " + (import counts)[i]);
} return void;
