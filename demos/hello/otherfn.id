//companion file for hello_world.id: testfn() calls otherfn(), which is not
//defined in that file (max 3 functions per file). Functions resolve across
//input files automatically; only variables need export/import.
otherfn() {
  print("otherfn was called");
} return void;
