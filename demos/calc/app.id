// calc demo, part 2: the entrypoint. Calls add/classify across files and
// publishes the result with export so the process exit code can return it.

main(int argc, string[] argv) {
  export int total = add(40, 2);
  string kind = classify(total);
  print("total = " + total + " (" + kind + ")");
} return int total;
