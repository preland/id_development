// The rest of a frame after the cursor is homed: every row, then the
// frame-finish (reset color + flush) -- split out of render() to stay
// within the action limit.

render_body() {
  render_rows(0);
  finish_frame();
} return void;
