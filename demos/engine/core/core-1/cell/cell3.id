// The attribute half of write_cell, split out to stay within the action
// limit -- the character write happens first, then this.

write_cell_attr(int x, int y, int attr) {
  int cell_idx_v = cell_idx(x, y);
  lset((import scrc), cell_idx_v, attr);
} return void;
