// A marker is "#file " at the start of a line. 35 is '#', 102 is 'f'.
is_marker(string src, int i) {
  int ok = 0;
  if(charat(src, i) == 35 && charat(src, i + 1) == 102) {
    ok = 1;
  }
} return int ok;
