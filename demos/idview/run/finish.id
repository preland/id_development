// run() already spends its 3 actions scanning the input and naming its
// length, so closing the last file's span and showing one is a tail call.
finish(string src, int len_v) {
  close_last(len_v);
  show(src);
} return void;
