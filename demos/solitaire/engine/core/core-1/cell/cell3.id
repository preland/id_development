// write_cell already spends its 3 actions on the character cell, so the
// attribute cell is a tail call.
write_attr(int x, int y, int attr) {
  int cell_idx_v = cell_idx(x, y);
  lset((import scrc), cell_idx_v, attr);
} return void;
