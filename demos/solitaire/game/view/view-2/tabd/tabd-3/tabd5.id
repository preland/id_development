// Mark the selected source run (in a column) with a red '*' down the left edge.

src_marker(int col, int x) {
  if(ui_get(1) == col + 6) {
    mark_src(col, x);
  }
} return void;

mark_src(int col, int x) {
  int j = ui_get(2);
  while(j < col_len(col)) {
    set_cell(x - 1, 7 + j, 42, 1);
    j = j + 1;
  }
} return void;
