// The face of a foundation: its top card if any, else a "-X-" suit placeholder.

found_card_face(int x, int i) {
  if((import found)[i] > 0) {
    draw_card_at(x, 4, ((import found)[i] - 1) * 4 + i);
  } else {
    draw_text(x, 4, suit_slot(i), 7);
  }
} return void;

suit_slot(int i) {
} return string "-" + (import suits)[i] + "-";
