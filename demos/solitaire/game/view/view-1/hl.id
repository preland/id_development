// Cursor / source markers under a top pile: a bright caret marks the cursor's
// pile, a red plus marks the selected source.

hl_if(int pile, int x, int y) {
  if(ui_get(0) == pile) {
    draw_text(x, y + 1, "^^^", 13);
  } else {
    hl_src(pile, x, y);
  }
} return void;

hl_src(int pile, int x, int y) {
  if(ui_get(1) == pile) {
    draw_text(x, y + 1, "+++", 1);
  }
} return void;
