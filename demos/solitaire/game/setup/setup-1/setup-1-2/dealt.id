// Deal the tableau: column col (0-based) gets col+1 cards, of which col are
// face down (the top one is face up).

deal_tableau(int col) {
  while(col < 7) {
    deal_col(col);
    col = col + 1;
  }
} return void;

deal_col(int col) {
  fill_col(col, 0);
  lset((import fd), col, col);
} return void;

fill_col(int col, int j) {
  while(j < col + 1) {
    push(col_of(col), pop((import deck)));
    j = j + 1;
  }
} return void;
