// Build the 52-card deck, then shuffle it.

build_deck() {
  make_deck();
  shuffle();
} return void;

make_deck() {
  export int[] deck = [];
  fill_deck(0);
} return void;

fill_deck(int i) {
  while(i < 52) {
    push((import deck), i);
    i = i + 1;
  }
} return void;
