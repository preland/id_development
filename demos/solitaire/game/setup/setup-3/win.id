// The other two foundations complete? (Split out to keep the boolean small.)
// Also the column-length helper shared by the move and render code.

found_hi() {
  int ok = 0;
  if((import found)[2] == 13 && (import found)[3] == 13) {
    ok = 1;
  }
} return int ok;

col_len(int col) {
  int[] col_of_v = col_of(col);
  int ret_i = len(col_of_v);
} return int ret_i;
