// Pad a card label to a 3-cell width, and draw a face-up card at a position.

pad3(string s) {
  string out = s;
  if(len(s) < 3) {
    out = s + " ";
  }
} return string out;

draw_card_at(int x, int y, int c) {
  draw_text(x, y, pad3(card_str(c)), card_attr(c));
} return void;
