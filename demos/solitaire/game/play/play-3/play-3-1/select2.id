// col_len(col) reads a call-in-call if folded into sel_tab_ok's condition,
// and the cursor column is only valid once ui_get(0) >= 6 has already
// passed, so this composition must stay a separate, later-evaluated call.
tab_has_faceup() {
  int ui_get_v = ui_get(0);
  int col = ui_get_v - 6;
  int ok = col_len(col) > (import fd)[col];
} return int ok;
