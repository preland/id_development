// The top card of a column, and the sequence rule: c continues t if c is one
// rank lower and the opposite color.

top_of(int d) {
} return int col_of(d)[col_len(d) - 1];

seq_match(int t, int c) {
  int ok = 0;
  if(card_rank(c) == card_rank(t) - 1 && card_red(c) != card_red(t)) {
    ok = 1;
  }
} return int ok;
