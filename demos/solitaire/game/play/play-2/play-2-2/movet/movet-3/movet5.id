// Remove the selected cards from the source: capture the count first (the
// column shrinks as we pop), then pop that many.

remove_src_cards() {
  pop_n(src_count(), 0);
} return void;

pop_n(int n, int j) {
  while(j < n) {
    remove_src_one();
    j = j + 1;
  }
} return void;
