// found_do already spends its 3 actions removing the source card and
// naming its rank, so recording it on the foundation and finishing the
// move is a tail call.
found_do_tail(int d, int card_rank_v) {
  lset((import found), d, card_rank_v);
  finish_move();
} return void;
