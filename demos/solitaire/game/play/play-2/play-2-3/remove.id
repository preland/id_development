// Remove one card from the top of the source pile (waste or tableau column).

remove_src_one() {
  if(ui_get(1) == 1) {
    pop((import waste));
  } else {
    pop_col_src();
  }
} return void;

pop_col_src() {
  pop(col_of(ui_get(1) - 6));
} return void;
