// Remove one card from the top of the source pile (waste or tableau column).

remove_src_one() {
  if(ui_get(1) == 1) {
    pop((import waste));
  } else {
    pop_col_src();
  }
} return void;

pop_col_src() {
  int ui_get_v = ui_get(1);
  int[] col_of_v = col_of(ui_get_v - 6);
  pop(col_of_v);
} return void;
