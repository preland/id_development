// Apply a cursor delta, clamped to the 0..12 pile range.

cursor_move(int d) {
  int ui_get_v = ui_get(0);
  int wrap_cur_v = wrap_cur(ui_get_v + d);
  ui_set(0, wrap_cur_v);
} return void;

wrap_cur(int c) {
  int r = c;
  if(c < 0) {
    r = 0;
  }
  if(c > 12) {
    r = 12;
  }
} return int r;
